执行已获用户确认的方案 A：修复"破韧后 BOSS 朝向持续追踪主角"。纯 C++，改 3 个文件（含 1 个头文件），不动任何资产。

## 1. Source/First/Public/AI/BTService_UpdateBossState.h
- 新增 `UPROPERTY(EditAnywhere, Category="Boss|State") FGameplayTagContainer ExecutionControlTags;`（与既有 ActionBlockingTags 同风格，含注释）。

## 2. Source/First/Private/AI/BTService_UpdateBossState.cpp
- 构造函数初始化 ExecutionControlTags：Executable / BeingExecuted / Executed 三个标签。
- TickNode 旋转模式选择：在 bIsDead 分支之后插入 `bExecutionControlled` 分支 → `DesiredRotationMode = Frozen` + `StopMovement()`。
- 末尾 ClearFocus 条件组追加 `bExecutionControlled ||`，确保每帧清掉 Gameplay Focus。

## 3. Source/First/Private/AI/Tasks/BTTask_BossAlert.cpp
- TickTask 中断条件追加：Boss 无效/死亡、或带三个处决标签之一 → ClearFocus + FinishLatentTask(Failed)，不再执行每帧 SetActorRotation。
- ExecuteTask 开头加同样防线（死亡/处决接管时不进入对峙、不 SetFocus、不写黑板）。

## 验证
- UnrealBuildTool 编译 FirstEditor（Development）目标确认零错误（本机 UBT 不可用时给出编辑器编译指引）。
- 代码层面自查：处决全程（破韧 Loop → Target 蒙太奇 → 起立）三条自动转向路径（bOrientRotationToMovement / bUseControllerDesiredRotation / bUseControllerRotationYaw）均为关；能力 EndAbility 后服务恢复常规模式。