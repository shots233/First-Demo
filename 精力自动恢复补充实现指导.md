# 精力自动恢复补充实现指导

> 适用项目：`First`（UE 5.6 / C++ / GAS）  
> 本文只提供实现指导，不会自动修改你的 C++ 或资产。  
> 目的：为角色补充“精力（Stamina）自动恢复”机制，并与你现有的闪避消耗、属性夹紧框架保持一致。

---

## 1. 现状核对

你项目的精力相关部分已经存在：

| 功能 | 位置 | 说明 |
|---|---|---|
| 精力属性 | `Source/First/Public/AbilitySystem/FirstAttributeSet.h` | `Stamina`、`MaxStamina` |
| 精力夹紧 | `Source/First/Private/AbilitySystem/FirstAttributeSet.cpp` | `PostGameplayEffectExecute` 已把 Stamina 限制在 `[0, MaxStamina]` |
| 闪避消耗 | `Source/First/Public/AbilitySystem/GameplayEffects/FirstGE_DodgeCost.h/.cpp` | Instant GE，Stamina -20 |
| 出生属性 GE | `/Game/DKCharacter/GAS/Effects/GE_DK_Initialize` | 设置 MaxStamina 等初始值 |
| 出生数据资产 | `/Game/DKCharacter/GAS/Data/DA_DKStartUpData` | 应用初始 GE、授予 Ability |
| 出生应用逻辑 | `DataAsset_StartUpDataBase.cpp` | 遍历 `StartUpGameplayEffects` 并应用 |

缺失的只有一块：**没有任何机制让 Stamina 自动涨回来**。

---

## 2. 设计目标

```text
1. 精力随时间自动恢复；
2. 恢复不会超过 MaxStamina；
3. 完全复用现有 GAS 框架，不写自定义 Tick；
4. 参数可调（每秒恢复量、恢复间隔）。
```

推荐实现方式：

```text
一个“无限持续 + 周期性执行”的 GameplayEffect：
  每 0.5 秒执行一次
  每次给 Stamina 添加固定数值
  Stamina 超过 MaxStamina 时由现有夹紧逻辑自动封顶
```

这就是 GAS 中最标准的属性再生写法（和生命自动回复同理）。

---

## 3. 前置知识讲解

### 3.1 GameplayEffect 的三种持续时间

| 类型 | 英文 | 含义 |
|---|---|---|
| 瞬间 | `Instant` | 执行一次立即结束，闪避消耗就是这种 |
| 有持续时间 | `HasDuration` | 持续一段时间，期间可以周期性执行 |
| 无限 | `Infinite` | 一直持续，直到被移除；配合 Period 可以周期执行 |

精力恢复要用 **Infinite**：

```text
不主动移除，就永远生效；
每过 Period 秒，重新执行一次 Modifiers。
```

### 3.2 Period（周期）

`Period` 是 GE 上的一个属性，表示：

```text
每隔多少秒执行一次 Modifiers。
```

例如：

```text
Period = 0.5 秒
Modifier = Stamina +10
```

效果就是每 0.5 秒恢复 10 点精力，相当于每秒 20 点。

### 3.3 Modifier（修改器）

```cpp
FGameplayModifierInfo Modifier;
Modifier.Attribute = UFirstAttributeSet::GetStaminaAttribute();
Modifier.ModifierOp = EGameplayModOp::Additive;
Modifier.ModifierMagnitude = FGameplayEffectModifierMagnitude(FScalableFloat(10.f));
```

含义：

```text
修改目标 = Stamina
运算方式 = 加法
数值     = +10（每次周期执行）
```

`FScalableFloat` 的意义：数值可以用“曲线/倍率”扩展，目前直接写死 10，以后想按等级成长可以替换。

### 3.4 为什么不需要改 AttributeSet

你的 `PostGameplayEffectExecute` 已经有这段：

```cpp
if (Data.EvaluatedData.Attribute == GetStaminaAttribute())
{
	SetStamina(FMath::Clamp(GetStamina(), 0.f, GetMaxStamina()));
}
```

周期性 GE 每执行一次，就是一次“Stamina 被修改”，会自动走这个夹紧逻辑：

```text
恢复后如果超过 MaxStamina → 自动变成 MaxStamina
```

所以属性侧**零改动**。

---

## 4. 新增 C++ GameplayEffect

### 4.1 新建头文件

路径建议：

```text
Source/First/Public/AbilitySystem/GameplayEffects/FirstGE_StaminaRegen.h
```

新建文件，内容：

```cpp
#pragma once

#include "CoreMinimal.h"
#include "GameplayEffect.h"
#include "FirstGE_StaminaRegen.generated.h"

/**
 * 精力自动恢复。
 *
 * 无限持续时间 + 周期性执行：
 * 每 Period 秒给 Stamina 添加 RegenPerPeriod 数值。
 * 超过 MaxStamina 的部分由 AttributeSet 的夹紧逻辑自动截断。
 */
UCLASS()
class FIRST_API UFirstGE_StaminaRegen : public UGameplayEffect
{
	GENERATED_BODY()

public:
	UFirstGE_StaminaRegen();
};
```

### 4.2 新建 CPP 文件

路径建议：

```text
Source/First/Private/AbilitySystem/GameplayEffects/FirstGE_StaminaRegen.cpp
```

新建文件，内容：

```cpp
#include "AbilitySystem/GameplayEffects/FirstGE_StaminaRegen.h"

#include "AbilitySystem/FirstAttributeSet.h"

UFirstGE_StaminaRegen::UFirstGE_StaminaRegen()
{
	// 1. 无限持续：应用后一直生效，直到被主动移除。
	DurationPolicy = EGameplayEffectDurationType::Infinite;

	// 2. 周期：每 0.5 秒执行一次下面的 Modifiers。
	Period = FScalableFloat(0.5f);

	// 3. 每次周期执行：Stamina +10。
	//    闪避消耗一次是 -20，所以每 1 秒恢复量 ≈ 每次消耗量的 1 倍。
	FGameplayModifierInfo RegenModifier;
	RegenModifier.Attribute = UFirstAttributeSet::GetStaminaAttribute();
	RegenModifier.ModifierOp = EGameplayModOp::Additive;
	RegenModifier.ModifierMagnitude = FGameplayEffectModifierMagnitude(FScalableFloat(10.f));

	Modifiers.Add(RegenModifier);
}
```

### 4.3 逐行讲解

```cpp
DurationPolicy = EGameplayEffectDurationType::Infinite;
```

和 `FirstGE_DodgeCost` 的 `Instant` 相对：

```text
DodgeCost：应用一次就消失 → 扣一次精力
StaminaRegen：应用后常驻 → 周期恢复
```

```cpp
Period = FScalableFloat(0.5f);
```

告诉 GAS：“这个 GE 每 0.5 秒重新执行一次”。`FScalableFloat` 是可变数值类型，现在写死 0.5。

```cpp
RegenModifier.Attribute = UFirstAttributeSet::GetStaminaAttribute();
```

和闪避消耗指向同一个属性，确保加减的是同一条数值。

```cpp
RegenModifier.ModifierOp = EGameplayModOp::Additive;
```

加法运算。闪避消耗是 -20，恢复是 +10，正好方向相反。

```cpp
Modifiers.Add(RegenModifier);
```

把修改器挂到 GE 上。以后想同时恢复生命，也可以往这个数组里再 Add 一个。

---

## 5. 把恢复 GE 应用到角色

### 5.1 推荐方式：加入出生数据资产（零额外代码）

你项目的出生流程是：

```text
DA_DKStartUpData
→ GiveToAbilitySystemComponent
→ 遍历 StartUpGameplayEffects 并应用
```

所以只需要把这个新 GE **加进出生数据资产的数组**：

1. 打开：

```text
/Game/DKCharacter/GAS/Data/DA_DKStartUpData
```

2. 找到 `StartUpGameplayEffects` 数组。
3. 点击 **+** 添加一项，选择：

```text
FirstGE_StaminaRegen
```

4. 保存。

这样角色出生时就会应用这个无限周期的恢复 GE，之后每 0.5 秒恢复 10 点精力。

> 注意：`StartUpGameplayEffects` 的注释建议放 Instant 初始属性，但代码本身对持续时间没有限制；把 Infinite GE 放进去完全合法，只是语义上它不属于“一次性出生属性”。如果你介意，可以用 5.2 的独立入口方式。

### 5.2 备选方式：在 DKStartUpData 里显式应用

如果你希望“初始属性”和“常驻恢复”分开管理，可以在 `UDataAsset_DKStartUpData` 增加一个配置项。

头文件新增：

```cpp
// 角色出生后立即应用的常驻 GE，例如精力恢复。
UPROPERTY(EditDefaultsOnly, Category="StartupData")
TSubclassOf<UGameplayEffect> OngoingStartupGameplayEffect;
```

CPP 的 `GiveToAbilitySystemComponent` 中，在 `Super::` 之后新增：

```cpp
if (OngoingStartupGameplayEffect)
{
	const UGameplayEffect* EffectCDO =
		OngoingStartupGameplayEffect.GetDefaultObject();

	InASCToGive->ApplyGameplayEffectToSelf(
		EffectCDO,
		ApplyLevel,
		InASCToGive->MakeEffectContext());
}
```

然后在 `DA_DKStartUpData` 的 `OngoingStartupGameplayEffect` 里填 `FirstGE_StaminaRegen`。

这个方式的优点是：初始数值 GE 和常驻恢复 GE 分成两个数组，语义清楚；缺点是多几行代码。

---

## 6. 参数调节与手感计算

先看你的消耗侧：

```text
闪避消耗：Stamina -20（FirstGE_DodgeCost）
闪避间隔：约 0.35 秒一次（DodgeDuration）
```

以 `GE_DK_Initialize` 设置 `MaxStamina = 100` 为例：

| 配置 | 含义 | 结果 |
|---|---|---|
| Period 0.5s，每次 +10 | 每秒 +20 | 从 0 回满需要 5 秒 |
| Period 0.5s，每次 +15 | 每秒 +30 | 从 0 回满约 3.3 秒 |
| Period 0.3s，每次 +10 | 每秒约 +33 | 从 0 回满约 3 秒 |
| Period 1s，每次 +25 | 每秒 +25 | 从 0 回满 4 秒 |

推荐起点：

```cpp
Period = 0.5f;
RegenModifier.ModifierMagnitude = FScalableFloat(10.f);
```

即“每秒恢复 20”，等于一次闪避消耗量。手感偏慢、偏保守；想更爽快就改成每次 +15。

调整位置：

- 间隔：`Period` 那一行；
- 每次恢复量：`ModifierMagnitude` 那一行。

改完重新编译即可（GE 数值写死在 C++，不需要改资产）。

---

## 7. 测试清单

| 测试 | 操作 | 预期结果 |
|---|---|---|
| 出生即有恢复 | 运行游戏，观察 Output Log 或调试属性 | Stamina 每 0.5 秒 +10 |
| 不超过上限 | 满精力时等待 | Stamina 稳定在 MaxStamina，不溢出 |
| 闪避后恢复 | 连续闪避把精力耗到 0，停止操作 | 精力自动上涨，重新能闪避 |
| 闪避消耗仍生效 | 精力满时闪避一次 | Stamina -20，随后继续恢复 |
| 死亡后不再恢复（可选） | 被打死后观察 | 若未做扩展，恢复仍会继续；见第 8 节 |

调试属性：

```text
ShowDebug AbilitySystem
```

可以在视口看到 Stamina / MaxStamina 的实时变化。

---

## 8. 可选扩展（以后再做）

### 8.1 闪避/奔跑时暂停恢复

实现思路（概念，不在此展开代码）：

```text
方案：闪避开始时移除恢复 GE，闪避结束时重新应用。
或者：给恢复 GE 增加“被 DK.Status.Dodging 阻止执行”的标签条件。
```

### 8.2 恢复数值随等级成长

把 `ModifierMagnitude` 从写死的 `FScalableFloat(10.f)` 换成引用曲线/倍率，或者改用 `SetByCaller`。

### 8.3 死亡后停止恢复

可以在死亡逻辑中移除恢复 GE，或者在恢复 GE 上增加“死亡时失效”的条件。

---

## 9. 最终职责

```text
FirstGE_DodgeCost     → 消耗精力（Instant）
FirstGE_StaminaRegen  → 恢复精力（Infinite + Period）
FirstAttributeSet     → 夹紧 Stamina 到 [0, MaxStamina]
DA_DKStartUpData      → 出生时把恢复 GE 应用到角色
```
