# 主角四方向受击与死亡实现指导

> 适用项目：`First`（UE 5.6 中文版 / C++ / GAS）  
> 编写依据：当前工程源码、`BP_DKCharacter`、`ABP_DK_Sword`、现有格挡/弹反/闪避/处决链路  
> 本册目标：主角真正掉血时，根据攻击者位于前、后、左、右播放对应受击动画；生命归零时只播放死亡，不再移动、操作、锁定或被 BOSS 继续追击  
> 重要说明：这是一份**实施指导**。创建本文件并不代表其中的 C++、蓝图或 UE 资产已经被修改

---

## 0. 完成后的最终流程

```text
BOSS 武器命中主角
  ↓
DKDefenseComponent 先判定防御
  ├─ 闪避无敌成功 → 不应用伤害 → 不受击
  ├─ 弹反成功       → 不应用伤害 → 播放弹反表现
  ├─ 格挡成功       → 不应用生命伤害 → 扣精力、播放格挡表现
  └─ 防御失败       → 应用 DamageTaken
                         ↓
                FirstAttributeSet 扣除 Health
                  ├─ Health > 0
                  │    → 发送 Shared.Event.DamageReceived
                  │    → GA_DKHitReact 读取攻击者位置
                  │    → 前 / 后 / 左 / 右受击
                  └─ Health = 0
                       → 只添加 Shared.Status.Dead
                       → GA_DKDeath 自动激活
                       → 清动作、停移动、解除锁定、播放死亡
```

这里最重要的原则是：

> **武器发生重叠不等于主角真正受伤。只有最终生命值确实下降，才触发普通受击。**

这样可以避免格挡、弹反、闪避成功后仍然播放掉血受击动画。

---

## 1. 本册采用的设计

### 1.1 受击由通用伤害事件触发

新增通用事件：

```text
Shared.Event.DamageReceived
```

事件在 `UFirstAttributeSet::PostGameplayEffectExecute()` 完成真实扣血后发送，并携带：

- `Instigator`：攻击者，本项目里就是造成伤害的 BOSS；
- `Target`：主角；
- `EventMagnitude`：本次实际伤害；
- `ContextHandle`：原始 GameplayEffect 上下文。

不建议在 `ADKCharacter` 里只监听 Health 改变再找攻击者，因为
`FOnAttributeChangeData` 只有新旧数值，没有可靠的伤害来源。四方向受击必须从伤害
`EffectContext` 获取攻击者。

当前 BOSS 伤害通过 `MakeBossDamageEffectSpecHandle()` 创建，上下文已经写入：

```cpp
ContextHandle.AddInstigator(GetAvatarActorFromActorInfo(), GetAvatarActorFromActorInfo());
```

因此当前 BOSS 攻击可以直接提供正确方向。

### 1.2 四方向表示“攻击者来自哪一侧”

本册中的四个方向统一表示：

```text
Front = 攻击者位于主角正面
Back  = 攻击者位于主角背面
Left  = 攻击者位于主角左侧
Right = 攻击者位于主角右侧
```

计算方法：

```text
                    Front
                      ↑
                      │
             Left ← 主角 → Right
                      │
                      ↓
                     Back
```

图中的方向以主角自身朝向为准，不以屏幕左右或摄像机朝向为准。

```text
ToAttacker = 攻击者位置 - 主角位置
ForwardDot = 主角前向量 · ToAttacker
RightDot   = 主角右向量 · ToAttacker

|ForwardDot| >= |RightDot|
  ├─ ForwardDot >= 0 → Front
  └─ ForwardDot < 0  → Back

否则
  ├─ RightDot >= 0 → Right
  └─ RightDot < 0  → Left
```

这个判断不依赖镜头左右，也不依赖动画资产如何命名。

### 1.3 普通受击与死亡严格互斥

- `Health > 0`：发送 DamageReceived，允许普通受击；
- `Health <= 0`：只添加 `Shared.Status.Dead`，不再发送 DamageReceived。

这样不会出现“先播放一帧受击，马上又切死亡”的竞争。

### 1.4 用玩家动作类别降低耦合

新增类别标签：

```text
DK.Ability.Action
```

攻击、闪避、格挡、破防、装备、卸下、处决都挂这个类别。普通受击只需要取消并阻止
`DK.Ability.Action`，不需要永远记住每一种具体动作。

以后新增玩家技能时，只要它也是可被受击打断的动作，就给它加上这一行：

```cpp
AssetTags.AddTag(MyGameplayTags::DK_Ability_Action);
```

受击能力无需再修改。

### 1.5 不照搬 BOSS 的受击窗口

玩家普通受击不要添加：

```text
Boss.Status.HitReactWindow
Boss.Cooldown.HitReact
```

它们是 BOSS “只有攻击后摇可被打断”的特殊规则。主角只要真正受到生命伤害，就应进入普通受击。

---

## 2. 当前工程中已确认的事实

| 项目 | 当前情况 |
|---|---|
| 主角 Mesh | `/Game/SwordAnimsetPro/Demo/Character/Mesh/SK_DKMannequin` |
| 主角骨架 | `SK_Mannequin_Skeleton` |
| 主角 AnimBP | `/Game/DKCharacter/AnimBP/Sword/ABP_DK_Sword` |
| 全身 Montage 插槽 | `DefaultSlot` 已存在 |
| 真实启动数据 | `/Game/DKCharacter/Data/DA_DKStartUpData` |
| 旧入口 | `/Game/DKCharacter/GAS/Data/DA_DKStartUpData`，不要在这里配置 |
| 生命扣除 | `FirstAttributeSet.cpp` 使用 `DamageTaken` 扣除 Health |
| 死亡标签 | Health 归零后已有 `Shared.Status.Dead` |
| 闪避无敌 | `DK.Status.Invincible` 会让伤害计算直接返回 |
| 格挡/弹反 | 成功时不会应用生命伤害，因此天然不会触发本册普通受击 |

主角可以直接使用的动画：

```text
/Game/SwordAnimsetPro/Animations/Root-Motion/Hit_01_F_Anim
/Game/SwordAnimsetPro/Animations/Root-Motion/Hit_01_B_Anim
/Game/SwordAnimsetPro/Animations/Root-Motion/Hit_01_L_Anim
/Game/SwordAnimsetPro/Animations/Root-Motion/Hit_01_R_Anim
/Game/SwordAnimsetPro/Animations/Root-Motion/Dead_Anim
```

以下是 BOSS 男骑士资源，骨架不同，不要直接给主角使用：

```text
/Game/Dark_Knight/Dark_Knight_Male/Animations/Anim_DKM_Hit_*
/Game/Dark_Knight/Dark_Knight_Male/Animations/Anim_DKM_Death
```

---

## 3. 改动总览

| 文件/资产 | 操作 | 内容 |
|---|---|---|
| `MyGameplayTags.h/.cpp` | 修改 | 新增 5 个标签 |
| `FirstAttributeSet.cpp` | 修改 | 非致命真实伤害发送 DamageReceived 事件 |
| 7 个现有玩家动作 Ability | 修改 | 添加 `DK.Ability.Action` 类别标签 |
| `DKCharacter.h/.cpp` | 修改 | 四个受击 Montage、死亡 Montage、受击时长、输入门控、死亡准备函数 |
| `FirstGA_DKHitReact.h/.cpp` | 新建 | 四方向选择、动作中断、短暂硬直 |
| `FirstGA_DKDeath.h/.cpp` | 新建 | 死亡标签触发、终止能力、停止移动、死亡动画 |
| `UBTService_UpdateCombatDistance.cpp` | 修改 | 主角死亡后 BOSS 清目标、停攻击 |
| 5 个 Montage | 新建 | 四方向受击 + 死亡 |
| `BP_DKCharacter` | 配置 | 填五个 Montage 与受击时长 |
| `DA_DKStartUpData` | 配置 | Reactive Abilities 追加受击与死亡能力 |

---

## 4. 第一步：新增 Gameplay Tags

### 4.1 修改 `MyGameplayTags.h`

文件：

```text
Source/First/Public/MyGameplayTags.h
```

在主角 Ability 身份附近新增：

```cpp
	// 所有可被受击/死亡中断的玩家动作共用类别。
	FIRST_API UE_DECLARE_GAMEPLAY_TAG_EXTERN(DK_Ability_Action);
	FIRST_API UE_DECLARE_GAMEPLAY_TAG_EXTERN(DK_Ability_HitReact);
	FIRST_API UE_DECLARE_GAMEPLAY_TAG_EXTERN(DK_Ability_Death);
```

在事件附近新增：

```cpp
	// 任意角色真正受到生命伤害后的通用事件。
	FIRST_API UE_DECLARE_GAMEPLAY_TAG_EXTERN(Shared_Event_DamageReceived);
```

在状态附近新增：

```cpp
	// 主角普通受击硬直期间存在。
	FIRST_API UE_DECLARE_GAMEPLAY_TAG_EXTERN(DK_Status_HitReact);
```

### 4.2 修改 `MyGameplayTags.cpp`

文件：

```text
Source/First/Private/MyGameplayTags.cpp
```

新增对应定义：

```cpp
	UE_DEFINE_GAMEPLAY_TAG(DK_Ability_Action, "DK.Ability.Action");
	UE_DEFINE_GAMEPLAY_TAG(DK_Ability_HitReact, "DK.Ability.HitReact");
	UE_DEFINE_GAMEPLAY_TAG(DK_Ability_Death, "DK.Ability.Death");

	UE_DEFINE_GAMEPLAY_TAG(Shared_Event_DamageReceived, "Shared.Event.DamageReceived");

	UE_DEFINE_GAMEPLAY_TAG(DK_Status_HitReact, "DK.Status.HitReact");
```

### 4.3 编译闸门 1

只改完标签后先 Build 一次。常见错误：

- 只在 `.h` 声明，没有在 `.cpp` 定义；
- C++ 名称和字符串拼写不一致；
- 漏掉 `FIRST_API`。

---

## 5. 第二步：为现有玩家动作添加通用类别

以下 Ability 的构造函数中都已经有局部变量：

```cpp
FGameplayTagContainer AssetTags;
```

在各自 `SetAssetTags(AssetTags);` **之前**添加：

```cpp
AssetTags.AddTag(MyGameplayTags::DK_Ability_Action);
```

需要修改的 7 个文件：

| 文件 | 当前具体身份标签 |
|---|---|
| `Private/AbilitySystem/Abilities/FirstGA_DKEquipSword.cpp` | `DK_Ability_Equip_Sword` |
| `Private/AbilitySystem/Abilities/DK/FirstGA_DKUnequipSword.cpp` | `DK_Ability_Unequip_Sword` |
| `Private/AbilitySystem/Abilities/DK/FirstGA_DKLightAttack.cpp` | `DK_Ability_Attack`、`DK_Ability_Attack_Light_Sword` |
| `Private/AbilitySystem/Abilities/DK/FirstGA_DKDodge.cpp` | `DK_Ability_Dodge` |
| `Private/AbilitySystem/Abilities/DK/FirstGA_DKGuardParry.cpp` | `DK_Ability_GuardParry` |
| `Private/AbilitySystem/Abilities/DK/FirstGA_DKGuardBreak.cpp` | `DK_Ability_GuardBreak` |
| `Private/AbilitySystem/Abilities/DK/FirstGA_DKExecute.cpp` | `DK_Ability_Execute` |

以轻攻击为例，修改后应是：

```cpp
FGameplayTagContainer AssetTags;
AssetTags.AddTag(MyGameplayTags::DK_Ability_Action);
AssetTags.AddTag(MyGameplayTags::DK_Ability_Attack);
AssetTags.AddTag(MyGameplayTags::DK_Ability_Attack_Light_Sword);
SetAssetTags(AssetTags);
```

不要给以下 Ability 添加 `DK.Ability.Action`：

- `FirstGA_DKSpawnSword`：出生初始化，不是玩家动作；
- `FirstGA_DKHitReact`：它负责取消 Action，不能把自己归入 Action；
- `FirstGA_DKDeath`：死亡优先级高于普通 Action。

### 5.1 为什么同时保留旧标签

`DK.Ability.Action` 是总类别，旧标签仍然表达具体身份。例如：

```text
同一个轻攻击 Ability 的 AssetTags 同时包含：
  - DK.Ability.Action
  - DK.Ability.Attack
  - DK.Ability.Attack.Light.Sword
```

以后闪避只取消攻击时仍可使用 `DK.Ability.Attack`；受击需要统一取消所有动作时使用
`DK.Ability.Action`。两者不冲突。

### 5.2 编译闸门 2

这一步只增加 Asset Tag，不改变动作逻辑。编译后先快速验证原来的攻击、闪避、格挡、处决仍能正常使用。

---

## 6. 第三步：真实掉血后发送带攻击者的事件

文件：

```text
Source/First/Private/AbilitySystem/FirstAttributeSet.cpp
```

当前 `DamageTaken` 分支中已经有：

```cpp
if (NewHealth <= 0.f)
{
	// 添加 Shared.Status.Dead
}
```

把这段死亡判断替换为下面的完整版本。原有扣血、日志和 UI 广播不变：

```cpp
		AActor* TargetActor = Data.Target.GetAvatarActor();
		const float ActualDamage = OldHealth - NewHealth;

		if (NewHealth <= 0.f)
		{
			if (TargetActor)
			{
				if (UAbilitySystemComponent* TargetASC =
					UAbilitySystemBlueprintLibrary::GetAbilitySystemComponent(TargetActor))
				{
					// 防止尸体继续受击时把 Loose Tag 计数不断叠加。
					if (!TargetASC->HasMatchingGameplayTag(
						MyGameplayTags::Shared_Status_Dead))
					{
						TargetASC->AddLooseGameplayTag(
							MyGameplayTags::Shared_Status_Dead);
					}
				}
			}
		}
		else if (TargetActor && ActualDamage > 0.f)
		{
			// 只有非致命的真实生命伤害才发送普通受击事件。
			const FGameplayEffectContextHandle DamageContext =
				Data.EffectSpec.GetContext();

			AActor* DamageInstigator = DamageContext.GetOriginalInstigator();
			if (!DamageInstigator)
			{
				DamageInstigator = DamageContext.GetEffectCauser();
			}

			FGameplayEventData EventData;
			EventData.EventTag =
				MyGameplayTags::Shared_Event_DamageReceived;
			EventData.Instigator = DamageInstigator;
			EventData.Target = TargetActor;
			EventData.EventMagnitude = ActualDamage;
			EventData.ContextHandle = DamageContext;

			UAbilitySystemBlueprintLibrary::SendGameplayEventToActor(
				TargetActor,
				MyGameplayTags::Shared_Event_DamageReceived,
				EventData);
		}
```

当前文件已经包含：

```cpp
#include "AbilitySystemBlueprintLibrary.h"
#include "GameplayEffectExtension.h"
#include "MyGameplayTags.h"
```

再显式新增：

```cpp
#include "Abilities/GameplayAbilityTypes.h"
```

虽然部分 GAS 头文件可能间接包含 `FGameplayEventData`，这里显式包含更稳定，也能避免以后引擎头文件依赖顺序变化导致编译失败。

### 6.1 不要在致命分支继续发送事件

错误写法：

```cpp
SendGameplayEventToActor(... DamageReceived ...);
if (NewHealth <= 0.f)
{
	AddLooseGameplayTag(Shared_Status_Dead);
}
```

这会让受击和死亡在同一帧竞争 Montage。必须保持：

```text
NewHealth > 0  → DamageReceived
NewHealth <= 0 → Dead
```

### 6.2 所有伤害继续走 DamageTaken

以后不要让攻击 Ability 直接 `SetHealth()`。否则可能绕过：

- 防御判定；
- DamageReceived 事件；
- 死亡 Tag；
- UI 更新。

本册新增的 Shared 事件不会替换现有 BOSS 受击规则。`ABossCharacter` 仍可继续用自己的
`Boss.Event.HitReact + Boss.Status.HitReactWindow` 控制“只有窗口内才普通硬直”；BOSS 没有监听
`Shared.Event.DamageReceived` 时，这个额外通用事件不会造成第二次受击。

### 6.3 编译闸门 3

编译后暂时还不会播放受击，因为接收事件的 Ability 尚未创建，这是正常的。确认原有扣血和死亡 Tag 没有失效即可。

---

## 7. 第四步：扩展 `ADKCharacter`

### 7.1 修改 `DKCharacter.h`

文件：

```text
Source/First/Public/Character/DKCharacter.h
```

前置声明区域确认有：

```cpp
class UAnimMontage;
```

在现有格挡/破防动画属性附近新增：

```cpp
	// 四个方向表示“攻击者来自主角哪一侧”，不是身体倒向哪一侧。
	UPROPERTY(EditDefaultsOnly, BlueprintReadOnly,
		Category="Combat|Hit React|Anim",
		meta=(AllowPrivateAccess="true"))
	TObjectPtr<UAnimMontage> HitReactFrontMontage;

	UPROPERTY(EditDefaultsOnly, BlueprintReadOnly,
		Category="Combat|Hit React|Anim",
		meta=(AllowPrivateAccess="true"))
	TObjectPtr<UAnimMontage> HitReactBackMontage;

	UPROPERTY(EditDefaultsOnly, BlueprintReadOnly,
		Category="Combat|Hit React|Anim",
		meta=(AllowPrivateAccess="true"))
	TObjectPtr<UAnimMontage> HitReactLeftMontage;

	UPROPERTY(EditDefaultsOnly, BlueprintReadOnly,
		Category="Combat|Hit React|Anim",
		meta=(AllowPrivateAccess="true"))
	TObjectPtr<UAnimMontage> HitReactRightMontage;

	// 普通受击的玩法硬直时间。动画会按这个时长计算播放速度。
	UPROPERTY(EditDefaultsOnly, BlueprintReadOnly,
		Category="Combat|Hit React",
		meta=(AllowPrivateAccess="true", ClampMin="0.05", UIMin="0.05"))
	float HitReactDuration = 0.55f;

	UPROPERTY(EditDefaultsOnly, BlueprintReadOnly,
		Category="Combat|Death|Anim",
		meta=(AllowPrivateAccess="true"))
	TObjectPtr<UAnimMontage> DeathMontage;
```

在 `private:` 函数区域新增：

```cpp
	// 原生输入的统一门控；Look 保留，不调用本函数。
	bool IsHitReactOrDead() const;
```

在底部 `public:` 区域新增访问器和死亡准备函数：

```cpp
	FORCEINLINE UAnimMontage* GetHitReactFrontMontage() const
	{
		return HitReactFrontMontage;
	}

	FORCEINLINE UAnimMontage* GetHitReactBackMontage() const
	{
		return HitReactBackMontage;
	}

	FORCEINLINE UAnimMontage* GetHitReactLeftMontage() const
	{
		return HitReactLeftMontage;
	}

	FORCEINLINE UAnimMontage* GetHitReactRightMontage() const
	{
		return HitReactRightMontage;
	}

	FORCEINLINE float GetHitReactDuration() const
	{
		return HitReactDuration;
	}

	FORCEINLINE UAnimMontage* GetDeathMontage() const
	{
		return DeathMontage;
	}

	// 由死亡 Ability 调用：清理角色层的跳跃、长按奔跑与目标锁定。
	void PrepareForDeath();
```

### 7.2 修改 `DKCharacter.cpp`

文件：

```text
Source/First/Private/Character/DKCharacter.cpp
```

文件末尾新增：

```cpp
bool ADKCharacter::IsHitReactOrDead() const
{
	return FirstAbilitySystemComponent &&
		(FirstAbilitySystemComponent->HasMatchingGameplayTag(
			MyGameplayTags::DK_Status_HitReact) ||
		 FirstAbilitySystemComponent->HasMatchingGameplayTag(
			MyGameplayTags::Shared_Status_Dead));
}

void ADKCharacter::PrepareForDeath()
{
	StopJumping();

	GetWorldTimerManager().ClearTimer(DodgeHoldRunTimerHandle);
	bDodgeInputHeld = false;
	SetRunning(false);

	if (TargetLockComponent)
	{
		TargetLockComponent->ClearTargetLock();
	}
}
```

### 7.3 给原生输入增加门控

GAS Ability 可以被 `DK.Ability.Action` 阻止，但以下输入直接由 `ADKCharacter` 处理，因此也要拦截。

#### `Input_Move`

把函数开头改为：

```cpp
void ADKCharacter::Input_Move(const FInputActionValue& Value)
{
	if (!Controller || IsHitReactOrDead())
	{
		return;
	}
```

#### `Input_JumpStarted`

在现有判断前新增：

```cpp
	if (IsHitReactOrDead())
	{
		return;
	}
```

#### `Input_TargetLock`、`Input_SwitchTarget`、`Input_ToggleSword`

三个函数开头都新增：

```cpp
	if (IsHitReactOrDead())
	{
		return;
	}
```

#### `Input_AbilityInputPressed`

必须在启动闪避长按计时器之前新增：

```cpp
	if (IsHitReactOrDead())
	{
		return;
	}
```

否则玩家在受击期间按住 Shift，虽然闪避 Ability 启动失败，角色层计时器仍可能在受击结束后把角色切到奔跑速度。

#### `Input_Look` 不增加门控

本册推荐：受击和死亡后仍允许玩家转动观察镜头，所以 `Input_Look` 保持现状。

#### `Input_AbilityInputReleased` 不要整体拦截

松开事件仍需清理闪避长按计时器与输入状态，因此不要在函数开头直接 return。

### 7.4 编译闸门 4

这一步增加了反射属性。若编辑器仍看不到新属性，关闭编辑器后做一次完整 Build，再重新打开；不要只依赖热重载。

---

## 8. 第五步：创建四方向受击 Ability

### 8.1 创建类时选择什么父类

在 UE 编辑器中：

1. `工具（Tools）→ 新建 C++ 类（New C++ Class）`；
2. 勾选 `显示所有类（Show All Classes）`；
3. 搜索并选择父类 **`FirstDKGameplayAbility`**；
4. 类名填写 **`FirstGA_DKHitReact`**，不要手动写 `U` 前缀；
5. 最终文件应放到：

```text
头文件：Source/First/Public/AbilitySystem/Abilities/DK/FirstGA_DKHitReact.h
源文件：Source/First/Private/AbilitySystem/Abilities/DK/FirstGA_DKHitReact.cpp
```

如果向导生成到了模块根目录，可以在 IDE 中移动到上述目录，并同步修正 include。

### 8.2 `FirstGA_DKHitReact.h`

```cpp
#pragma once

#include "CoreMinimal.h"
#include "GameFramework/CharacterMovementComponent.h"
#include "AbilitySystem/Abilities/FirstDKGameplayAbility.h"
#include "FirstGA_DKHitReact.generated.h"

class ADKCharacter;
class UAnimMontage;

/**
 * 主角普通掉血受击。
 * 由 Shared.Event.DamageReceived 触发；根据事件 Instigator 选择四方向 Montage。
 */
UCLASS()
class FIRST_API UFirstGA_DKHitReact : public UFirstDKGameplayAbility
{
	GENERATED_BODY()

public:
	UFirstGA_DKHitReact();

protected:
	virtual void ActivateAbility(
		const FGameplayAbilitySpecHandle Handle,
		const FGameplayAbilityActorInfo* ActorInfo,
		const FGameplayAbilityActivationInfo ActivationInfo,
		const FGameplayEventData* TriggerEventData) override;

	virtual void EndAbility(
		const FGameplayAbilitySpecHandle Handle,
		const FGameplayAbilityActorInfo* ActorInfo,
		const FGameplayAbilityActivationInfo ActivationInfo,
		bool bReplicateEndAbility,
		bool bWasCancelled) override;

private:
	UFUNCTION()
	void HandleHitReactFinished();

	void FinishHitReact(bool bWasCancelled);

	UAnimMontage* SelectHitReactMontage(
		const ADKCharacter* DK,
		const AActor* DamageInstigator) const;

	bool bSavedMovementMode = false;
	TEnumAsByte<EMovementMode> SavedMovementMode = MOVE_Walking;
	uint8 SavedCustomMovementMode = 0;
};
```

### 8.3 `FirstGA_DKHitReact.cpp`

```cpp
#include "AbilitySystem/Abilities/DK/FirstGA_DKHitReact.h"

#include "Abilities/GameplayAbilityTypes.h"
#include "Abilities/Tasks/AbilityTask_PlayMontageAndWait.h"
#include "Abilities/Tasks/AbilityTask_WaitDelay.h"
#include "AbilitySystem/FirstAbilitySystemComponent.h"
#include "Animation/AnimMontage.h"
#include "Character/DKCharacter.h"
#include "MyGameplayTags.h"

UFirstGA_DKHitReact::UFirstGA_DKHitReact()
{
	InstancingPolicy = EGameplayAbilityInstancingPolicy::InstancedPerActor;

	FGameplayTagContainer AssetTags;
	AssetTags.AddTag(MyGameplayTags::DK_Ability_HitReact);
	SetAssetTags(AssetTags);

	// 激活后自动取消当前玩家动作，并在整个受击期间阻止新动作。
	CancelAbilitiesWithTag.AddTag(MyGameplayTags::DK_Ability_Action);
	BlockAbilitiesWithTag.AddTag(MyGameplayTags::DK_Ability_Action);

	ActivationOwnedTags.AddTag(MyGameplayTags::DK_Status_HitReact);
	ActivationBlockedTags.AddTag(MyGameplayTags::DK_Status_HitReact);
	ActivationBlockedTags.AddTag(MyGameplayTags::Shared_Status_Dead);

	FAbilityTriggerData Trigger;
	Trigger.TriggerTag = MyGameplayTags::Shared_Event_DamageReceived;
	Trigger.TriggerSource = EGameplayAbilityTriggerSource::GameplayEvent;
	AbilityTriggers.Add(Trigger);
}

void UFirstGA_DKHitReact::ActivateAbility(
	const FGameplayAbilitySpecHandle Handle,
	const FGameplayAbilityActorInfo* ActorInfo,
	const FGameplayAbilityActivationInfo ActivationInfo,
	const FGameplayEventData* TriggerEventData)
{
	Super::ActivateAbility(
		Handle,
		ActorInfo,
		ActivationInfo,
		TriggerEventData);

	ADKCharacter* DK = GetDKCharacterFromActorInfo();
	UFirstAbilitySystemComponent* ASC =
		GetFirstAbilitySystemComponentFromActorInfo();

	if (!DK || !ASC)
	{
		FinishHitReact(true);
		return;
	}

	DK->StopJumping();

	if (UCharacterMovementComponent* Movement =
		DK->GetCharacterMovement())
	{
		SavedMovementMode = Movement->MovementMode;
		SavedCustomMovementMode = Movement->CustomMovementMode;
		bSavedMovementMode = true;

		Movement->StopMovementImmediately();
		Movement->DisableMovement();
	}

	const AActor* DamageInstigator = TriggerEventData
		? TriggerEventData->Instigator.Get()
		: nullptr;

	UAnimMontage* Montage =
		SelectHitReactMontage(DK, DamageInstigator);

	const float HitReactDuration =
		FMath::Max(DK->GetHitReactDuration(), 0.05f);

	if (Montage)
	{
		// 玩法硬直由 HitReactDuration 决定；四个动画长度不同时也保持一致。
		const float PlayRate = Montage->GetPlayLength() > 0.f
			? Montage->GetPlayLength() / HitReactDuration
			: 1.f;

		UAbilityTask_PlayMontageAndWait* MontageTask =
			UAbilityTask_PlayMontageAndWait::
			CreatePlayMontageAndWaitProxy(
				this,
				TEXT("DKHitReactMontage"),
				Montage,
				PlayRate);

		MontageTask->ReadyForActivation();
	}

	// WaitDelay 是玩法权威时长：有没有动画、动画原始长度多少，
	// 都严格在 HitReactDuration 后结束硬直。
	UAbilityTask_WaitDelay* DelayTask =
		UAbilityTask_WaitDelay::WaitDelay(
			this,
			HitReactDuration);
	DelayTask->OnFinish.AddDynamic(
		this,
		&ThisClass::HandleHitReactFinished);
	DelayTask->ReadyForActivation();
}

UAnimMontage* UFirstGA_DKHitReact::SelectHitReactMontage(
	const ADKCharacter* DK,
	const AActor* DamageInstigator) const
{
	if (!DK)
	{
		return nullptr;
	}

	// 环境伤害、来源丢失或双方重合时，稳定回退到正面受击。
	if (!IsValid(DamageInstigator) || DamageInstigator == DK)
	{
		return DK->GetHitReactFrontMontage();
	}

	FVector ToAttacker =
		DamageInstigator->GetActorLocation() - DK->GetActorLocation();
	ToAttacker.Z = 0.f;

	if (ToAttacker.IsNearlyZero())
	{
		return DK->GetHitReactFrontMontage();
	}

	ToAttacker.Normalize();

	FVector Forward = DK->GetActorForwardVector();
	Forward.Z = 0.f;
	Forward.Normalize();

	FVector Right = DK->GetActorRightVector();
	Right.Z = 0.f;
	Right.Normalize();

	const float ForwardDot = FVector::DotProduct(Forward, ToAttacker);
	const float RightDot = FVector::DotProduct(Right, ToAttacker);

	UAnimMontage* SelectedMontage = nullptr;

	if (FMath::Abs(ForwardDot) >= FMath::Abs(RightDot))
	{
		SelectedMontage = ForwardDot >= 0.f
			? DK->GetHitReactFrontMontage()
			: DK->GetHitReactBackMontage();
	}
	else
	{
		SelectedMontage = RightDot >= 0.f
			? DK->GetHitReactRightMontage()
			: DK->GetHitReactLeftMontage();
	}

	// 某一个方向忘记配置时，不让能力失败，统一回退到 Front。
	return SelectedMontage
		? SelectedMontage
		: DK->GetHitReactFrontMontage();
}

void UFirstGA_DKHitReact::HandleHitReactFinished()
{
	FinishHitReact(false);
}

void UFirstGA_DKHitReact::FinishHitReact(bool bWasCancelled)
{
	if (!IsActive())
	{
		return;
	}

	EndAbility(
		CurrentSpecHandle,
		CurrentActorInfo,
		CurrentActivationInfo,
		true,
		bWasCancelled);
}

void UFirstGA_DKHitReact::EndAbility(
	const FGameplayAbilitySpecHandle Handle,
	const FGameplayAbilityActorInfo* ActorInfo,
	const FGameplayAbilityActivationInfo ActivationInfo,
	bool bReplicateEndAbility,
	bool bWasCancelled)
{
	ADKCharacter* DK = GetDKCharacterFromActorInfo();
	UFirstAbilitySystemComponent* ASC =
		GetFirstAbilitySystemComponentFromActorInfo();

	const bool bIsDead = ASC &&
		ASC->HasMatchingGameplayTag(
			MyGameplayTags::Shared_Status_Dead);

	// 被死亡能力打断时绝对不能恢复移动。
	if (DK && bSavedMovementMode && !bIsDead)
	{
		if (UCharacterMovementComponent* Movement =
			DK->GetCharacterMovement())
		{
			Movement->MaxWalkSpeed =
				DK->GetDesiredLocomotionSpeed();
			Movement->SetMovementMode(
				SavedMovementMode,
				SavedCustomMovementMode);
		}
	}

	bSavedMovementMode = false;

	Super::EndAbility(
		Handle,
		ActorInfo,
		ActivationInfo,
		bReplicateEndAbility,
		bWasCancelled);
}
```

### 8.4 当前连续受击策略

`DK.Status.HitReact` 会阻止受击 Ability 在 0.55 秒内重复启动：

- 后续命中仍然正常扣血；
- 不会每一帧重启受击动画；
- 可以避免主角被极高频重叠事件永久锁死。

如果以后希望 BOSS 三连的每一段都重新播放受击，需要让已激活的受击 Ability 内部继续监听
`Shared.Event.DamageReceived` 并重启 Montage。首版不建议立刻增加这层复杂度。

### 8.5 编译闸门 5

此时类应该可以编译，但还没有被授予，也没有配置 Montage，因此 PIE 中暂时看不到动画是正常的。

---

## 9. 第六步：创建死亡 Ability

### 9.1 创建类时选择什么父类

1. `工具 → 新建 C++ 类 → 显示所有类`；
2. 父类选择 **`FirstDKGameplayAbility`**；
3. 类名填写 **`FirstGA_DKDeath`**；
4. 文件放到：

```text
头文件：Source/First/Public/AbilitySystem/Abilities/DK/FirstGA_DKDeath.h
源文件：Source/First/Private/AbilitySystem/Abilities/DK/FirstGA_DKDeath.cpp
```

### 9.2 `FirstGA_DKDeath.h`

```cpp
#pragma once

#include "CoreMinimal.h"
#include "AbilitySystem/Abilities/FirstDKGameplayAbility.h"
#include "FirstGA_DKDeath.generated.h"

/**
 * 主角死亡能力。
 * Shared.Status.Dead 被添加时自动激活；当前版本是终局状态，不在本 Ability 内复活。
 */
UCLASS()
class FIRST_API UFirstGA_DKDeath : public UFirstDKGameplayAbility
{
	GENERATED_BODY()

public:
	UFirstGA_DKDeath();

protected:
	virtual void ActivateAbility(
		const FGameplayAbilitySpecHandle Handle,
		const FGameplayAbilityActorInfo* ActorInfo,
		const FGameplayAbilityActivationInfo ActivationInfo,
		const FGameplayEventData* TriggerEventData) override;
};
```

### 9.3 `FirstGA_DKDeath.cpp`

```cpp
#include "AbilitySystem/Abilities/DK/FirstGA_DKDeath.h"

#include "Abilities/GameplayAbilityTypes.h"
#include "Abilities/Tasks/AbilityTask_PlayMontageAndWait.h"
#include "AbilitySystem/FirstAbilitySystemComponent.h"
#include "Character/DKCharacter.h"
#include "Components/CapsuleComponent.h"
#include "GameFramework/CharacterMovementComponent.h"
#include "MyGameplayTags.h"

UFirstGA_DKDeath::UFirstGA_DKDeath()
{
	InstancingPolicy = EGameplayAbilityInstancingPolicy::InstancedPerActor;

	FGameplayTagContainer AssetTags;
	AssetTags.AddTag(MyGameplayTags::DK_Ability_Death);
	SetAssetTags(AssetTags);

	// 死亡是终局状态；不允许新的玩家动作启动。
	BlockAbilitiesWithTag.AddTag(MyGameplayTags::DK_Ability_Action);

	// 不要把 Shared.Status.Dead 放进 ActivationBlockedTags，
	// 否则正是这个标签触发死亡时，死亡 Ability 会把自己挡住。
	FAbilityTriggerData Trigger;
	Trigger.TriggerTag = MyGameplayTags::Shared_Status_Dead;
	Trigger.TriggerSource = EGameplayAbilityTriggerSource::OwnedTagAdded;
	AbilityTriggers.Add(Trigger);
}

void UFirstGA_DKDeath::ActivateAbility(
	const FGameplayAbilitySpecHandle Handle,
	const FGameplayAbilityActorInfo* ActorInfo,
	const FGameplayAbilityActivationInfo ActivationInfo,
	const FGameplayEventData* TriggerEventData)
{
	Super::ActivateAbility(
		Handle,
		ActorInfo,
		ActivationInfo,
		TriggerEventData);

	ADKCharacter* DK = GetDKCharacterFromActorInfo();
	UFirstAbilitySystemComponent* ASC =
		GetFirstAbilitySystemComponentFromActorInfo();

	if (!DK || !ASC)
	{
		EndAbility(
			Handle,
			ActorInfo,
			ActivationInfo,
			true,
			true);
		return;
	}

	// 死亡可以全面取消；普通受击只能按 DK.Ability.Action 精确取消。
	ASC->CancelAllAbilities(this);

	// 清除角色层输入状态和目标锁定。
	DK->PrepareForDeath();

	if (UCharacterMovementComponent* Movement =
		DK->GetCharacterMovement())
	{
		Movement->StopMovementImmediately();
		Movement->DisableMovement();
	}

	// 保留胶囊对地面的碰撞，只忽略 Pawn，防止 BOSS 推动尸体。
	if (UCapsuleComponent* Capsule = DK->GetCapsuleComponent())
	{
		Capsule->SetCollisionResponseToChannel(
			ECC_Pawn,
			ECR_Ignore);
	}

	if (UAnimMontage* Montage = DK->GetDeathMontage())
	{
		UAbilityTask_PlayMontageAndWait* MontageTask =
			UAbilityTask_PlayMontageAndWait::
			CreatePlayMontageAndWaitProxy(
				this,
				TEXT("DKDeathMontage"),
				Montage);

		// 死亡是终局状态：不绑定 Completed 去 EndAbility。
		// Ability 保持激活，持续阻止 DK.Ability.Action。
		MontageTask->ReadyForActivation();
	}
}
```

### 9.4 为什么死亡 Ability 不正常结束

当前阶段没有原地复活流程。让死亡 Ability 保持激活有三个好处：

- `BlockAbilitiesWithTag` 持续有效；
- 不会误执行任何移动恢复代码；
- 逻辑上明确表示角色仍处于终局死亡状态。

以后实现检查点复活时，应单独设计：重建 Pawn，或者完整移除 Dead Tag、恢复碰撞/移动、结束死亡能力并重置属性。不要在本册死亡 Montage 的 Completed 回调里自动复活。

### 9.5 编译闸门 6

确认：

- 使用 UE 5.6 的 `FAbilityTriggerData`，不是旧教程里的 `FGameplayAbilityTrigger`；
- Death 构造函数没有把 `Shared.Status.Dead` 加入 `ActivationBlockedTags`。

---

## 10. 第七步：主角死亡后让 BOSS 放弃目标

仅仅停止主角还不够。当前 BOSS 的 `UpdateCombatDistance` 只判断有没有 `TargetActor`，没有判断目标是否死亡。

文件：

```text
Source/First/Private/AI/UBTService_UpdateCombatDistance.cpp
```

新增 include：

```cpp
#include "AbilitySystemBlueprintLibrary.h"
#include "AbilitySystemComponent.h"
#include "AbilitySystem/FirstAbilitySystemComponent.h"
#include "Character/BaseCharacter.h"
#include "MyGameplayTags.h"
```

在以下代码之后：

```cpp
const APawn* BossPawn = AIController->GetPawn();
AActor* Target = Cast<AActor>(
	BB->GetValueAsObject(TEXT("TargetActor")));
```

保留原有 `!BossPawn || !Target` 分支，然后在它后面新增：

```cpp
	UAbilitySystemComponent* TargetASC =
		UAbilitySystemBlueprintLibrary::
		GetAbilitySystemComponent(Target);

	const bool bTargetDead = TargetASC &&
		TargetASC->HasMatchingGameplayTag(
			MyGameplayTags::Shared_Status_Dead);

	if (bTargetDead)
	{
		// 取消正在播放的 BOSS 攻击；不取消死亡、受击等其它能力。
		if (const ABaseCharacter* BossCharacter =
			Cast<ABaseCharacter>(BossPawn))
		{
			if (UFirstAbilitySystemComponent* BossASC =
				BossCharacter->GetFirstAbilitySystemComponent())
			{
				FGameplayTagContainer AttackTags;
				AttackTags.AddTag(
					MyGameplayTags::Boss_Ability_Attack);
				BossASC->CancelAbilities(&AttackTags);
			}
		}

		AIController->StopMovement();
		AIController->ClearFocus(EAIFocusPriority::Gameplay);

		BB->SetValueAsObject(TEXT("TargetActor"), nullptr);
		BB->SetValueAsBool(TEXT("bPlayerDetected"), false);
		BB->SetValueAsBool(TEXT("bInAlertRange"), false);
		BB->SetValueAsBool(TEXT("bInAttackRange"), false);
		BB->SetValueAsBool(TEXT("bPlayerTooClose"), false);
		BB->SetValueAsBool(TEXT("bInStrafeRange"), false);
		BB->SetValueAsBool(TEXT("bPlayerTooFar"), false);
		BB->SetValueAsBool(TEXT("bShouldChase"), false);
		BB->SetValueAsBool(TEXT("bStrafeTimeout"), false);
		BB->SetValueAsBool(TEXT("bHasAttacked"), false);
		return;
	}
```

这一步放在服务中而不是等待感知回调，原因是：玩家原地死亡时仍在 BOSS 视野内，AI Perception 不一定产生“失去目标”事件，但行为树服务会持续检查 Dead Tag。

### 10.1 感知入口也拒绝死亡目标

服务负责清理“已经锁定后才死亡”的主角；感知入口负责避免死亡目标被新的视觉刺激重新写回黑板。两层职责不同，建议都保留。

文件：

```text
Source/First/Private/Controller/BossAIController.cpp
```

新增 include：

```cpp
#include "AbilitySystemComponent.h"
```

当前文件已经包含 `AbilitySystemBlueprintLibrary.h` 和 `MyGameplayTags.h`，不要重复添加。

在 `HandleTargetPerceptionUpdated()` 中，完成 Candidate 判空之后：

```cpp
ABaseCharacter* Candidate = Cast<ABaseCharacter>(Actor);
if (!Candidate || Candidate == GetPawn())
{
	return;
}
```

紧接着新增：

```cpp
	UAbilitySystemComponent* CandidateASC =
		UAbilitySystemBlueprintLibrary::
		GetAbilitySystemComponent(Candidate);

	const bool bCandidateDead = CandidateASC &&
		CandidateASC->HasMatchingGameplayTag(
			MyGameplayTags::Shared_Status_Dead);

	if (bCandidateDead)
	{
		// 若死亡者恰好还是当前目标，立即清干净；
		// 若不是当前目标，则只拒绝本次写入。
		AActor* CurrentTarget = Cast<AActor>(
			BB->GetValueAsObject(TEXT("TargetActor")));

		if (CurrentTarget == Candidate)
		{
			StopMovement();
			ClearFocus(EAIFocusPriority::Gameplay);

			BB->SetValueAsObject(TEXT("TargetActor"), nullptr);
			BB->SetValueAsBool(TEXT("bPlayerDetected"), false);
			BB->SetValueAsBool(TEXT("bInAlertRange"), false);
			BB->SetValueAsBool(TEXT("bInAttackRange"), false);
			BB->SetValueAsBool(TEXT("bPlayerTooClose"), false);
			BB->SetValueAsBool(TEXT("bInStrafeRange"), false);
			BB->SetValueAsBool(TEXT("bPlayerTooFar"), false);
			BB->SetValueAsBool(TEXT("bShouldChase"), false);
			BB->SetValueAsBool(TEXT("bStrafeTimeout"), false);
			BB->SetValueAsBool(TEXT("bHasAttacked"), false);
		}

		return;
	}
```

这段必须位于：

```cpp
if (Stimulus.WasSuccessfullySensed())
```

之前。否则成功感知分支会先把尸体重新写入 `TargetActor`。

### 10.2 编译闸门 7

确认：

- `BT_Boss` 的主战斗 Selector 上仍挂着 `Update Combat Distance` 服务，否则服务层检查不会执行；
- `BossAIController.cpp` 的死亡 Candidate 判断位于成功感知分支之前；
- 角色死亡后，输出日志中不再出现对该角色的新锁定。

---

## 11. 第八步：创建四方向受击与死亡 Montage

### 11.1 先复制动画，不修改第三方原始资产

建议建立：

```text
/Game/DKCharacter/AnimBP/Sword/Source/HitReact/
/Game/DKCharacter/AnimBP/Sword/Source/Death/
/Game/DKCharacter/AnimBP/Sword/Montages/HitReact/
/Game/DKCharacter/AnimBP/Sword/Montages/Death/
```

把以下动画复制到主角自己的 `Source` 目录：

```text
Hit_01_F_Anim
Hit_01_B_Anim
Hit_01_L_Anim
Hit_01_R_Anim
Dead_Anim
```

不要移动或直接修改 `SwordAnimsetPro` 原资产，避免以后其它示例角色受到影响。

### 11.2 先预览 F/B/L/R 的命名含义

不同动画包的方向命名可能表示两种含义：

```text
约定 A：F 表示攻击来自 Front
约定 B：F 表示身体被推向 Forward
```

本册 C++ 属性已经明确表示“攻击来自哪一侧”，因此只需要在蓝图中填对 Montage。

建议把复制品重命名为语义明确的名字，例如：

```text
AS_DK_HitReact_FromFront
AS_DK_HitReact_FromBack
AS_DK_HitReact_FromLeft
AS_DK_HitReact_FromRight
AS_DK_Death
```

如果预览后发现动画名表示身体移动方向，通常应使用相反方向：

| 攻击来源 | 常见的身体反应方向候选 |
|---|---|
| Front | B |
| Back | F |
| Left | R |
| Right | L |

这只是常见规律，不要跳过实际预览。

### 11.3 处理 Root Motion，避免受击滑步

候选动画位于 `Root-Motion` 目录。普通受击首版建议尽量保持胶囊原地：

1. 只修改复制品；
2. 打开复制的受击动画；
3. 在资产详情的 Root Motion 设置中先尝试：
   - `Enable Root Motion（启用根运动）` 关闭；
   - `Force Root Lock（强制根锁定）` 开启；
4. 创建 Montage 后 PIE 实测；
5. 如果确实希望有很小的受击位移，再单独启用和调整，不要四个方向一次全开。

死亡动画也建议先让胶囊原地，避免关闭移动后 Mesh 与胶囊分离。确认死亡姿势稳定后再决定是否保留死亡根运动。

### 11.4 创建五个 Montage

分别从四个受击复制品创建：

```text
AM_DK_HitReact_FromFront
AM_DK_HitReact_FromBack
AM_DK_HitReact_FromLeft
AM_DK_HitReact_FromRight
```

从 `AS_DK_Death` 创建：

```text
AM_DK_Death
```

五个 Montage 都设置：

```text
Slot = DefaultSlot
```

不要使用 `UpperBody`。普通受击和死亡都应该覆盖全身。

### 11.5 受击 Montage 设置

四个受击 Montage：

- 不循环；
- `Enable Auto Blend Out（启用自动混出）` 保持开启；
- 不需要 `ANS_HitReactWindow`；
- 不需要 `ANS_InvincibleWindow`；
- 不需要武器碰撞 Notify。

受击 Ability 会按 `HitReactDuration` 自动计算播放速度。

### 11.6 死亡 Montage 防止重新站起

死亡 Montage 不能播放完后交还给 Locomotion，否则主角会重新回到站立 Idle。

首版推荐：

```text
关闭 AM_DK_Death 的 Enable Auto Blend Out（启用自动混出）
```

如果动画尾帧仍不稳定，可以在完全倒地处创建 `Lying` Section，并把：

```text
Fall  的 Next Section = Lying
Lying 的 Next Section = Lying
```

不要让整个死亡过程从头循环，否则角色会不断重复倒下。

---

## 12. 第九步：配置 `BP_DKCharacter`

打开：

```text
/Game/DKCharacter/BP_DKCharacter
```

进入 `类默认值（Class Defaults）`，找到：

```text
Combat | Hit React | Anim
Combat | Hit React
Combat | Death | Anim
```

填写：

| 属性 | 值 |
|---|---|
| Hit React Front Montage | `AM_DK_HitReact_FromFront` |
| Hit React Back Montage | `AM_DK_HitReact_FromBack` |
| Hit React Left Montage | `AM_DK_HitReact_FromLeft` |
| Hit React Right Montage | `AM_DK_HitReact_FromRight` |
| Hit React Duration | `0.55` |
| Death Montage | `AM_DK_Death` |

编译并保存。

### 12.1 确认 AnimBP 插槽

当前 `/Game/DKCharacter/AnimBP/Sword/ABP_DK_Sword` 已有 `DefaultSlot`，通常无需重新添加。

如果 PIE 中日志和状态都正确但完全看不到 Montage，再打开 AnimGraph 确认最终输出链仍经过全身 `DefaultSlot`。

---

## 13. 第十步：授予两个 Reactive Ability

打开真实资产：

```text
/Game/DKCharacter/Data/DA_DKStartUpData
```

找到：

```text
Reactive Abilities
```

在现有数组末尾**追加**：

```text
FirstGA_DKHitReact
FirstGA_DKDeath
```

注意：

- 不要清空原有 `FirstGA_DKGuardBreak` 等 Reactive Ability；
- 不要配置旧入口 `/Game/DKCharacter/GAS/Data/DA_DKStartUpData`；
- 不要把受击和死亡放进 `BP_DKWeapon_Sword`，主角没装备武器时也必须能受击和死亡；
- 下拉列表找不到原生类时，先关闭编辑器做完整 Build，再重新打开资产。

保存资产。

---

## 14. 推荐编译与实施顺序

```text
闸门 1：MyGameplayTags
  ↓
闸门 2：给现有玩家动作添加 DK.Ability.Action
  ↓
闸门 3：FirstAttributeSet 发送 DamageReceived
  ↓
闸门 4：DKCharacter 新增属性、访问器、输入门控
  ↓
闸门 5：FirstGA_DKHitReact
  ↓
闸门 6：FirstGA_DKDeath
  ↓
闸门 7：UpdateCombatDistance 检查死亡目标
  ↓
完整 Build
  ↓
创建 Montage → 配 BP_DKCharacter → 配 DA_DKStartUpData
```

涉及 `UCLASS`、`UPROPERTY`、原生 Gameplay Tag 和新文件时，推荐关闭 UE 编辑器做完整 Build。仅用 Live Coding 容易让反射面板暂时看不到新类或新属性。

---

## 15. 分阶段验收

不要一次测试所有内容。按下面顺序更容易定位问题。

### 阶段 A：事件与动作中断

暂时不填 Montage：

1. 让 BOSS 正常命中主角；
2. 主角生命下降；
3. 主角应停止约 0.55 秒；
4. 攻击、闪避、格挡、换剑应被中断；
5. 0.55 秒后重新恢复移动。

若阶段 A 不通过，先不要排查动画。

### 阶段 B：单方向动画

先只填写 `Hit React Front Montage`：

1. BOSS 在主角正面命中；
2. 应播放 Front；
3. BOSS 从其它方向命中时，因为对应属性为空，会回退到 Front。

确认后再填另外三个方向。

### 阶段 C：四方向

| BOSS 相对主角的位置 | 预期属性 |
|---|---|
| 正前方 | Front |
| 正后方 | Back |
| 左侧 | Left |
| 右侧 | Right |

如果前后或左右反了，只交换 `BP_DKCharacter` 对应 Montage，不要先改方向计算代码。

### 阶段 D：防御互斥

| 操作 | 预期 |
|---|---|
| 正面格挡成功 | 扣精力，播放格挡 Hit，不扣血，不播放普通受击 |
| 弹反成功 | BOSS 弹反僵直，不扣主角血，不播放普通受击 |
| 格挡时背后受击 | 正常扣血，取消格挡，播放 Back/Left/Right 中正确方向 |
| 闪避无敌窗被命中 | 不扣血，不播放普通受击 |
| 闪避非无敌帧被命中 | 扣血，取消闪避，播放普通受击 |
| 破防当击 | 仍由 GuardBreak 表现，不叠加普通掉血受击 |
| 破防后下一段命中 | 正常扣血并进入普通受击 |

### 阶段 E：死亡

1. 把主角打到只剩少量生命；
2. 用一次致命攻击命中；
3. 只应播放死亡，不先播放普通受击；
4. 主角停止移动、跳跃、攻击、闪避、格挡、换剑；
5. 锁定标记消失；
6. BOSS 停止当前攻击并清除目标；
7. 尸体不被 BOSS 胶囊推走；
8. 死亡动画结束后不会重新站起；
9. 镜头仍可自由观察。

---

## 16. 完整回归测试清单

| 编号 | 场景 | 预期 |
|---|---|---|
| T01 | BOSS 从正面普通命中 | Front 受击 |
| T02 | BOSS 从背后命中 | Back 受击 |
| T03 | BOSS 从主角左侧命中 | Left 受击 |
| T04 | BOSS 从主角右侧命中 | Right 受击 |
| T05 | 正面成功格挡 | 只有格挡表现，无普通受击 |
| T06 | 成功弹反 | 只有弹反表现，无普通受击 |
| T07 | 闪避无敌窗命中 | 无伤害、无受击 |
| T08 | 闪避起手/收尾被打 | 闪避被取消，进入受击 |
| T09 | 轻攻击中被打 | 攻击 Montage/武器碰撞正确结束，进入受击 |
| T10 | 格挡中被背刺 | 格挡标签被清理，进入方向受击 |
| T11 | 装备/卸下中被打 | 换剑被取消，进入受击 |
| T12 | 锁定中受击 | 锁定保持，受击结束后可继续操作 |
| T13 | 锁定中死亡 | 立即解除锁定 |
| T14 | 非致命连续命中 | 血正常下降，0.55 秒内不疯狂重启 Montage |
| T15 | 致命攻击 | 只进入死亡，不进入普通受击 |
| T16 | 攻击中死亡 | 所有玩家 Ability 被取消，武器碰撞关闭 |
| T17 | 主角死亡后 | BOSS 清目标、停移动、停当前攻击 |
| T18 | 尸体状态 | 不站起、不移动、不被 BOSS 推走 |
| T19 | 重启 PIE | 所有状态恢复初始值，无 Loose Tag 残留 |

---

## 17. 常见问题与排查

| 现象 | 最可能原因 | 处理 |
|---|---|---|
| 掉血但完全没有受击 | HitReact 未加入真实 `DA_DKStartUpData`，或事件标签拼写不一致 | 检查第 6、13 节 |
| 有 0.55 秒停顿但没动画 | Montage 没填、骨架不匹配，或 AnimGraph 未经过 `DefaultSlot` | 检查第 11、12 节 |
| 永远只播 Front | 其它三个 Montage 没填，或事件里的 Instigator 为空 | 先检查 BP 属性；未来伤害必须正确写 EffectContext Instigator |
| 前后完全相反 | 动画包的 F/B 表示身体运动方向 | 在 BP 中交换 Front/Back Montage |
| 左右完全相反 | 动画包的 L/R 命名约定与攻击来源相反 | 在 BP 中交换 Left/Right Montage |
| 受击时明显滑步 | Root Motion 仍在推动角色或 Root Bone 未锁定 | 只修改动画复制品，检查 `Enable Root Motion` 与 `Force Root Lock` |
| 受击后不能恢复移动 | HitReact 没有正常 End，或受击 Montage 被设成循环 | 受击 Montage 不能循环；检查回调和 EndAbility |
| 死亡时又恢复移动 | HitReact 的 EndAbility 没检查 Dead | 必须保留 `!bIsDead` 才恢复 MovementMode |
| 受击时仍能攻击/闪避 | 某个 Ability 漏加 `DK.Ability.Action` | 对照第 5 节逐个检查 AssetTags |
| 受击时按 Shift，结束后突然奔跑 | `Input_AbilityInputPressed` 没有提前检查 HitReact | 按第 7.3 节在启动计时器前 return |
| 致命伤先播受击再死亡 | 致命分支仍发送 DamageReceived | 严格使用 `if Dead / else DamageReceived` |
| 死亡 Ability 不触发 | 没授予，或 Death 自己被 `Shared.Status.Dead` 阻止 | 检查 Reactive Abilities；Death 不得把 Dead 放进 BlockedTags |
| 死亡后重新站起 | Death Montage 自动 Blend Out 或最后 Section 没有保持 | 关闭 Auto Blend Out，或让 `Lying → Lying` |
| 主角死后 BOSS 仍追击 | 行为树服务没检查目标 Dead，或服务没挂在活跃父节点上 | 检查第 10 节和 BT_Boss 服务位置 |
| 主角死后 BOSS 不追但当前连击继续 | 只清了黑板，没有取消 `Boss.Ability.Attack` | 保留第 10 节的定向 `CancelAbilities` |
| 编译提示 `FGameplayAbilityTrigger` 不存在 | 使用了 UE 5.5 或更早教程的旧名称 | UE 5.6 使用 `FAbilityTriggerData` |
| 编辑器下拉找不到新类 | UHT/编辑器反射尚未刷新 | 关闭编辑器，完整 Build，再打开 |

---

## 18. 本册刻意不做的内容

为了先得到稳定闭环，本册暂不加入：

- 轻受击、重受击、击飞、倒地的伤害等级；
- 受击期间后半段允许闪避取消；
- 每一段高速连击都强制重启受击动画；
- 受击音效、镜头震动、血液 Niagara；
- 死亡 UI、检查点复活、读档；
- 布娃娃尸体。

这些内容都可以建立在本册的 `Shared.Event.DamageReceived`、`DK.Status.HitReact` 和
`Shared.Status.Dead` 主链上，不需要推翻当前结构。

---

## 19. 最终结构检查

完成后应得到：

```text
真实生命伤害
  └─ Shared.Event.DamageReceived
       └─ FirstGA_DKHitReact
            ├─ Cancel DK.Ability.Action
            ├─ Block DK.Ability.Action
            ├─ 根据 Instigator 计算 Front/Back/Left/Right
            ├─ 播放对应全身 Montage
            └─ 未死亡时恢复移动

生命归零
  └─ Shared.Status.Dead
       └─ FirstGA_DKDeath
            ├─ CancelAllAbilities(this)
            ├─ 清长按奔跑/跳跃/锁定
            ├─ DisableMovement
            ├─ Capsule 忽略 Pawn
            ├─ 播放并保持死亡姿势
            └─ BOSS 服务清除死亡目标
```

这个结构把职责分开：

- `DKDefenseComponent`：决定是否防住；
- `FirstAttributeSet`：决定是否真实掉血或死亡；
- `FirstGA_DKHitReact`：负责非致命受击表现与硬直；
- `FirstGA_DKDeath`：负责终局死亡；
- `DKCharacter`：保存可调动画和角色层输入状态；
- BOSS 行为树服务：决定是否继续把目标当作战斗目标。

因此四方向动画、死亡表现、输入和 AI 不会被塞进同一个类里，后续扩展仍然保持清晰。
