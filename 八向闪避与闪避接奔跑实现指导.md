# 八向闪避与闪避接奔跑实现指导（最终版）

> 适用项目：`First`（UE 5.6 / C++ / GAS / Enhanced Input）  
> 本文只提供实现指导。源码修改必须经你明确指示后才能进行。  
> 本文为最终版：8 向闪避（动画自带根运动位移）、长按闪避后直接进入奔跑、无 DodgeToRun 衔接动画、闪避动画已裁剪站立收尾。

---

## 0. 版本说明

本方案经历过的调整，最终状态如下：

```text
1. DodgeToRun 衔接动画：已取消（代码与本文档均已移除）
2. 闪避位移优化（临时修改 MaxWalkSpeed / 刹停）：已弃用
3. 闪避位移来源：动画自带根运动（bDodgeUsesRootMotion = true）
4. 长按奔跑：由角色层计时器实现，不再播放衔接动画
5. 闪避动画末尾站立收尾：已裁剪，长按衔接更顺滑
```

---

## 1. 设计目标与最终行为

```text
1. 按键方向量化为 8 向（前/前右/右/后右/后/后左/左/前左）；
2. 播放对应方向的闪避动画，位移由动画根运动提供；
3. 点按闪避键：只闪避一次，结束后步行；
4. 长按闪避键：闪避动画结束后，由角色层计时器进入奔跑；
5. 闪避动画已裁剪站立收尾，闪避结束能直接衔接奔跑。
```

---

## 2. 资产核对（已读取确认）

你项目里有完整的 8 向闪避动画：

`Content\Katana_Animations\Animations\Sequence\06_Dodge\01_Dodge\`

| 方向 | 序列资产 |
|---|---|
| 前 | `AS_Dodge_F_0_Seq` |
| 前右 | `AS_Dodge_F_R_45_Seq` |
| 右 | `AS_Dodge_R_90_Seq` |
| 后右 | `AS_Dodge_B_R_45_Seq` |
| 后 | `AS_Dodge_B_180_Seq` |
| 后左 | `AS_Dodge_B_L_45_Seq` |
| 左 | `AS_Dodge_L_90_Seq` |
| 前左 | `AS_Dodge_F_L_45_Seq` |

> `03_Dodge_to_Run` 那套动画不再使用。

### 2.1 骨架差异

```text
Katana 动画骨架：UE4_Mannequin_Skeleton
主角骨架：SK_Mannequin_Skeleton
```

两者不同，必须先做 IK 重定向。

---

## 3. 方向规则

方向基准 = 镜头（控制器）相对方向：

```text
S + Shift → 相对镜头向后
A + Shift → 相对镜头向左
单独 Shift → 无输入，默认向前
```

方向索引固定顺序（顺时针）：

```text
0=前  1=前右  2=右  3=后右
4=后  5=后左  6=左  7=前左
```

这个顺序就是蒙太奇数组的填写顺序。

---

## 4. 第一步：资产准备

### 4.1 创建 IK 绑定（IK Rig）

1. 打开 `UE4_Mannequin_Skeleton` → 右键 → **创建 IK 绑定**，命名 `IK_UE4Mannequin`。
2. 打开 `SK_Mannequin_Skeleton` → 右键 → **创建 IK 绑定**，命名 `IK_SKMannequin`。
3. 检查两个 IK 绑定的骨骼链是否完整。

### 4.2 创建 IK 重定向器（IK Retargeter）

1. 右键内容浏览器 → **动画 → IK 重定向器**。
2. `Source = IK_UE4Mannequin`，`Target = IK_SKMannequin`，命名 `IK_BossDodge`。

### 4.3 批量重定向

1. 选中 `01_Dodge` 下 8 个序列 → 右键 → **重定向动画资产** → 选 `IK_BossDodge`。
2. 输出到新建目录：

```text
/Game/DKCharacter/AnimBP/Sword/Source/Dodge8Way
```

---

## 5. 第二步：创建 8 个闪避蒙太奇

只为 8 个重定向后的序列创建蒙太奇（**不再创建 DodgeToRun 蒙太奇**）：

| 索引 | 蒙太奇 |
|---|---|
| 0 | `AM_DK_Sword_Dodge_F` |
| 1 | `AM_DK_Sword_Dodge_FR` |
| 2 | `AM_DK_Sword_Dodge_R` |
| 3 | `AM_DK_Sword_Dodge_BR` |
| 4 | `AM_DK_Sword_Dodge_B` |
| 5 | `AM_DK_Sword_Dodge_BL` |
| 6 | `AM_DK_Sword_Dodge_L` |
| 7 | `AM_DK_Sword_Dodge_FL` |

---

## 6. 第三步：裁剪闪避动画的站立收尾（推荐）

闪避动画末尾有一段“恢复站立”的收尾动作。长按闪避时，这段会让闪避结束和奔跑衔接出现停顿，建议裁掉。

操作（对 8 个重定向后的序列逐个执行）：

1. 打开动画序列（`Dodge8Way` 里的 `AS_Dodge_*`）；
2. 播放预览，找到“开始恢复站立”的那一帧；
3. 把播放头停在这一帧；
4. **右键底部时间轴（Scrub 条）** → 选择“删除从第 X 帧到第 Y 帧”（保留当前帧之前、删除之后）的那一项；
5. 保存。

注意：

```text
站立收尾段没有位移，裁剪它不会缩短闪避距离；
裁剪后蒙太奇长度自动变短；
角色层长按计时器使用 GetDodgeMontage()->GetPlayLength()，
请确认 DodgeMontage 指向裁剪后的向前闪避蒙太奇，保证计时一致。
```

---

## 7. 第四步：ABaseCharacter 通用成员

### 7.1 头文件新增

文件：`Source/First/Public/Character/BaseCharacter.h`

私有成员（闪避数据）：

```cpp
	// 兜底向前闪避蒙太奇。
	UPROPERTY(EditDefaultsOnly, BlueprintReadOnly, Category="Combat|Dodge", meta=(AllowPrivateAccess="true"))
	TObjectPtr<UAnimMontage> DodgeMontage;

	// 8 个方向的闪避蒙太奇，顺序固定：
	// 0=前 1=前右 2=右 3=后右 4=后 5=后左 6=左 7=前左。
	UPROPERTY(EditDefaultsOnly, BlueprintReadOnly, Category="Combat|Dodge", meta=(AllowPrivateAccess="true"))
	TArray<TObjectPtr<UAnimMontage>> DodgeMontages;

	// 无根运动时的位移强度（当前根运动方案下不生效）。
	UPROPERTY(EditDefaultsOnly, BlueprintReadOnly, Category="Combat|Dodge", meta=(AllowPrivateAccess="true"))
	float DodgeImpulse = 650.f;

	// 没有蒙太奇时的占位持续时间。
	UPROPERTY(EditDefaultsOnly, BlueprintReadOnly, Category="Combat|Dodge", meta=(AllowPrivateAccess="true"))
	float DodgeDuration = 0.35f;

	// 是否由动画根运动提供位移（当前方案：true）。
	UPROPERTY(EditDefaultsOnly, BlueprintReadOnly, Category="Combat|Dodge", meta=(AllowPrivateAccess="true"))
	bool bDodgeUsesRootMotion = false;
```

公开接口：

```cpp
	// 方向来源：玩家=按键方向，BOSS=以后由 AI 决定。可覆写。
	virtual FVector GetDodgeInputDirection() const;

	// 方向基准 Yaw：玩家=镜头；BOSS 以后可覆写为目标方向。
	virtual float GetDodgeReferenceYaw() const;

	// 计算 8 向索引（0～7）。
	int32 GetDodgeDirectionIndex() const;

	// 纯函数：任意方向向量 + 基准Yaw → 8向索引。
	static int32 QuantizeDodgeDirection(const FVector& InDirection, float ReferenceYaw);

	// 按方向索引取闪避蒙太奇；越界或未配置时回退到 DodgeMontage。
	UAnimMontage* GetDodgeMontageForDirection(int32 DirectionIndex) const;

	FORCEINLINE UAnimMontage* GetDodgeMontage() const { return DodgeMontage; }
	FORCEINLINE float GetDodgeImpulse() const { return DodgeImpulse; }
	FORCEINLINE float GetDodgeDuration() const { return DodgeDuration; }
	FORCEINLINE bool DoesDodgeUseRootMotion() const { return bDodgeUsesRootMotion; }
```

### 7.2 实现

文件：`Source/First/Private/Character/BaseCharacter.cpp`

```cpp
// 默认方向来源：角色面朝方向。玩家和 BOSS 会各自覆写。
FVector ABaseCharacter::GetDodgeInputDirection() const
{
	FVector Direction = GetActorForwardVector();
	Direction.Z = 0.0f;
	Direction.Normalize();
	return Direction;
}

// 默认基准：控制器镜头 Yaw；没有控制器时用角色朝向。
float ABaseCharacter::GetDodgeReferenceYaw() const
{
	if (Controller)
	{
		return Controller->GetControlRotation().Yaw;
	}

	return GetActorRotation().Yaw;
}

int32 ABaseCharacter::GetDodgeDirectionIndex() const
{
	return QuantizeDodgeDirection(GetDodgeInputDirection(), GetDodgeReferenceYaw());
}

int32 ABaseCharacter::QuantizeDodgeDirection(const FVector& InDirection, float ReferenceYaw)
{
	FVector Direction = InDirection;
	Direction.Z = 0.0f;

	if (Direction.IsNearlyZero())
	{
		return 0; // 前
	}

	Direction.Normalize();

	const float DirectionYaw = FMath::RadiansToDegrees(FMath::Atan2(Direction.Y, Direction.X));
	const float RelativeYaw = FRotator::NormalizeAxis(DirectionYaw - ReferenceYaw);

	int32 Index = FMath::RoundToInt(RelativeYaw / 45.f);
	Index = (Index + 8) % 8;

	return Index;
}

UAnimMontage* ABaseCharacter::GetDodgeMontageForDirection(int32 DirectionIndex) const
{
	if (DodgeMontages.IsValidIndex(DirectionIndex) && DodgeMontages[DirectionIndex])
	{
		return DodgeMontages[DirectionIndex].Get();
	}

	return DodgeMontage;
}
```

---

## 8. 第五步：ADKCharacter 玩家专属

### 8.1 覆写方向来源

`DKCharacter.h`：

```cpp
	// 玩家：方向来源 = 按键方向。
	virtual FVector GetDodgeInputDirection() const override;
```

`DKCharacter.cpp`：

```cpp
FVector ADKCharacter::GetDodgeInputDirection() const
{
	FVector Direction = GetLastMovementInputVector();
	Direction.Z = 0.f;

	if (Direction.IsNearlyZero())
	{
		Direction = GetActorForwardVector();
		Direction.Z = 0.f;
	}

	Direction.Normalize();
	return Direction;
}
```

### 8.2 长按奔跑（玩家专属，已实现，不改动）

玩家侧保留：

```text
bDodgeInputHeld：闪避键是否仍按住
DodgeHoldRunTimerHandle + HandleDodgeHoldElapsed：闪避快结束时 SetRunning(true)
DodgeRunOverlap：提前进入奔跑的窗口
```

长按闪避的完整流程：

```text
按下 Shift → 闪避能力激活 → 播放方向闪避动画（根运动位移）
→ 角色计时器到点（长度 - DodgeRunOverlap）→ SetRunning(true)
→ 闪避动画结束 → 能力结束 → 直接进入奔跑
```

---

## 9. 第六步：闪避能力

### 9.1 GetBaseCharacterFromActorInfo

`FirstDKGameplayAbility.h/.cpp` 新增，让能力同时服务玩家和 BOSS：

```cpp
ABaseCharacter* UFirstDKGameplayAbility::GetBaseCharacterFromActorInfo()
{
	return Cast<ABaseCharacter>(CurrentActorInfo->AvatarActor);
}
```

### 9.2 ActivateAbility：按方向选动画

`FirstGA_DKDodge.cpp` 关键部分：

```cpp
	// 8 向：方向来源由角色提供，再量化索引。
	CurrentDodgeDirectionIndex = BaseCharacter->GetDodgeDirectionIndex();

	FVector DodgeDirection = BaseCharacter->GetDodgeInputDirection();

	UAnimMontage* DodgeMontage =
		BaseCharacter->GetDodgeMontageForDirection(CurrentDodgeDirectionIndex);
	const bool bAnimationProvidesMovement =
		DodgeMontage && BaseCharacter->DoesDodgeUseRootMotion();

	if (!bAnimationProvidesMovement)
	{
		BaseCharacter->LaunchCharacter(
			DodgeDirection * BaseCharacter->GetDodgeImpulse(),
			true,
			false);
	}
```

当前方案 `bDodgeUsesRootMotion = true`，所以不会执行 `LaunchCharacter`，位移完全由动画根运动提供。

### 9.3 HandleDodgeCompleted：直接结束

```cpp
// 作用：正常结束闪避；GAS 随 EndAbility 自动移除 DK.Status.Dodging。
void UFirstGA_DKDodge::HandleDodgeCompleted()
{
	// 闪避动画正常结束后直接结束能力。
	// 长按奔跑由角色层计时器负责（SetRunning），这里不再播放衔接动画。
	FinishDodge(false);
}
```

---

## 10. 编辑器配置

`BP_DKCharacter` → `Combat | Dodge`：

```text
DodgeMontages（数组，按 0～7 顺序填 8 个蒙太奇）
bDodgeUsesRootMotion = true
```

动画与蒙太奇：

```text
序列：启用根运动 = 勾选；强制根锁定 = 取消勾选
蒙太奇：Root Motion Mode = 从蒙太奇
DodgeImpulse 当前方案下不生效，可保留任意值
```

---

## 11. 函数与概念讲解

### 11.1 GetDodgeInputDirection（虚函数）

告诉能力“朝哪闪”。玩家返回按键方向，BOSS 以后返回 AI 方向。

### 11.2 GetDodgeReferenceYaw（虚函数）

定义“哪个方向算前”。默认镜头 Yaw；锁敌后覆写为目标方向 Yaw。

### 11.3 QuantizeDodgeDirection（静态纯函数）

方向向量 + 基准 Yaw → 0～7 索引。无角色依赖。

### 11.4 GetDodgeDirectionIndex

组合两个虚函数得到本次闪避方向索引。

### 11.5 GetDodgeMontageForDirection

索引 → 蒙太奇，带兜底。

### 11.6 CurrentDodgeDirectionIndex

记录本次闪避方向，供蒙太奇选择使用。

### 11.7 HandleDodgeCompleted

闪避动画结束后直接结束能力；奔跑由角色层计时器接管。

### 11.8 长按计时器（玩家侧）

`HandleDodgeHoldElapsed` 到点且仍按住时 `SetRunning(true)`；松开时 `SetRunning(false)`。

---

## 12. 编译与常见错误

| 报错 | 原因 | 修复 |
|---|---|---|
| `GetBaseCharacterFromActorInfo 未声明` | 辅助函数没加 | 确认 9.1 两步都完成 |
| `GetDodgeInputDirection 未覆写任何基类方法` | 父类没声明 virtual | 确认 ABaseCharacter 已加虚函数 |
| 播放动画方向不对 | 数组顺序填错 | 严格按 0～7 顺序核对 |
| 动画变形/穿模 | 骨架不一致 | 确认 IK 重定向完成 |
| 闪避不位移 | bDodgeUsesRootMotion=false 或根运动被忽略 | 检查第 10 节配置 |
| 闪避后停顿 | 站立收尾没裁 | 按第 6 节裁剪 |

---

## 13. 测试清单

| 测试 | 操作 | 预期 |
|---|---|---|
| 向前 | 单独 Shift | 前闪，根运动位移 |
| 向后 | S + Shift | 后闪 |
| 向左 | A + Shift | 左闪 |
| 向右 | D + Shift | 右闪 |
| 斜向 | W/A/S/D 组合 + Shift | FL/FR/BL/BR 对应动画 |
| 点按 | 快速按一下松开 | 只闪避，结束后步行 |
| 长按 | 按住不放 | 闪避结束（无站立停顿）→ 直接进入奔跑 |
| 攻击后摇闪避 | 取消窗口内按方向 + Shift | 攻击被打断，按方向闪避 |
| 体力不足 | 体力耗尽时 | 闪避失败，不位移 |

---

## 14. BOSS 复用说明

`ABossCharacter` 以后会自动获得：

```text
8 向蒙太奇数组
方向量化算法
蒙太奇选择
闪避能力支持
```

BOSS 侧只需要：

```text
1. 覆写 GetDodgeInputDirection()：返回 AI 决定的方向；
2. 覆写 GetDodgeReferenceYaw()：返回目标方向作为基准（锁敌语义）；
3. 在 BOSS 蓝图里填自己的 8 个蒙太奇；
4. 由行为树触发闪避能力。
```

---

## 15. 可选优化（后续）

```text
1. 锁敌：GetDodgeReferenceYaw 覆写为目标方向 Yaw；
2. 每方向距离：根运动下由动画决定，如需微调改动画；
3. 8 向翻滚替换：07_Roll 资产同结构。
```

---

## 16. 最终职责

```text
ABaseCharacter
  ├─ 闪避数据（8 向蒙太奇数组、参数）
  ├─ 方向量化与蒙太奇选择
  └─ 虚函数：方向来源 / 基准Yaw

ADKCharacter
  ├─ Shift 输入、按住状态、长按奔跑计时器
  └─ 覆写：方向来源 = 按键方向

FirstGA_DKDodge
  ├─ 只依赖 ABaseCharacter
  ├─ 按方向选动画（根运动位移）
  └─ 动画结束直接结束能力
```
