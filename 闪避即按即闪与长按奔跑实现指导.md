# 闪避“按下即闪、长按衔接奔跑”实现指导

> 适用项目：`First`（UE 5.6 / C++ / GAS / Enhanced Input）  
> 本文只提供实现指导，不会自动修改你的 C++、蓝图或输入资产。  
> 本文取代旧的“Shift 短按闪避 / 长按奔跑”方案。

---

## 0. 项目实际资产核对（2026-08-15 已读取验证）

本文涉及的所有资产均来自你项目的真实路径，已通过文件系统读取核对：

| 用途 | 内容浏览器路径 | 磁盘路径 |
|---|---|---|
| 输入配置 DataAsset | `/Game/DKCharacter/DA_InputConfig` | `D:\UE2026\First\Content\DKCharacter\DA_InputConfig.uasset` |
| 默认输入映射环境 | `/Game/DKCharacter/Input/IMC_Default` | `D:\UE2026\First\Content\DKCharacter\Input\IMC_Default.uasset` |
| 闪避输入动作 | `/Game/DKCharacter/Input/Action/IA_Dodge` | `D:\UE2026\First\Content\DKCharacter\Input\Action\IA_Dodge.uasset` |
| 奔跑输入动作 | `/Game/DKCharacter/Input/Action/IA_Run` | `D:\UE2026\First\Content\DKCharacter\Input\Action\IA_Run.uasset` |
| 角色蓝图 | `/Game/DKCharacter/BP_DKCharacter` | `D:\UE2026\First\Content\DKCharacter\BP_DKCharacter.uasset` |
| 闪避蒙太奇 | `/Game/DKCharacter/AnimBP/Sword/Montages/AM_DK_Sword_Dodge_F` | `D:\UE2026\First\Content\DKCharacter\AnimBP\Sword\Montages\AM_DK_Sword_Dodge_F.uasset` |
| 闪避动画 | `/Game/DKCharacter/AnimBP/Sword/Source/A_DK_Sword_Dodge_F` | `D:\UE2026\First\Content\DKCharacter\AnimBP\Sword\Source\A_DK_Sword_Dodge_F.uasset` |
| 攻击蒙太奇 1~4 | `/Game/DKCharacter/AnimBP/Sword/Montages/AM_DK_Sword_Attack_1` ～ `_4` | `D:\UE2026\First\Content\DKCharacter\AnimBP\Sword\Montages\` |

### 0.1 当前输入配置的实测状态

从 `IMC_Default.uasset` 二进制中读取到的按键映射资产：

```text
IA_Move        （对应 WASD）
IA_Look        （对应 Mouse2D）
IA_Jump        （对应 SpaceBar）
IA_Run         （对应 Shift）
IA_Attack      （对应 LeftMouseButton）
IA_ToggleSword （对应 C）
```

**IMC_Default 中目前没有 `IA_Dodge`**。这证实了旧的闪避完全由 Shift（`IA_Run`）手势在代码里手工触发，`IA_Dodge` 从未绑定到任何物理按键。

从 `DA_InputConfig.uasset` 二进制中读取到它包含：

```text
InputTag.Dodge
InputTag.Run
InputTag.LightAttack
InputTag.ToggleSword
...
IA_Dodge
IA_Run
IA_Attack
...
```

也就是说文件里已经存在 `InputTag.Dodge` 和 `IA_Dodge` 的引用。具体在 `NativeInputActions` 还是 `AbilityInputActions` 数组、`InputAction` 是否为空，需要你在编辑器中打开 `DA_InputConfig` 确认（二进制字符串无法可靠区分数组归属）。

> 以上均为二进制字符串证据，最终以编辑器里看到的为准。

---

## 1. 设计目标与最终行为

参考永劫无间等动作游戏的闪避手感，把闪避键设计成“一个键同时负责闪避和奔跑”：

```text
按下闪避键
→ 立即触发闪避（在“按下”边沿触发，不等待松开）
→ 闪避播放期间松开了
   → 只闪避一次，结束后恢复步行
→ 一直按住没松开
   → 闪避结束后自动进入奔跑
   → 松开后停止奔跑
```

默认状态仍然是步行；奔跑不再需要单独的 Shift 键，而是由“闪避键长按”兼任。

---

## 2. 实现原理

闪避本身不需要修改：`FirstGA_DKDodge` 仍然在输入按下时被 GAS 同步激活。

角色只增加两个小逻辑：

```text
1. 记录闪避键是否仍被按住（bDodgeInputHeld）。
2. 按下时启动一个“闪避时长”计时器；
   计时器到点后如果按键仍未松开，就调用 SetRunning(true) 进入奔跑。
```

时序：

```text
按下闪避键（Started）
   ├─ 转发给 ASC → 闪避 Ability 立即激活
   └─ 启动计时器（时长 = 闪避蒙太奇长度）

闪避蒙太奇播放中
   ├─ 松开 → 清除计时器、停止奔跑判定
   └─ 一直按住 → 计时器到点 → SetRunning(true) → 进入奔跑
```

---

## 3. 涉及文件与资源

| 类型 | 位置 | 操作 |
|---|---|---|
| 输入 DataAsset | `/Game/DKCharacter/DA_InputConfig` | 确认 Dodge 条目、删除 Run 条目 |
| 输入映射 | `/Game/DKCharacter/Input/IMC_Default` | 闪避键绑定、删除 IA_Run 映射 |
| C++ 头文件 | `Source/First/Public/Character/DKCharacter.h` | 删除旧声明、新增新成员 |
| C++ 源文件 | `Source/First/Private/Character/DKCharacter.cpp` | 删除旧函数、修改两个输入函数、新增一个回调 |

---

## 4. 第一步：编辑器输入配置

### 4.1 确认闪避输入条目

打开 `/Game/DKCharacter/DA_InputConfig`：

1. 找到 `AbilityInputActions` 数组。
2. 确认里面有 `InputTag.Dodge` 条目，并且它的 `InputAction` 指向 `/Game/DKCharacter/Input/Action/IA_Dodge`。
3. 如果数组里没有这个条目，就新增一行：
   - `InputTag`：`InputTag.Dodge`
   - `InputAction`：`IA_Dodge`

> 实测提示：`DA_InputConfig.uasset` 文件里已经存在 `InputTag.Dodge` 和 `IA_Dodge` 字符串，所以大概率只是条目放错了数组（例如放在 `NativeInputActions`）或 `InputAction` 没填。先在编辑器里确认这两点，比直接新增更稳妥。

### 4.2 绑定按键

打开 `/Game/DKCharacter/Input/IMC_Default`：

1. 把 `IA_Dodge` 绑定到你想要的键（建议 Shift，因为 Shift 不再承担奔跑）。
2. 删除 `IA_Run` 的 Shift 按键映射（可选，但建议删除，避免残留困惑）。

> 实测提示：`IMC_Default` 当前没有 `IA_Dodge` 的映射，所以这一步是必须的，否则新方案按下闪避键时 `Input_AbilityInputPressed` 根本不会被调用。

### 4.3 删除 Run 输入条目

回到 `/Game/DKCharacter/DA_InputConfig`：

1. 在 `NativeInputActions` 数组中找到 `InputTag.Run` 条目。
2. 删除它。

> 注意：`InputTag_Run` 这个 GameplayTag 本身可以保留在代码里不用，以后想加“切换步行”时还能用；真正要删的是它的输入绑定和角色处理函数。

---

## 5. 第二步：修改 DKCharacter.h

文件：`Source/First/Public/Character/DKCharacter.h`

### 5.1 删除：旧 Shift 手势的函数声明

找到下面四个声明并删除：

```cpp
// 作用：按下 Shift 时开始计算按住时长；达到阈值后才进入奔跑。
void Input_RunStarted(const FInputActionValue& Value);
// 作用：正常松开 Shift；长按分支停止奔跑，短按分支触发一次闪避。
void Input_RunCompleted(const FInputActionValue& Value);
// 作用：输入被窗口切换等原因取消时只清理奔跑，不把取消误判成闪避。
void Input_RunCanceled(const FInputActionValue& Value);
// 作用：Shift 持续按住达到阈值后进入奔跑状态。
void HandleRunHoldThresholdReached();
```

### 5.2 删除：旧 Shift 手势的成员变量

找到下面四个成员并删除：

```cpp
// 短于该时间松开 Shift 视为闪避；达到该时间仍按住才进入奔跑。
UPROPERTY(EditDefaultsOnly, BlueprintReadOnly, Category="Character Data|Movement",
	meta=(AllowPrivateAccess="true", ClampMin="0.05", UIMin="0.05", UIMax="0.5"))
float RunHoldThreshold = 0.1f;

// 记录 Shift 物理键是否仍按住，避免 Timer 到点后在已经松开时误启动奔跑。
bool bRunInputHeld = false;

// 区分本次 Shift 手势已经走长按分支，还是松开时应走短按闪避分支。
bool bRunActivatedByHold = false;

// 管理短按/长按分界计时；松开或取消输入时必须清理。
FTimerHandle RunHoldTimerHandle;
```

### 5.3 保留：不需要动的成员

下面这些成员保留，闪避长按奔跑会继续使用它们：

```cpp
float WalkSpeed = 250.f;
float RunSpeed = 500.f;
bool bIsRunning = false;
void SetRunning(bool bNewRunning);
TObjectPtr<UAnimMontage> DodgeMontage;
float DodgeDuration = 0.35f;
float GetDodgeDuration() const;
```

`#include "TimerManager.h"` 也要保留，因为新方案仍然使用计时器。

### 5.4 新增：闪避按住状态、计时器与回调声明

在原来 `RunHoldTimerHandle` 的位置附近新增：

```cpp
// 闪避键是否仍被按住。
// true 表示玩家在闪避播放期间没有松开，闪避结束后应进入奔跑。
bool bDodgeInputHeld = false;

// “闪避结束后检查是否仍按住”的计时器。
// 在按下时启动，在松开时清除。
FTimerHandle DodgeHoldRunTimerHandle;
```

在 `SetRunning` 声明附近新增：

```cpp
// 计时器到点：如果闪避键仍未松开，进入奔跑。
void HandleDodgeHoldElapsed();
```

---

## 6. 第三步：修改 DKCharacter.cpp

文件：`Source/First/Private/Character/DKCharacter.cpp`

### 6.1 删除：SetupPlayerInputComponent 中的 Run 绑定

找到 `SetupPlayerInputComponent()` 中的这三段并整体删除：

```cpp
FirstInput->BindNativeInputAction(
	InputConfigDataAsset,
	MyGameplayTags::InputTag_Run,
	ETriggerEvent::Started,
	this,
	&ThisClass::Input_RunStarted);

FirstInput->BindNativeInputAction(
	InputConfigDataAsset,
	MyGameplayTags::InputTag_Run,
	ETriggerEvent::Completed,
	this,
	&ThisClass::Input_RunCompleted);

FirstInput->BindNativeInputAction(
	InputConfigDataAsset,
	MyGameplayTags::InputTag_Run,
	ETriggerEvent::Canceled,
	this,
	&ThisClass::Input_RunCanceled);
```

删除后，`SetupPlayerInputComponent()` 中保留的绑定是：Move、Look、Jump、ToggleSword，以及最后一行 `BindAbilityInputActions`。

### 6.2 删除：四个旧 Shift 函数定义

找到并整体删除下面四个函数：

```cpp
// 作用：记录 Shift 按下并启动一次性 Timer；尚未到阈值时仍保持步行。
void ADKCharacter::Input_RunStarted(const FInputActionValue& Value)
{
	bRunInputHeld = true;
	bRunActivatedByHold = false;
	SetRunning(false);

	GetWorldTimerManager().ClearTimer(RunHoldTimerHandle);
	if (RunHoldThreshold <= KINDA_SMALL_NUMBER)
	{
		HandleRunHoldThresholdReached();
		return;
	}

	GetWorldTimerManager().SetTimer(
		RunHoldTimerHandle,
		this,
		&ThisClass::HandleRunHoldThresholdReached,
		RunHoldThreshold,
		false);
}

// 作用：正常松开 Shift，并在“停止奔跑”和“触发闪避”之间只选择一个分支。
void ADKCharacter::Input_RunCompleted(const FInputActionValue& Value)
{
	const bool bShouldDodge = bRunInputHeld && !bRunActivatedByHold;

	GetWorldTimerManager().ClearTimer(RunHoldTimerHandle);
	bRunInputHeld = false;
	bRunActivatedByHold = false;
	SetRunning(false);

	if (bShouldDodge)
	{
		TriggerAbilityInputTap(MyGameplayTags::InputTag_Dodge);
	}
}

// 作用：处理失去焦点等 Canceled 边沿，只恢复步行，不产生一次非玩家意图的闪避。
void ADKCharacter::Input_RunCanceled(const FInputActionValue& Value)
{
	GetWorldTimerManager().ClearTimer(RunHoldTimerHandle);
	bRunInputHeld = false;
	bRunActivatedByHold = false;
	SetRunning(false);
}

// 作用：Timer 到点时再次确认 Shift 仍按住，然后将移动速度切换到 RunSpeed。
void ADKCharacter::HandleRunHoldThresholdReached()
{
	if (!bRunInputHeld || bRunActivatedByHold)
	{
		return;
	}

	bRunActivatedByHold = true;
	SetRunning(true);
}
```

删除后，`TriggerAbilityInputTap()` 仍然保留，因为它还被 `Input_ToggleSword`（1 键装备/卸下）使用。

### 6.3 修改：Input_AbilityInputPressed

原代码：

```cpp
void ADKCharacter::Input_AbilityInputPressed(FGameplayTag InputTag)
{
	// 这三个 Tag 已改由 Shift 手势和 1 键切换函数产生。
	// 即使 DataAsset 暂时残留旧条目，也不允许 Alt/E/Q 绕过新输入规则直接激活 Ability。
	if (InputTag == MyGameplayTags::InputTag_Dodge ||
		InputTag == MyGameplayTags::InputTag_EquipSword ||
		InputTag == MyGameplayTags::InputTag_UnequipSword)
	{
		return;
	}

	if (FirstAbilitySystemComponent)
	{
		// 角色只转发 Tag。ASC 会找到匹配的已授予 Spec：未装备剑时没有攻击 Spec，
		// 因此左键不需要额外 if 判断就会自然失效。
		FirstAbilitySystemComponent->OnAbilityInputPressed(InputTag);
	}
}
```

改为：

```cpp
void ADKCharacter::Input_AbilityInputPressed(FGameplayTag InputTag)
{
	// 闪避改为“按下立即触发 + 长按衔接奔跑”，不再由 Shift 手势生成。
	if (InputTag == MyGameplayTags::InputTag_Dodge)
	{
		// 记录按键仍被按住，供闪避结束后的奔跑判定使用。
		bDodgeInputHeld = true;

		// 计时时长取闪避蒙太奇的实际长度；
		// 没有配置蒙太奇时退回占位时长 DodgeDuration。
		const float DodgeHoldDuration = DodgeMontage
			? DodgeMontage->GetPlayLength()
			: GetDodgeDuration();

		GetWorldTimerManager().SetTimer(
			DodgeHoldRunTimerHandle,
			this,
			&ThisClass::HandleDodgeHoldElapsed,
			DodgeHoldDuration,
			false);
	}

	// 装备/卸下仍由 1 键切换函数产生；
	// 即使 DataAsset 暂时残留旧条目，也不允许它们绕过新的切换规则。
	if (InputTag == MyGameplayTags::InputTag_EquipSword ||
		InputTag == MyGameplayTags::InputTag_UnequipSword)
	{
		return;
	}

	if (FirstAbilitySystemComponent)
	{
		// 角色只转发 Tag。ASC 会找到匹配的已授予 Spec。
		// 闪避在这里被按下边沿立即激活，不再等待松开。
		FirstAbilitySystemComponent->OnAbilityInputPressed(InputTag);
	}
}
```

说明：

- 闪避分支放在最前面，先记录按住状态、启动计时器，然后才转发给 ASC。
- `DodgeMontage->GetPlayLength()` 返回动画总时长；计时器到点的时间点，基本就是闪避结束的时间点。
- 删除 `InputTag_Dodge` 的拦截后，闪避键按下会立刻走 `OnAbilityInputPressed → TryActivateAbility`，这就是“按下即闪避”的关键。

### 6.4 修改：Input_AbilityInputReleased

原代码：

```cpp
void ADKCharacter::Input_AbilityInputReleased(FGameplayTag InputTag)
{
	if (InputTag == MyGameplayTags::InputTag_Dodge ||
		InputTag == MyGameplayTags::InputTag_EquipSword ||
		InputTag == MyGameplayTags::InputTag_UnequipSword)
	{
		return;
	}

	if (FirstAbilitySystemComponent)
	{
		// Released 对长按技能和 AbilityTask_WaitInputRelease 有用；连击最小版本
		// 主要监听 Press，但仍应完整转发两个边沿事件。
		FirstAbilitySystemComponent->OnAbilityInputReleased(InputTag);
	}
}
```

改为：

```cpp
void ADKCharacter::Input_AbilityInputReleased(FGameplayTag InputTag)
{
	// 松开闪避键：结束“长按奔跑”的判定并恢复步行。
	if (InputTag == MyGameplayTags::InputTag_Dodge)
	{
		bDodgeInputHeld = false;
		GetWorldTimerManager().ClearTimer(DodgeHoldRunTimerHandle);
		SetRunning(false);
	}

	if (InputTag == MyGameplayTags::InputTag_EquipSword ||
		InputTag == MyGameplayTags::InputTag_UnequipSword)
	{
		return;
	}

	if (FirstAbilitySystemComponent)
	{
		// Released 对长按技能和 AbilityTask_WaitInputRelease 有用；连击最小版本
		// 主要监听 Press，但仍应完整转发两个边沿事件。
		FirstAbilitySystemComponent->OnAbilityInputReleased(InputTag);
	}
}
```

说明：

- `BindAbilityInputActions` 会把 `Completed` 和 `Canceled` 两个边沿都绑定到这个函数，所以失去窗口焦点时也会正确清除按住状态，不会卡在奔跑状态。
- `SetRunning(false)` 在这里是无条件调用：没在奔跑时调用也只是把 false 再写一次，没有副作用。

### 6.5 新增：HandleDodgeHoldElapsed 函数定义

建议放在 `Input_AbilityInputReleased` 函数之后：

```cpp
// 作用：闪避计时结束后的“长按检查”。
// 玩家如果在整个闪避期间都没有松开闪避键，就进入奔跑。
void ADKCharacter::HandleDodgeHoldElapsed()
{
	if (bDodgeInputHeld)
	{
		SetRunning(true);
	}
}
```

说明：

- 这个函数只负责“闪避结束后是否奔跑”；
- 如果玩家早就松开了，`bDodgeInputHeld` 已经是 false，函数什么都不做；
- `SetRunning(true)` 会设置 `bIsRunning = true` 并把 `MaxWalkSpeed` 改成 `RunSpeed`，这正是你已有的奔跑切换逻辑。

---

## 7. 第四步：编译

关闭 PIE 后，在 Visual Studio 编译 `FirstEditor Win64 Development`。

常见编译错误检查：

| 报错 | 原因 | 修复 |
|---|---|---|
| `Input_RunStarted 未定义` | 头文件删了声明但 cpp 没删定义，或反过来 | 两处都删干净 |
| `RunHoldTimerHandle 未声明` | cpp 里还有旧代码引用 | 搜索并删除所有旧 Shift 代码 |
| `bRunInputHeld 未声明` | 同上 | 用 `rg -n "RunInputHeld\|RunActivatedByHold\|RunHold"` 检查残留 |
| 找不到 `DodgeMontage` | 该成员在 DKCharacter.h 中是私有成员 | 确认新代码写在 `ADKCharacter` 成员函数内 |

---

## 8. 测试清单

| 测试 | 操作 | 预期结果 |
|---|---|---|
| 单次闪避 | 按下闪避键后立刻松开 | 按下瞬间开始闪避；结束后恢复步行，不进入奔跑 |
| 长按闪避 | 按住闪避键不放 | 按下瞬间开始闪避；闪避结束后自动进入奔跑；松开后停止奔跑 |
| 移动中闪避 | 按住 W 的同时按闪避键 | 立即向前闪避（方向取最后移动输入） |
| 奔跑中连闪 | 长按进入奔跑后，再次按闪避键 | 立即再闪避一次；结束后因仍按住而继续奔跑 |
| 攻击后摇闪避 | 在攻击取消窗口内按闪避键 | 攻击被打断，立即闪避 |
| 体力不足 | 体力耗尽时长按闪避键 | 闪避激活失败，但长按仍会在计时结束后进入奔跑 |
| 失去焦点 | 长按奔跑时切走窗口再切回 | 不应卡在奔跑状态（Canceled 边沿清理） |

---

## 9. 边界情况说明

### 9.1 体力不足

闪避 Ability 的 `CommitAbility` 会先检查体力；不足时闪避不激活。

但按下时计时器仍然会启动，因此“按住”仍然会进入奔跑。这符合“按住 = 奔跑”的直觉，可以接受。

### 9.2 闪避被提前打断

计时器方案以“蒙太奇长度”估算闪避结束时间。如果以后加入受击、硬直等会打断闪避的系统，计时器到点时玩家可能已经不在闪避中，但按键仍按住时会进入奔跑。

这对“按住 = 奔跑”的目标通常没有负面影响；如果希望严格在“闪避真正结束”的瞬间衔接，见第 11 节的可选进阶方案。

---

## 10. 清理项（可选）

### 10.1 更新过时注释

`Input_AbilityInputPressed` 中原来的注释“这三个 Tag 已改由 Shift 手势和 1 键切换函数产生”已经过时，新代码里已经替换为闪避相关的说明。

### 10.2 旧 Tag 是否删除

`MyGameplayTags::InputTag_Run` 的声明和定义可以保留，因为它以后可能用于“切换步行”功能；真正要删除的是输入绑定和角色处理函数。

---

## 11. 可选进阶：用 Dodging 标签事件精确衔接奔跑

计时器方案的时长来自蒙太奇长度；如果想在“闪避 Ability 真正结束”的瞬间衔接奔跑，可以改用 GAS 标签事件。

### 11.1 在 PossessedBy 注册标签监听

在 `ADKCharacter::PossessedBy` 中，调用 `Super::PossessedBy` 之后新增：

```cpp
FirstAbilitySystemComponent->RegisterGameplayTagEvent(
	MyGameplayTags::DK_Status_Dodging,
	EGameplayTagEventType::AnyCountChange
).AddUObject(this, &ThisClass::HandleDodgingTagChanged);
```

### 11.2 头文件新增声明

```cpp
// 闪避状态标签变化时调用；NewCount == 0 表示闪避结束。
void HandleDodgingTagChanged(const FGameplayTag& Tag, int32 NewCount);
```

### 11.3 CPP 新增实现

```cpp
void ADKCharacter::HandleDodgingTagChanged(const FGameplayTag& Tag, int32 NewCount)
{
	// Dodging 标签消失 = 闪避 Ability 真正结束。
	// 此时如果玩家仍按住闪避键，就进入奔跑。
	if (NewCount == 0 && bDodgeInputHeld)
	{
		SetRunning(true);
	}
}
```

### 11.4 与计时器方案的差异

| 对比 | 计时器方案 | 标签事件方案 |
|---|---|---|
| 代码量 | 少 | 多一点 |
| 是否知道闪避真正结束 | 用蒙太奇长度估算 | 精确 |
| 闪避被提前打断时 | 计时器仍会到点 | 标签消失时才触发 |
| 体力不足、闪避没激活时 | 长按仍可奔跑 | 不会进入奔跑 |
| 学习难度 | 低 | 中等（需要理解 GAS 标签事件） |

学习阶段建议先用计时器方案；等以后加入打断、硬直系统后，再决定是否升级。

---

## 12. 最终职责

```text
输入层（DKCharacter）
    → 按下闪避键：立即转发给 ASC，并记录按住状态、启动长按计时。
    → 松开闪避键：清除按住状态，恢复步行。

Dodge Ability
    → 只负责闪避本身：体力、方向、蒙太奇，结束时机由 Ability 管理。

角色移动状态（SetRunning）
    → 只负责步行/奔跑的速度切换，不关心输入手势。
```
