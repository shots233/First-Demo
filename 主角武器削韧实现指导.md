# 主角武器削韧实现指导

> 本册是《主角格挡、弹反、破防与BOSS韧性处决实现指导》的**后续扩展**。请先完成并验收原指导中的弹反削韧、五秒回韧和可处决流程，再实施本册。不要把两份指导交叉做到一半。

---

## 0. 最终效果

本册完成后，主角当前装备的剑会拥有独立的武器削韧值：

```text
BP_DKWeapon_Sword
└─ Weapon Poise Damage = 15

每次真实、去重后的武器命中 BOSS
├─ 先结算原有生命伤害
├─ 再固定削减 15 点 Poise
├─ 不使用生命伤害的连击倍率
├─ Poise > 0 时不新增强制僵直
└─ Poise 首次降到 0 时只发送一次 Boss.Event.PoiseBroken
   └─ 进入现有 5 秒可处决状态
```

BOSS 最大韧性为 `100` 时，纯普通武器命中的节奏是：

```text
100 → 85 → 70 → 55 → 40 → 25 → 10 → 0
```

因此需要第 `7` 次有效命中破韧。最后一次虽然配置为 `15`，但由于 AttributeSet 会把 Poise 夹紧到 `0`，实际只扣除剩余的 `10`。

弹反仍然固定削韧 `50`，而且只有弹反在未破韧时播放 `1` 秒普通弹反僵直。普通武器和弹反共享同一条“扣韧、刷新计时、首次归零”入口，但不共享未破韧时的动作表现。

---

## 1. 本册采用的固定规则

### 1.1 普通武器削韧

| 项目 | 首版规则 |
|---|---|
| 每次有效命中 | 固定削韧 `15` |
| 当前四段轻攻击 | 四段都为 `15`，不乘生命伤害倍率 |
| 未破韧表现 | 只更新韧性条，不新增强制僵直 |
| 归零表现 | 发送 `Boss.Event.PoiseBroken`，进入现有 Executable |
| 回韧 | 从最后一次实际削韧开始重新计算 `5` 秒 |
| 适用目标 | 首版只对 `ABossCharacter` 生效 |

当前生命伤害计算使用：

```text
第 1 段：1.00 倍
第 2 段：1.15 倍
第 3 段：1.30 倍
第 4 段：1.45 倍
```

这里说的是**主角当前的四段轻攻击**，不要和 BOSS 的三连击混淆。这些倍率只属于生命伤害。削韧值必须始终直接读取武器数据中的 `15`，不能复用 `ComboMultiplier`，否则第 2～4 段会错误变成 `17.25`、`19.5` 和 `21.75`。

### 1.2 与弹反共享同一韧性池

加入普通武器削韧后，“必须两次弹反才能处决”不再是唯一途径。所有合法削韧来源共同消耗同一条 Poise：

```text
纯弹反：
100 → 50 → 0

纯普通攻击：
100 → 85 → 70 → 55 → 40 → 25 → 10 → 0

混合示例：
弹反一次：100 → 50
普通命中四次：50 → 35 → 20 → 5 → 0
```

这是本册的正式规则：**任意合法来源把 Poise 首次降到 `0`，都可以进入可处决状态。**

### 1.3 普通攻击不伪装成弹反

普通武器命中绝对不要发送：

```text
Boss.Event.Parried
```

也不要直接激活：

```text
GA_Boss_ParryStagger
```

二者表示“BOSS 的攻击被玩家弹反”，会取消 BOSS 攻击并播放一秒弹反僵直。普通武器只调用 BOSS 的集中削韧入口；未破韧时不产生额外动作，破韧时由集中入口发送 `Boss.Event.PoiseBroken`。

### 1.4 本册不改变生命伤害资格

本册只新增 Poise 结算，不改变现有生命伤害规则：

- 可处决状态下是否仍能受到普通生命伤害，保持当前项目行为；
- 普通受击动画仍受现有 `Boss.Status.HitReactWindow` 约束；
- 一击直接把 Health 降到 `0` 时，死亡优先，不再同时进入可处决；
- 本册不会给 Executable 增加无敌标签。

如果以后希望“可处决期间完全免疫普通生命伤害”，应单独设计，不要借削韧入口顺便修改。

---

## 2. 前置条件检查

### 2.1 必须先完成原指导

至少完成《主角格挡、弹反、破防与BOSS韧性处决实现指导》的以下内容：

| 原指导章节 | 必须已经存在的内容 |
|---|---|
| 第 7 节 | `Boss.Event.PoiseBroken`、`Combat.SetByCaller.PoiseDelta` |
| 第 9.5 节 | `UFirstGE_PoiseChange` |
| 第 16 节 | `ABossCharacter` 的 Poise GE 入口与五秒计时 |
| 第 17 节 | `UGA_Boss_ParryStagger` |
| 第 18 节 | `UGA_Boss_Executable` |
| 初始化 GE | `Poise / MaxPoise = 100 / 100` |

### 2.2 前置验收

实施本册前，原流程必须已经满足：

```text
第一次弹反：100 → 50
→ 播放 1 秒弹反僵直

50 点状态下 5 秒没有新削韧：50 → 100

5 秒内第二次弹反：50 → 0
→ 进入 Executable
→ 5 秒未处决时退出并恢复到 100
```

如果上述任意一步失败，先继续排查原指导。武器削韧依赖这条基础链，提前接入只会让问题来源更难区分。

---

## 3. 当前项目的真实命中链

当前玩家近战流程是：

```text
AFirstWeaponBase::OnCollisionBoxBeginOverlap
→ UDKCombatComponent::OnHitTargetActor
→ 用 OverlappedActors 对“同一碰撞窗口、同一目标”去重
→ 发送 DK.Event.MeleeHit
→ UFirstGA_DKLightAttack::HandleMeleeHit
→ 制作并应用现有生命伤害 Spec
```

削韧应该接在 `HandleMeleeHit()`，因为这里同时知道：

- 这次武器命中已经通过碰撞窗口去重；
- 当前装备的是哪把武器；
- 当前 Ability Level；
- 被命中的具体目标；
- 生命伤害应该在何时结算。

不应把削韧写进：

| 位置 | 原因 |
|---|---|
| `AFirstWeaponBase` 的 Overlap | 武器 Actor 只报告碰撞，不拥有攻击规则；还可能绕过去重层 |
| `FirstANS_WeaponCollision` | Notify State 只控制有效帧，不知道最终命中了谁 |
| `UGEExecCalc_DamageTaken` | 会把陷阱、环境伤害或其它复用该公式的伤害也变成削韧 |
| `UFirstGE_Damage` | 它应该继续只描述生命伤害计算 |
| 行为树 | 行为树不负责单次命中的微观战斗结算 |

---

## 4. 文件、父类和路径总览

本扩展**不创建任何新的 `UCLASS`**，因此不需要使用“工具 → 新建 C++ 类”，也没有新的父类选择步骤。

| 修改对象 | 类型或父类 | 文件路径 | 修改职责 |
|---|---|---|---|
| `EFirstPoiseDamageResult` | `UENUM`，无父类、无 `.cpp` | `Source/First/Public/Types/FirstCombatTypes.h` | 集中削韧入口的三态结果 |
| `FFirstDKWeaponData` | `USTRUCT`，无父类 | `Source/First/Public/Types/FirstStructTypes.h` | 保存武器削韧值 `15` |
| `UDKCombatComponent` | `UFirstCombatComponent` | `Public/Components/Combat/DKCombatComponent.h`、`Private/Components/Combat/DKCombatComponent.cpp` | 读取当前武器削韧值 |
| `ABossCharacter` | `AEnemyCharacter` | `Public/Character/BossCharacter.h`、`Private/Character/BossCharacter.cpp` | 唯一扣韧、计时与破韧发布入口 |
| `UFirstGA_DKLightAttack` | `UFirstDKGameplayAbility` | `Private/AbilitySystem/Abilities/DK/FirstGA_DKLightAttack.cpp` | 在真实命中后请求削韧 |
| `UGA_Boss_ParryStagger` | `UFirstBossGameplayAbility` | `Private/AbilitySystem/Abilities/BOSS/GA_Boss_ParryStagger.cpp` | 弹反改用同一削韧入口 |
| `UGA_Boss_Executable` | `UFirstBossGameplayAbility` | `Private/AbilitySystem/Abilities/BOSS/GA_Boss_Executable.cpp` | 破韧时取消拔剑、攻击与旧反应能力 |
| `BP_DKWeapon_Sword` | `ADKWeapon` 蓝图子类 | `/Game/DKCharacter/Weapons/BP_DKWeapon_Sword` | 明确填写 `Weapon Poise Damage = 15` |

### 4.1 本册复用而不新建的内容

```text
UFirstGE_PoiseChange
Combat.SetByCaller.PoiseDelta
Boss.Event.PoiseBroken
Boss.Status.Executable
Boss.Status.BeingExecuted
Boss.Status.Executed
现有五秒 PoiseRecoveryTimer
现有 GA_Boss_Executable
```

不要为了 `15` 再创建一个 `FirstGE_WeaponPoiseDamage`。`UFirstGE_PoiseChange` 本来就是通用的 SetByCaller 数值工具：弹反传 `-50`、武器命中传 `-15`、回满传正数差值。

---

## 5. 为什么必须集中封装

如果普通攻击和弹反各自编写下面的逻辑：

```text
扣 Poise
→ 自己检查是否为 0
→ 自己刷新计时器
→ 自己发送 PoiseBroken
```

很容易出现：

- 同一帧重复发送两次 `PoiseBroken`；
- Poise 已经为 `0`，后续命中仍刷新可处决窗口；
- 某个新招式只扣了数值，却忘记触发可处决；
- 普通攻击误用了弹反的一秒僵直；
- 正数回满时错误重启五秒计时；
- 致死一击同时激活死亡与 Executable。

因此本册规定：

```text
ABossCharacter::ApplyPoiseDamage
├─ 是所有负向 Poise 的唯一公开入口
├─ 是 Boss.Event.PoiseBroken 的唯一发布者
├─ 只有实际下降才刷新五秒计时
├─ 使用 OldPoise > 0 && NewPoise <= 0 作为一次性归零边沿
└─ 返回 Ignored / Reduced / Broken
```

不额外保存 `bPoiseBrokenSent`。旧值为正、新值为零的边沿本身就能天然去重；恢复到正数后，下一轮仍能再次正常破韧。

---

## 6. 增加三态削韧结果

修改：

`Source/First/Public/Types/FirstCombatTypes.h`

在现有 `EFirstDefenseResult` 后、`FFirstMeleeDefenseData` 前增加：

```cpp
// 一次削韧请求的最终结果。
// 普通武器只关心是否请求成功；弹反需要区分 Reduced 与 Broken，
// 以决定是否继续播放 1 秒普通弹反僵直。
UENUM(BlueprintType)
enum class EFirstPoiseDamageResult : uint8
{
	// 参数或状态不合法、目标已死亡/可处决、Poise 已为 0，
	// 或 GE 没有让数值实际下降。
	Ignored,

	// Poise 已实际下降，但仍大于 0。
	Reduced,

	// 本次请求把 Poise 从正数首次降到了 0。
	Broken
};
```

不要新建 `.cpp`。`FirstCombatTypes.generated.h` 仍必须保持为这个头文件的最后一个 `#include`。

三种返回值的用途：

| 结果 | 普通武器 | 弹反 |
|---|---|---|
| `Ignored` | 不做额外处理 | 安全结束弹反反应，不创建一秒任务 |
| `Reduced` | 只更新韧性条 | 播放一秒普通弹反僵直 |
| `Broken` | 由事件进入 Executable | 不再叠加一秒僵直，直接进入 Executable |

### 编译闸门 1

关闭编辑器，完整编译 `FirstEditor Win64 Development`。新增 `UENUM` 后不要依赖 Live Coding。

---

## 7. 把 ABossCharacter 改成唯一削韧入口

这一节假设原指导第 16 节已经为 `ABossCharacter` 加入：

```cpp
void ApplyPoiseDelta(float Delta);
void RestartPoiseRecoveryTimer();
void ClearPoiseRecoveryTimer();
void RestorePoiseToFull();
```

现在要把“原始 Delta 工具”和“受到削韧的完整规则”分开：

```text
ApplyPoiseDeltaRaw
└─ private，只负责通过 GE 改数值

ApplyPoiseDamage
└─ public，负责状态门控、实际扣韧、计时、首次归零和事件
```

这样未来不会有攻击 Ability 直接调用 `ApplyPoiseDelta(-15)`，绕过破韧判定。

### 7.1 修改 BossCharacter.h

文件：

`Source/First/Public/Character/BossCharacter.h`

确认顶部已经包含：

```cpp
#include "Types/FirstCombatTypes.h"
```

原格挡弹反指导在第 15.2 节已经要求增加它。如果缺失才补，不要重复写两次。

它必须写在：

```cpp
#include "BossCharacter.generated.h"
```

之前。和所有 Unreal 反射头文件一样，`BossCharacter.generated.h` 必须继续作为该文件的最后一个 `#include`，不能把新 include 追加到它下面。

把 public 区域原来的：

```cpp
	// 所有 Poise 改动都经过 GE，保留 Clamp 与 UI 广播。
	void ApplyPoiseDelta(float Delta);
	void RestartPoiseRecoveryTimer();
	void ClearPoiseRecoveryTimer();
	void RestorePoiseToFull();
```

替换为：

```cpp
	// 所有负向 Poise 来源的唯一公开入口。
	// Damage 使用正数，例如普通武器传 15、弹反传 50。
	EFirstPoiseDamageResult ApplyPoiseDamage(
		float Damage,
		AActor* SourceActor);

	// Executable Ability 仍需要控制这三个生命周期工具。
	void RestartPoiseRecoveryTimer();
	void ClearPoiseRecoveryTimer();
	void RestorePoiseToFull();
```

在 private 区域增加：

```cpp
	// 原始 GE 数值工具。只供 ABossCharacter 内部调用：
	// 负数只能由 ApplyPoiseDamage 传入，正数只用于恢复。
	void ApplyPoiseDeltaRaw(float Delta, AActor* SourceActor);
```

同时把 `PoiseRecoveryDelay` 的最小值从 `0.0` 改为 `0.1`：

```cpp
	UPROPERTY(EditDefaultsOnly, BlueprintReadOnly,
		Category="Boss|Combat|Poise",
		meta=(AllowPrivateAccess="true", ClampMin="0.1", UIMin="0.1"))
	float PoiseRecoveryDelay = 5.f;
```

这是为了防止 `0` 秒计时器在削韧函数尚未完成时同步回满，然后又继续发送破韧事件。默认值仍然是 `5` 秒。

### 7.2 替换 BossCharacter.cpp 的原始 Delta 工具

文件：

`Source/First/Private/Character/BossCharacter.cpp`

确认顶部已经包含：

```cpp
#include "AbilitySystemBlueprintLibrary.h"
#include "AbilitySystem/FirstAbilitySystemComponent.h"
#include "AbilitySystem/FirstAttributeSet.h"
#include "AbilitySystem/GameplayEffects/FirstGE_PoiseChange.h"
#include "MyGameplayTags.h"
```

把原来的整个 `ABossCharacter::ApplyPoiseDelta` 替换成：

```cpp
void ABossCharacter::ApplyPoiseDeltaRaw(
	float Delta,
	AActor* SourceActor)
{
	UFirstAbilitySystemComponent* ASC =
		GetFirstAbilitySystemComponent();
	if (!ASC || FMath::IsNearlyZero(Delta))
	{
		return;
	}

	// SourceActor 只用于正确记录本次削韧来自谁。
	// 回满时传 this；普通武器/弹反时传玩家。
	AActor* EffectiveSource = IsValid(SourceActor)
		? SourceActor
		: this;

	FGameplayEffectContextHandle Context = ASC->MakeEffectContext();
	Context.AddSourceObject(EffectiveSource);
	Context.AddInstigator(EffectiveSource, EffectiveSource);

	FGameplayEffectSpecHandle Spec = ASC->MakeOutgoingSpec(
		UFirstGE_PoiseChange::StaticClass(),
		1.f,
		Context);

	if (!Spec.IsValid())
	{
		return;
	}

	Spec.Data->SetSetByCallerMagnitude(
		MyGameplayTags::Combat_SetByCaller_PoiseDelta,
		Delta);

	ASC->ApplyGameplayEffectSpecToSelf(*Spec.Data.Get());
}
```

注意这里已经删除原来的：

```cpp
if (Delta < 0.f)
{
	RestartPoiseRecoveryTimer();
}
```

计时器不能由 Raw 工具负责。只有确认 Poise **真的下降**后，公开入口才允许刷新计时。

### 7.3 新增 ApplyPoiseDamage

紧跟 `ApplyPoiseDeltaRaw()` 增加：

```cpp
EFirstPoiseDamageResult ABossCharacter::ApplyPoiseDamage(
	float Damage,
	AActor* SourceActor)
{
	UFirstAbilitySystemComponent* ASC =
		GetFirstAbilitySystemComponent();

	if (!ASC || !FirstAttributeSet ||
		!FMath::IsFinite(Damage) ||
		Damage <= KINDA_SMALL_NUMBER)
	{
		return EFirstPoiseDamageResult::Ignored;
	}

	// 已经进入终局状态时，不再修改 Poise，也不刷新可处决窗口。
	if (ASC->HasMatchingGameplayTag(
			MyGameplayTags::Shared_Status_Dead) ||
		ASC->HasMatchingGameplayTag(
			MyGameplayTags::Boss_Status_Executable) ||
		ASC->HasMatchingGameplayTag(
			MyGameplayTags::Boss_Status_BeingExecuted) ||
		ASC->HasMatchingGameplayTag(
			MyGameplayTags::Boss_Status_Executed))
	{
		return EFirstPoiseDamageResult::Ignored;
	}

	const float OldPoise = FirstAttributeSet->GetPoise();
	if (!FMath::IsFinite(OldPoise) ||
		OldPoise <= KINDA_SMALL_NUMBER)
	{
		return EFirstPoiseDamageResult::Ignored;
	}

	// 外部传正数，只有这里负责转换成 GE 使用的负 Delta。
	ApplyPoiseDeltaRaw(-Damage, SourceActor);

	const float NewPoise = FirstAttributeSet->GetPoise();
	if (!FMath::IsFinite(NewPoise) ||
		NewPoise >= OldPoise)
	{
		// Spec 无效、GE 未执行或数值没有实际下降时，不启动计时器。
		return EFirstPoiseDamageResult::Ignored;
	}

	// 每次“实际下降”都从头计算五秒。
	// 破韧这一击也要启动：这同一个 Timer 就是可处决窗口计时。
	RestartPoiseRecoveryTimer();

	if (NewPoise > KINDA_SMALL_NUMBER)
	{
		return EFirstPoiseDamageResult::Reduced;
	}

	// OldPoise > 0 且 NewPoise == 0：天然的一次性归零边沿。
	// Poise 已为 0 的后续命中会在前面被忽略，因此不会重复发送。
	FGameplayEventData PoiseBrokenEvent;
	PoiseBrokenEvent.Instigator = IsValid(SourceActor)
		? SourceActor
		: this;
	PoiseBrokenEvent.Target = this;
	PoiseBrokenEvent.EventMagnitude = OldPoise - NewPoise;

	UAbilitySystemBlueprintLibrary::SendGameplayEventToActor(
		this,
		MyGameplayTags::Boss_Event_PoiseBroken,
		PoiseBrokenEvent);

	return EFirstPoiseDamageResult::Broken;
}
```

不要加入下面这个门控：

```cpp
if (ASC->HasMatchingGameplayTag(MyGameplayTags::Boss_Status_Staggered))
{
	return EFirstPoiseDamageResult::Ignored;
}
```

`Boss.Status.Staggered` 同时用于普通受击、弹反僵直和可处决表现。玩家应该能在普通受击或一秒弹反僵直期间继续攻击并削韧；真正需要忽略的可处决状态已经由 `Executable / BeingExecuted / Executed` 单独拦截。

### 7.4 让计时器只异步触发

整体替换 `RestartPoiseRecoveryTimer()`：

```cpp
void ABossCharacter::RestartPoiseRecoveryTimer()
{
	GetWorldTimerManager().ClearTimer(PoiseRecoveryTimerHandle);

	// 不能在 ApplyPoiseDamage 调用栈中同步回满。
	const float SafeDelay = FMath::Max(PoiseRecoveryDelay, 0.1f);

	GetWorldTimerManager().SetTimer(
		PoiseRecoveryTimerHandle,
		this,
		&ThisClass::HandlePoiseRecoveryExpired,
		SafeDelay,
		false);
}
```

### 7.5 修改 RestorePoiseToFull

整体替换为：

```cpp
void ABossCharacter::RestorePoiseToFull()
{
	// 正常到期、异常取消或手动恢复都先清理旧 Timer，
	// 防止已经回满后残留回调再次执行。
	ClearPoiseRecoveryTimer();

	if (!FirstAttributeSet)
	{
		return;
	}

	const float MissingPoise =
		FirstAttributeSet->GetMaxPoise() -
		FirstAttributeSet->GetPoise();

	if (MissingPoise > 0.f)
	{
		ApplyPoiseDeltaRaw(MissingPoise, this);
	}
}
```

正数恢复只经过 Raw 工具，不进入 `ApplyPoiseDamage()`，所以不会错误发送破韧事件或重新启动五秒计时。

### 7.6 集中入口的不变量

完成后必须始终满足：

```text
所有负向 Poise
→ 只能调用 ApplyPoiseDamage(正数 Damage)

所有正向恢复
→ 只能由 RestorePoiseToFull
→ 内部调用 ApplyPoiseDeltaRaw(正数 Delta)

Boss.Event.PoiseBroken
→ 只能由 ApplyPoiseDamage 发布
```

### 7.7 先不要在这里单独编译

第 7.1 节已经删除旧的 `ApplyPoiseDelta()`，而现有 `GA_Boss_ParryStagger.cpp` 此刻仍在调用它。因此第 7 节与第 8 节必须连续完成；如果现在单独编译，会出现“找不到 `ApplyPoiseDelta`”的预期错误。

完成下一节、把弹反改为 `ApplyPoiseDamage()` 后，再执行编译闸门 2。

---

## 8. 让弹反也使用集中入口

普通武器和弹反必须共享 `ApplyPoiseDamage()`，否则仍然会保留两套归零判定。

修改：

`Source/First/Private/AbilitySystem/Abilities/BOSS/GA_Boss_ParryStagger.cpp`

在 `ActivateAbility()` 中保留原有的：

- 取消 BOSS 当前攻击；
- 关闭 BOSS 武器碰撞；
- 停止移动；
- 从 `TriggerEventData->EventMagnitude` 读取弹反削韧 `50`。

把从：

```cpp
Boss->ApplyPoiseDelta(-PoiseDamage);
```

到手动创建并发送 `PoiseBrokenEvent` 的整段代码删除，替换为：

```cpp
	AActor* PoiseSource = TriggerEventData
		? const_cast<AActor*>(TriggerEventData->Instigator.Get())
		: nullptr;

	const EFirstPoiseDamageResult PoiseResult =
		Boss->ApplyPoiseDamage(PoiseDamage, PoiseSource);

	// PoiseBroken 会同步激活 Executable；Executable 又可能立刻取消
	// 当前 ParryStagger。返回后先检查生命周期，不能继续创建旧任务。
	if (!IsActive())
	{
		return;
	}

	if (PoiseResult == EFirstPoiseDamageResult::Broken)
	{
		// 归零事件已经由 Boss 统一发送，不再叠加 1 秒僵直。
		FinishParryStagger(false);
		return;
	}

	if (PoiseResult == EFirstPoiseDamageResult::Ignored)
	{
		// 没有发生实际削韧时，不能伪装成普通 Reduced。
		FinishParryStagger(true);
		return;
	}

	// 只有 Reduced 会继续执行下面原有的 Montage + WaitDelay 1 秒逻辑。
```

后面的注释不要再写死为：

```text
100 → 50
```

普通武器可能已经把 Poise 降到任意正数。应改为：

```cpp
	// Poise 已实际下降但仍大于 0：播放普通 1 秒弹反僵直。
```

删除手动发送事件后，如果该文件其它位置不再使用 `UAbilitySystemBlueprintLibrary`，可以删除：

```cpp
#include "AbilitySystemBlueprintLibrary.h"
```

### 8.1 为什么返回后必须检查 IsActive

这是一条容易漏掉的同步调用链：

```text
GA_Boss_ParryStagger
→ ApplyPoiseDamage(50)
→ Poise 归零
→ 同步发送 Boss.Event.PoiseBroken
→ GA_Boss_Executable 同步激活
→ Executable 取消 ParryStagger
→ ApplyPoiseDamage 返回到已经结束的 ParryStagger
```

如果没有 `if (!IsActive()) return;`，已经被取消的弹反 Ability 仍会继续创建 Montage Task 和 WaitDelay，之后与可处决状态冲突。

### 编译闸门 2

第 7、8 节连续完成后，关闭编辑器并完整编译。此时普通武器还没有接入，但下面的原弹反回归必须先通过：

```text
第一次弹反：Reduced → 一秒僵直
第二次弹反：Broken → 同步发送 PoiseBroken
```

---

## 9. 破韧时取消旧的 BOSS 冲突能力

普通武器可能在以下时机把 Poise 打到 `0`：

- BOSS 正在拔剑；
- BOSS 正在普通/三连攻击；
- BOSS 正在原有普通 HitReact；
- BOSS 正处于弹反的一秒僵直。

因此 Executable 激活时不能只取消 `Boss.Ability.Attack`。

修改：

`Source/First/Private/AbilitySystem/Abilities/BOSS/GA_Boss_Executable.cpp`

在 `ActivateAbility()` 中，把原来的：

```cpp
	FGameplayTagContainer AttackTags;
	AttackTags.AddTag(MyGameplayTags::Boss_Ability_Attack);
	ASC->CancelAbilities(&AttackTags, nullptr, this);
```

替换为：

```cpp
	// 普通武器可能在拔剑、攻击、普通受击或弹反僵直期间打空 Poise。
	// CancelAbilities 对 WithTags 使用 HasAny，因此命中任一标签都会被取消。
	FGameplayTagContainer AbilitiesToCancel;
	AbilitiesToCancel.AddTag(MyGameplayTags::Boss_Ability_DrawSword);
	AbilitiesToCancel.AddTag(MyGameplayTags::Boss_Ability_Attack);
	AbilitiesToCancel.AddTag(MyGameplayTags::Boss_Ability_ParryStagger);
	AbilitiesToCancel.AddTag(MyGameplayTags::Boss_Ability_HitReact);

	ASC->CancelAbilities(&AbilitiesToCancel, nullptr, this);
```

当前 `GA_Boss_DrawSword::HandleMontageInterrupted()` 会调用 `AttachWeaponToHand()`，所以精确取消拔剑时仍能把剑挂到手并写入 `Boss.Status.WeaponDrawn`，不会让后续攻击因为“武器未拔出”而永久失效。若你以后重写拔剑 Ability，必须继续保留这条取消兜底。

保留后面的：

```text
关闭 BOSS 武器碰撞
停止 BOSS 移动
播放 Executable Start / Loop
```

不要改成 `CancelAllAbilities()`。它可能误杀死亡、常驻监听或以后新增的系统 Ability；按资产标签精确取消已经足够。

### 编译闸门 3

完整编译并先回归原弹反流程：

```text
第一次弹反：Reduced → 一秒僵直
第二次弹反：Broken → Executable
第二次破韧后不再残留一秒 ParryStagger Delay
```

这一关通过后，再给武器增加 `15`。

---

## 10. 给武器数据增加削韧值

这一节只扩展现有 `FFirstDKWeaponData`，不新建武器类，也不把数值写死在攻击 Ability 中。

最终的数据归属是：

```text
BP_DKWeapon_Sword
└─ DK Weapon Data
   ├─ Weapon Base Damage       ← 生命伤害
   └─ Weapon Poise Damage = 15 ← 削韧
```

这样以后换成重剑、匕首或其它武器时，可以分别配置削韧，而不必复制攻击 Ability。

### 10.1 修改 FirstStructTypes.h

文件：

`Source/First/Public/Types/FirstStructTypes.h`

在 `FFirstDKWeaponData` 的 `WeaponBaseDamage` 后增加：

```cpp
	// 当前武器每次有效近战命中的基础削韧。
	// 首版所有连击段统一读取 15，不使用生命伤害 ComboMultiplier。
	UPROPERTY(EditDefaultsOnly, BlueprintReadOnly,
		meta=(ClampMin="0.0", UIMin="0.0"))
	FScalableFloat WeaponPoiseDamage = FScalableFloat(15.f);
```

这个文件已经包含：

```cpp
#include "ScalableFloat.h"
```

因此不需要再添加第二次 include。

这里继续使用 `FScalableFloat`，是为了和 `WeaponBaseDamage` 保持一致，也方便未来按武器等级或曲线成长。首版只使用固定值 `15`。

### 10.2 在 DKCombatComponent.h 声明读取函数

文件：

`Source/First/Public/Components/Combat/DKCombatComponent.h`

父类仍然是现有的：

```text
UDKCombatComponent
└─ UFirstCombatComponent
```

在 `GetDKCurrentEquippedWeaponDamageAtLevel()` 后增加：

```cpp
	// 读取当前武器在指定 Ability Level 下的削韧值。
	UFUNCTION(BlueprintCallable, Category="First | Combat")
	float GetDKCurrentEquippedWeaponPoiseDamageAtLevel(
		float InLevel) const;
```

### 10.3 在 DKCombatComponent.cpp 实现读取函数

文件：

`Source/First/Private/Components/Combat/DKCombatComponent.cpp`

紧跟 `GetDKCurrentEquippedWeaponDamageAtLevel()` 增加：

```cpp
float UDKCombatComponent::
	GetDKCurrentEquippedWeaponPoiseDamageAtLevel(
		float InLevel) const
{
	const ADKWeapon* CurrentWeapon =
		GetDKCurrentEquippedWeapon();

	const float PoiseDamage = CurrentWeapon
		? CurrentWeapon->DKWeaponData.WeaponPoiseDamage
			.GetValueAtLevel(InLevel)
		: 0.f;

	// 即使以后蓝图或曲线配置错误，也不允许返回负削韧。
	return FMath::Max(PoiseDamage, 0.f);
}
```

这层读取函数的职责只是：

```text
当前装备武器
→ 读取 WeaponPoiseDamage
→ 按 Ability Level 求值
→ 保证结果不小于 0
```

它不应该在这里寻找 BOSS、扣 Poise 或发送破韧事件。

### 编译闸门 4

关闭编辑器并完整编译。因为这一步给 `USTRUCT` 增加了 `UPROPERTY`，不要只用 Live Coding。

编译成功后再打开编辑器，确认 `BP_DKWeapon_Sword` 的 `DK Weapon Data` 中能看到新的 `Weapon Poise Damage`。

---

## 11. 在真实武器命中后结算削韧

修改：

`Source/First/Private/AbilitySystem/Abilities/DK/FirstGA_DKLightAttack.cpp`

### 11.1 增加 BOSS 头文件

在顶部 include 区域增加：

```cpp
#include "Character/BossCharacter.h"
```

### 11.2 整体替换 HandleMeleeHit

把现有 `HandleMeleeHit()` 整体替换为：

```cpp
// 一次已经通过碰撞窗口去重的真实武器命中：
// 先结算生命伤害，再按武器数据请求削韧。
void UFirstGA_DKLightAttack::HandleMeleeHit(
	FGameplayEventData Payload)
{
	UDKCombatComponent* CombatComponent =
		GetDKCombatComponentFromActorInfo();
	AActor* TargetActor =
		const_cast<AActor*>(Payload.Target.Get());

	if (!CombatComponent || !TargetActor)
	{
		return;
	}

	const float WeaponBaseDamage = CombatComponent
		->GetDKCurrentEquippedWeaponDamageAtLevel(
			GetAbilityLevel());

	const float WeaponPoiseDamage = CombatComponent
		->GetDKCurrentEquippedWeaponPoiseDamageAtLevel(
			GetAbilityLevel());

	if (WeaponBaseDamage <= 0.f &&
		WeaponPoiseDamage <= 0.f)
	{
		return;
	}

	// 必须先结算生命伤害。
	// 如果这一击已经致死，随后 ApplyPoiseDamage 的 Dead 门控
	// 会阻止同一击再进入 Executable。
	if (WeaponBaseDamage > 0.f)
	{
		FGameplayEffectSpecHandle DamageSpec =
			MakeDKDamageEffectSpecHandle(
				UFirstGE_Damage::StaticClass(),
				WeaponBaseDamage,
				CurrentComboStep);

		ApplyEffectSpecHandleToTarget(
			TargetActor,
			DamageSpec);
	}

	// 首版只让 ABossCharacter 消耗 Poise。
	// 普通敌人仍只接收上面的生命伤害。
	if (WeaponPoiseDamage > 0.f)
	{
		if (ABossCharacter* BossTarget =
			Cast<ABossCharacter>(TargetActor))
		{
			BossTarget->ApplyPoiseDamage(
				WeaponPoiseDamage,
				GetAvatarActorFromActorInfo());
		}
	}
}
```

可以顺手把 `Source/First/Public/AbilitySystem/Abilities/DK/FirstGA_DKLightAttack.h` 中 `HandleMeleeHit` 上方旧的“只创建 Damage GE”注释改成“先结算生命伤害，再请求削韧”。这只是同步注释，不需要改函数声明。

### 11.3 为什么生命伤害必须在削韧前面

假设某次攻击同时满足：

```text
BOSS 剩余 Health = 5
BOSS 剩余 Poise  = 10
本次生命伤害足以致死
本次削韧         = 15
```

正确顺序：

```text
先扣 Health
→ BOSS 进入 Dead
→ ApplyPoiseDamage 检测到 Dead
→ 返回 Ignored
→ 只死亡，不进入 Executable
```

如果顺序反过来，可能先激活 Executable，紧接着又进入死亡，造成 Montage、标签和行为树状态互相冲突。

### 11.4 为什么普通攻击可以忽略三态返回值

这里不需要根据 `Reduced / Broken` 再写分支：

- `Reduced`：只让 AttributeSet 和 UI 反映新 Poise；
- `Broken`：`ABossCharacter` 已经发送 `Boss.Event.PoiseBroken`；
- `Ignored`：目标当前状态不接受削韧，安全结束即可。

普通攻击 Ability 不拥有 BOSS 的反应表现。真正的后续行为由 BOSS 自己的 `ParryStagger / Executable` Ability 负责，耦合会更低。

### 11.5 不要用 Instant GE 的返回句柄判断是否命中

不要写成：

```cpp
const FActiveGameplayEffectHandle Handle =
	ApplyEffectSpecHandleToTarget(...);

if (Handle.IsValid())
{
	// 再削韧
}
```

Instant Gameplay Effect 通常不会作为持续 Active Effect 留在 ASC 中，返回句柄不是“这次生命伤害一定成功”的可靠证明。当前 `DK.Event.MeleeHit` 已经表示武器碰撞命中并通过了窗口去重，削韧资格由 BOSS 的集中入口再次做状态门控即可。

### 编译闸门 5

关闭编辑器，完整编译。此时 C++ 接线已经完成，但在蓝图中明确填写 `15` 后再进行最终测试。

---

## 12. 在武器蓝图中填写 15

打开：

```text
/Game/DKCharacter/Weapons/BP_DKWeapon_Sword
```

选择 `Class Defaults`，找到：

```text
DK Weapon Data
└─ Weapon Poise Damage
```

首版设置：

```text
Value       = 15
Curve Table = None
Row Name    = None
```

如果详情面板只显示一个可展开的小三角，先展开 `Weapon Poise Damage`，再填写内部的 `Value`。

虽然 C++ 默认值已经是 `15`，仍建议在现有 `BP_DKWeapon_Sword` 中手动确认并保存。已有蓝图可能序列化旧结构的零值，不能只凭 C++ 默认值猜测它一定会自动变成 `15`。

### 12.1 首版不要在这些地方再填一次 15

不需要在以下位置重复配置：

- `GA_DKLightAttack` 蓝图默认值；
- 攻击 Montage；
- 武器碰撞 Notify State；
- `UFirstGE_Damage`；
- BOSS 蓝图；
- 行为树 Blackboard。

“每把武器削多少韧”只属于武器数据。BOSS 只负责接受一个正数请求并执行统一规则。

---

## 13. 推荐实施与编译顺序

初学阶段不要一次改完所有文件再处理几十条编译错误。按下面顺序分段验收：

| 阶段 | 修改内容 | 通过标准 |
|---|---|---|
| 0 | 先完成原格挡弹反指导 | 弹反 `50`、一秒僵直、破韧、五秒恢复均正常 |
| 1 | `EFirstPoiseDamageResult` | 完整编译成功 |
| 2 | 连续完成 `ABossCharacter` 集中入口与 `ParryStagger` 改接 | 编译成功；原弹反的 Reduced / Broken 分支正常 |
| 3 | `Executable` 精确取消拔剑、攻击、HitReact 与 ParryStagger | 第二次弹反直接进入 Executable，没有残留旧任务 |
| 4 | 武器数据字段与 CombatComponent Getter | 编辑器能看到 `Weapon Poise Damage` |
| 5 | `HandleMeleeHit` 接入 | 普通命中可以固定扣 `15` |
| 6 | `BP_DKWeapon_Sword = 15` | 完整测试表通过 |

凡是新增 `UENUM`、`UPROPERTY` 或修改反射结构的阶段，都应：

```text
保存文件
→ 关闭 Unreal Editor
→ 编译 FirstEditor / Win64 / Development
→ 编译成功后重新打开编辑器
```

不要用 Live Coding 判断新字段是否正确生成。若编辑器曾经崩溃，也先确认代码能完整编译，再重新打开项目。

---

## 14. 完整运行流程

### 14.1 普通命中但未破韧

```text
武器碰撞窗口命中 BOSS
→ CombatComponent 去重
→ DK.Event.MeleeHit
→ HandleMeleeHit
→ 原生命伤害
→ ApplyPoiseDamage(15)
→ 100 → 85
→ 返回 Reduced
→ 刷新五秒回韧计时
→ 不新增一秒弹反僵直
```

### 14.2 第七次普通命中破韧

```text
Poise 10 → 0
→ ApplyPoiseDamage 检测到 Old > 0 && New == 0
→ 刷新五秒计时
→ 只发送一次 Boss.Event.PoiseBroken
→ GA_Boss_Executable 激活
→ 取消拔剑 / 攻击 / HitReact / ParryStagger
→ 关闭武器碰撞并停止移动
→ 进入五秒可处决状态
```

### 14.3 弹反未破韧

```text
ParryStagger
→ ApplyPoiseDamage(50)
→ 返回 Reduced
→ 播放一秒普通弹反僵直
```

### 14.4 混合削韧破韧

```text
普通攻击三次：100 → 85 → 70 → 55
弹反一次：55 → 5
普通攻击一次：5 → 0
→ 由同一入口进入 Executable
```

### 14.5 五秒内没有新削韧

```text
最后一次实际下降
→ RestartPoiseRecoveryTimer
→ 等待 5 秒
→ RestorePoiseToFull
→ 用正 Delta 经过 Poise GE 回到 MaxPoise
```

如果在第 `4.9` 秒再次有效命中：

```text
旧计时器清除
→ 再扣 15
→ 新计时器从 5 秒重新开始
```

如果 Poise 已经为 `0`，后续削韧请求会返回 `Ignored`，既不会重复发送破韧事件，也不会不断延长已存在的可处决窗口。

---

## 15. 最小验收测试表

建议临时显示 BOSS 的 `Health / Poise / 关键 Gameplay Tag`，逐项测试。不要只观察动画，因为动画正常不等于数值和 Ability 生命周期正确。

| 编号 | 操作 | 预期结果 |
|---|---|---|
| T01 | 满韧时普通攻击命中一次 | `100 → 85` |
| T02 | 分别用主角轻攻击第 1、2、3、4 段命中 | 四段都只扣 `15`，不会出现 `17.25 / 19.5 / 21.75` |
| T03 | 单个碰撞窗口持续贴住 BOSS | 同一目标只扣一次生命和一次 Poise |
| T04 | 关闭碰撞窗口后下一段重新打开 | 下一段可以再次扣 `15` |
| T05 | 普通攻击命中非 BOSS 敌人 | 原生命伤害正常，不尝试 BOSS Poise 流程 |
| T06 | 从 `100` 连续有效命中七次 | 第七次 `10 → 0`，只触发一次 Executable |
| T07 | Poise 已为 `0` 时继续命中 | 不重复发送 `PoiseBroken`，不延长窗口 |
| T08 | 扣韧后等待 `4.9` 秒再命中 | 计时器重新从五秒开始 |
| T09 | 扣韧后完整等待五秒 | Poise 恢复到 `MaxPoise` |
| T10 | 弹反一次再普通命中四次 | `100 → 50 → 35 → 20 → 5 → 0` |
| T11 | 普通命中三次再弹反一次 | `100 → 85 → 70 → 55 → 5`，未破韧则只播放一秒僵直 |
| T12 | Poise `40` 时弹反 | `40 → 0`，直接 Executable，不叠加一秒僵直 |
| T13 | 普通攻击直接击杀低血量 BOSS | 只进入死亡，不同时进入 Executable |
| T14 | BOSS 正在攻击时普通命中破韧 | 当前攻击被取消，武器碰撞关闭，进入 Executable |
| T15 | BOSS 正在普通 HitReact 时破韧 | HitReact 被取消，切换到 Executable |
| T16 | BOSS 正在一秒 ParryStagger 时普通命中破韧 | ParryStagger 被取消，不残留旧 Delay 或 Montage |
| T17 | BOSS 正在拔剑时被普通命中破韧 | DrawSword 被取消；剑仍挂到手；进入 Executable |
| T18 | 未破韧的普通武器命中 | 只扣生命/Poise；不凭空播放一秒弹反僵直 |
| T19 | Executable / BeingExecuted / Executed 期间请求削韧 | 返回 `Ignored`，Poise 与计时器不变化 |
| T20 | 第一次可处决超时并回满后再次打空 | 第二轮仍能正常触发一次 `PoiseBroken` |
| T21 | 观察韧性 UI | 满韧时 100%，每次普通命中减少 15 个百分点 |

### 15.1 用日志定位三类问题

调试期间可以临时在 `ApplyPoiseDamage()` 中记录：

```text
SourceActor
OldPoise
Damage
NewPoise
Result
```

推荐只记录一次集中入口，不要同时在 Overlap、CombatComponent 和 Ability 三层都刷屏。问题定位完成后再删除或降低日志等级。

---

## 16. 常见错误与排查

### 16.1 命中后 Poise 完全不变

依次检查：

1. `BP_DKWeapon_Sword` 的 `Weapon Poise Damage` 是否明确为 `15`；
2. 攻击是否真的发送了 `DK.Event.MeleeHit`；
3. `HandleMeleeHit()` 的目标能否 Cast 为 `ABossCharacter`；
4. `UFirstGE_PoiseChange` 是否正确读取 `Combat.SetByCaller.PoiseDelta`；
5. BOSS 的 `Poise / MaxPoise` 是否已初始化为 `100 / 100`；
6. BOSS 是否已经带有 Dead 或三个可处决状态标签。

### 16.2 四段轻攻击的后续段扣得更多

原因通常是削韧复用了：

```cpp
CurrentComboStep
```

或生命伤害的 `ComboMultiplier`。削韧必须直接传：

```cpp
BossTarget->ApplyPoiseDamage(
	WeaponPoiseDamage,
	GetAvatarActorFromActorInfo());
```

不能把 `CurrentComboStep` 交给 Poise GE。

### 16.3 一段攻击重复扣多次 Poise

检查削韧是否误写到了：

- 武器 `BeginOverlap`；
- Anim Notify Tick；
- 伤害 ExecCalc；
- 同一攻击的多个事件监听器。

正确入口只有去重后的 `HandleMeleeHit()`。同时确认每个武器碰撞窗口结束时确实调用 `ToggleWeaponCollision(false)`，使 `OverlappedActors` 为下一段重新清空。

### 16.4 Poise 到 0 却没有进入可处决

检查：

- 是否还有旧代码直接调用 `ApplyPoiseDeltaRaw(-15, ...)`；
- 是否由某处绕过 `ApplyPoiseDamage()`；
- `Boss.Event.PoiseBroken` 是否已经注册为 Native Tag；
- `GA_Boss_Executable` 是否使用该事件作为 Trigger；
- 该 Ability 是否已授予 BOSS；
- BOSS 是否已死亡。

### 16.5 到 0 后重复激活 Executable

通常是普通攻击或弹反仍手动发送 `Boss.Event.PoiseBroken`。全项目搜索后应满足：

```text
SendGameplayEventToActor(... Boss_Event_PoiseBroken ...)
```

只存在于：

```text
ABossCharacter::ApplyPoiseDamage
```

### 16.6 每次命中没有重置五秒

确认 `RestartPoiseRecoveryTimer()` 位于：

```text
NewPoise < OldPoise
```

之后，而不是只放在 `Broken` 分支。`Reduced` 与 `Broken` 都应刷新；`Ignored` 和正数恢复不刷新。

### 16.7 新属性没有出现在蓝图

常见原因是只用了 Live Coding。处理顺序：

```text
关闭编辑器
→ 确认 UPROPERTY 语法正确
→ 完整编译 FirstEditor
→ 重新打开编辑器
```

不要反复删除蓝图尝试“刷新”。

### 16.8 破韧时拔剑、旧攻击或旧受击动画仍在播放

检查 `GA_Boss_Executable` 是否取消了：

```text
Boss.Ability.Attack
Boss.Ability.DrawSword
Boss.Ability.ParryStagger
Boss.Ability.HitReact
```

并确认这些 Gameplay Ability 的 `AssetTags` 与上面相符。取消标签写对，但 Ability 自己没有对应 Asset Tag，同样不会被取消。

### 16.9 致死一击同时出现死亡和可处决

确认 `HandleMeleeHit()` 的顺序是：

```text
生命伤害
→ 削韧
```

并确认死亡流程会及时添加 `Shared.Status.Dead`。不要通过交换顺序来规避其它错误。

### 16.10 编译提示找不到 EFirstPoiseDamageResult

确认使用该枚举的头文件能看到：

```cpp
#include "Types/FirstCombatTypes.h"
```

同时确保 `FirstCombatTypes.generated.h` 仍然是它所属头文件中的最后一个 include。

---

## 17. 本册明确不修改的内容

为了把首版范围控制清楚，本册不做以下扩展：

- 不修改 `FirstANS_WeaponCollision`；
- 不修改 `UGEExecCalc_DamageTaken`；
- 不把 Poise 合并进 `UFirstGE_Damage`；
- 不修改 `UFirstAttributeSet` 现有 Clamp 与 UI 广播；
- 不修改行为树或 Blackboard；
- 不创建新的 Gameplay Tag；
- 不创建新的 Gameplay Effect；
- 不创建新的 C++ 类；
- 不恢复“首次降到 50 就大僵直”的旧规则；
- 不改变弹反固定削韧 `50`；
- 不给当前四段轻攻击设置不同削韧；
- 不给普通武器命中增加一秒强制僵直；
- 不处理重攻击、技能、投射物或环境伤害的削韧；
- 不改变 Executable 状态下的生命伤害资格。

这不是功能缺失，而是首版边界。先让“一次真实武器命中固定扣 15、所有来源共享破韧入口”稳定工作，再扩展新来源。

---

## 18. 后续扩展建议

### 18.1 多种武器

以后可以直接在不同武器蓝图中配置：

```text
匕首：8
长剑：15
重剑：25
```

攻击 Ability 不需要跟着修改。

### 18.2 不同招式倍率

如果以后确实希望重击削韧更高，建议新增独立的“削韧倍率”，不要复用生命伤害 ComboMultiplier：

```text
最终削韧 = WeaponPoiseDamage × AttackPoiseMultiplier
```

普通四段轻攻击仍可保持 `1.0`，重击或蓄力攻击再使用更高倍率。

### 18.3 多种带韧性的敌人

首版用：

```cpp
Cast<ABossCharacter>(TargetActor)
```

足够直接、容易调试。等普通精英敌人也需要 Poise 时，再抽象一个接口，例如：

```text
IFirstPoiseDamageable
└─ ApplyPoiseDamage
```

不要为了只有一个 BOSS 的当前需求提前增加接口层。

### 18.4 表现层反馈

数值流程稳定后，可以分别为三态增加表现：

```text
Ignored → 无反馈
Reduced → 韧性条闪烁、轻微音效或火花
Broken  → 强音效、镜头反馈、可处决提示
```

表现只读取结果或状态变化，不应重新负责扣 Poise。

---

## 19. 最终职责边界

完成后，各层职责应保持为：

| 层 | 只负责什么 | 不负责什么 |
|---|---|---|
| 武器蓝图 | 保存基础生命伤害与基础削韧 `15` | 不直接修改目标属性 |
| 武器 Actor | 报告碰撞 | 不决定伤害、削韧或破韧 |
| CombatComponent | 去重命中、读取当前武器数据 | 不播放 BOSS 反应 |
| LightAttack Ability | 先申请生命伤害，再请求 BOSS 削韧 | 不发送破韧事件、不播放 BOSS 僵直 |
| `ABossCharacter` | 唯一扣韧、刷新计时、判断首次归零并发事件 | 不决定玩家连击倍率 |
| ParryStagger Ability | 弹反取消攻击；Reduced 时播放一秒僵直 | 不再自行扣韧或判断归零 |
| Executable Ability | 取消冲突能力并管理五秒可处决状态 | 不重复计算是谁造成破韧 |
| AttributeSet / Poise GE | 修改、Clamp 并广播属性 | 不决定战斗状态转换 |

最终自检：

```text
[ ] 普通武器每个有效命中固定削韧 15
[ ] 主角四段轻攻击均为 15
[ ] 同一碰撞窗口对同一目标只结算一次
[ ] 弹反仍固定削韧 50
[ ] 普通命中未破韧时没有一秒弹反僵直
[ ] 所有负向 Poise 都经过 ApplyPoiseDamage
[ ] Poise 首次归零只发送一次 PoiseBroken
[ ] 最后一次实际削韧会重置五秒计时
[ ] Poise 已为 0 的后续命中不会延长窗口
[ ] 致死一击只进入死亡
[ ] 破韧能取消拔剑、攻击、HitReact 与 ParryStagger
[ ] 本册没有新增 GE、Tag 或 C++ 类
```

全部通过后，主角武器削韧首版才算完成。
