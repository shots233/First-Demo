# 主角武器拖影与剑光实现指导

> 适用项目：`First`（UE 5.6 / C++ / GAS / Enhanced Input / 中文版编辑器）
>
> 使用素材：`SlashTrail_SoftTofu`（已将完整内容放在 `Content/SlashTrail_SoftTofu`，无需再导入）
>
> 范围说明：本册只接入「武器挥砍拖影」与「常驻剑身光」两类效果；命中火花、射弹刀光、BOSS 武器拖影属于后续扩展。
>
> 本文只提供实现指导，不会自动修改 C++、蓝图或蒙太奇。

---

## 0. 最终效果

```text
装备剑后挥出轻攻击
→ 刀身划过期间出现弧光拖影（Ribbon 丝带随武器运动生成）
→ 收招/碰撞窗口结束 → 拖影立即消失
→ 换风格只需改武器蓝图中的一个资源引用

可选：配一把“常驻光刃”
→ 持剑期间刀身持续发光（不随挥砍开关）
```

拖影不是粒子喷泉，而是“随武器高速运动生成的丝带”：武器静止时不会显示，挥刀越快越明显。

## 1. 素材包结构速览

| 目录/资产 | 用途 | 本册是否使用 |
|---|---|---|
| `Niagara/<风格>/NS_SlashTrail_<风格>` | 挥砍拖影（Ribbon） | ✅ 主用 |
| `Niagara/<风格>/NS_SlashTrail_<风格>_Loop` | 常驻拖影循环版 | ❌ 暂不用 |
| `Niagara/<风格>/NS_Sword_<风格>` | 剑身光刃（常驻发光） | ✅ 可选 |
| `Niagara/<风格>/NS_Hit_<风格>` | 命中目标火花 | ❌ 后续扩展 |
| `Blueprint/*/BP_Slash_Projectile_*` | 射弹式刀光 | ❌ 远程招式才用 |
| `Map/Overview_2.umap` | 官方演示地图 | ✅ 先看它 |
| `M_Read_Me` | 素材说明材质 | ✅ 双击查看 |

风格列表：`Basic / Dark / Fire / Ice / Lightning / LightSaber / Matrix / Mystic / Nature / Sand / Scifi / water / Wind / Cyberpunk / Distortion / Music`。
首版建议先用 `Basic` 验证流程，再换喜欢的风格。

## 2. 当前工程已确认的事实

| 项目 | 当前情况 |
|---|---|
| 主角武器 | `BP_DKWeapon_Sword`，父类 `ADKWeapon → AFirstWeaponBase` |
| 武器结构 | 静态网格 `WeaponMesh`（根）+ 碰撞盒 `WeaponCollisionBox` |
| 攻击蒙太奇 | `/Game/DKCharacter/AnimBP/Sword/Montages/Attack/AM_DK_Sword_Attack_1~4`（以 `BP_DKWeapon_Sword → DKWeaponData → Light Attack Montages` 实际引用为准） |
| 现有通知族 | `ANS_ActionCancel`、`ANS_HitReactWindow`、`ANS_InvincibleWindow`、`FirstANS_WeaponCollision` |
| 取当前武器 | `UFirstCombatComponent::GetCharacterCurrentEquippedWeapon()` 直接返回 `AFirstWeaponBase*` |
| 蒙太奇骨架 | 主角 `SK_DKMannequin` / `SK_Mannequin_Skeleton`，插槽 `DefaultSlot` |
| Niagara 模块 | ❌ 尚未加入模块依赖，不加会编译报错（本册第一步修复） |

## 3. 改动总览

| 文件/资产 | 操作 | 内容 |
|---|---|---|
| `First.Build.cs` | 修改 | 增加 `Niagara` 模块依赖 |
| `FirstWeaponBase.h/.cpp` | 修改 | 拖影模板属性 + Niagara 组件 + `SetSlashTrailEnabled()` |
| `Notifies/ANS_SlashTrail.h/.cpp` | 新建 | 蒙太奇通知：进入开启拖影、离开关闭 |
| `BP_DKWeapon_Sword` | 配置 | 填 `SlashTrailTemplate`；可选调整拖影组件位置 |
| 四个攻击蒙太奇 | 配置 | 挥剑区间加一个 `Slash Trail` 通知 |

## 4. 第一步：添加 Niagara 模块依赖

文件：

```text
Source/First/First.Build.cs
```

在 `PublicDependencyModuleNames.AddRange(new string[] { ... })` 中增加：

```cpp
		"Niagara",
```

示例（增加后）：

```cpp
		PublicDependencyModuleNames.AddRange(new string[] { "Core",
			"CoreUObject",
			"Engine",
			"InputCore",
			"EnhancedInput",
			"GameplayTags",
			"GameplayAbilities",
			"GameplayTasks",
			"AIModule",
			"NavigationSystem",
			"UMG",
			"Niagara" });
```

`UNiagaraComponent` 与 `UNiagaraSystem` 都属于 `Niagara` 模块，不加会出现“无法打开 NiagaraComponent.h”之类的编译错误。

## 5. 第二步：FirstWeaponBase 增加拖影组件与开关

### 5.1 修改 `FirstWeaponBase.h`

文件：

```text
Source/First/Public/Items/Weapons/FirstWeaponBase.h
```

在前置声明区域增加：

```cpp
class UBoxComponent;
class UStaticMeshComponent;
class UNiagaraComponent;
class UNiagaraSystem;
```

在 protected 的 `WeaponCollisionBox` 之后增加：

```cpp
	// 刀光拖影的 Niagara 模板；在 BP_DKWeapon_Sword 中统一配置，换风格只改这里。
	UPROPERTY(EditDefaultsOnly, BlueprintReadOnly, Category="Weapon|VFX",
		meta=(AllowPrivateAccess="true"))
	TObjectPtr<UNiagaraSystem> SlashTrailTemplate;

	// 拖影组件：默认停用，由蒙太奇通知在挥剑期间开关。
	// 挂到 WeaponMesh，跟随刀身运动生成 Ribbon 拖影。
	UPROPERTY(VisibleAnywhere, BlueprintReadOnly, Category="Weapon|VFX",
		meta=(AllowPrivateAccess="true"))
	TObjectPtr<UNiagaraComponent> SlashTrailComponent;
```

在 public 区域增加：

```cpp
	// 作用：开关刀光拖影。启用时切换/激活 SlashTrailTemplate；停用时立即关闭。
	UFUNCTION(BlueprintCallable, Category="Weapon|VFX")
	void SetSlashTrailEnabled(bool bShouldEnable);
```

注意：`FirstWeaponBase.generated.h` 必须继续是最后一个 `#include`。

### 5.2 修改 `FirstWeaponBase.cpp`

文件：

```text
Source/First/Private/Items/Weapons/FirstWeaponBase.cpp
```

顶部 include 区域增加：

```cpp
#include "NiagaraComponent.h"
#include "NiagaraSystem.h"
```

构造函数中，`WeaponCollisionBox` 创建之后增加：

```cpp
	// 拖影初始为关闭；只有蒙太奇的 Slash Trail 通知按挥剑窗口开启。
	SlashTrailComponent = CreateDefaultSubobject<UNiagaraComponent>(TEXT("SlashTrailComponent"));
	SlashTrailComponent->SetupAttachment(WeaponMesh);
	SlashTrailComponent->SetAutoActivate(false);
	SlashTrailComponent->SetAutoDestroy(false);
```

文件末尾增加实现：

```cpp
void AFirstWeaponBase::SetSlashTrailEnabled(bool bShouldEnable)
{
	if (!SlashTrailComponent)
	{
		return;
	}

	if (bShouldEnable)
	{
		// 模板存在且与当前资源不同时才替换，避免每次挥剑重复 SetAsset。
		if (SlashTrailTemplate &&
			SlashTrailComponent->GetAsset() != SlashTrailTemplate)
		{
			SlashTrailComponent->SetAsset(SlashTrailTemplate);
		}

		SlashTrailComponent->Activate(true);
	}
	else
	{
		SlashTrailComponent->Deactivate();
	}
}
```

要点：

- 组件**必须** `SetupAttachment(WeaponMesh)`：拖影 Ribbon 是“组件位置随时间移动”生成的，挂 Actor 根上拿不到刀身弧线；
- `SetAutoActivate(false)` 防止剑背在背上时就持续喷特效；
- `bShouldEnable=false` 路径要保证拔刀/收刀、攻击被打断时拖影能关掉（见 11 节排查）。

### 编译闸门 1

关闭编辑器，完整编译 `FirstEditor Win64 Development`。此时武器上还看不到任何东西（模板未配置、通知未添加），行为应与之前完全一致。

## 6. 第三步：新建 ANS_SlashTrail 通知

### 6.1 创建类信息

在 IDE 中手动新建两个文件（不需要编辑器向导）：

```text
头文件：Source/First/Public/Notifies/ANS_SlashTrail.h
源文件：Source/First/Private/Notifies/ANS_SlashTrail.cpp
```

- 父类：`UAnimNotifyState`（“所有类”中搜索 `AnimNotifyState`）；
- 类名：`ANS_SlashTrail`，不要写 `U` 前缀；
- 编辑器显示名：`Slash Trail`。

### 6.2 `ANS_SlashTrail.h`

```cpp
#pragma once

#include "CoreMinimal.h"
#include "Animation/AnimNotifies/AnimNotifyState.h"
#include "ANS_SlashTrail.generated.h"

/**
 * 武器刀光拖影窗口。
 * 只能跟在攻击蒙太奇里：进入时开启当前装备武器的拖影，离开时关闭。
 * 本通知不参与战斗判定，与 FirstANS_WeaponCollision（伤害窗口）相互独立。
 */
UCLASS(meta = (DisplayName = "Slash Trail"))
class FIRST_API UANS_SlashTrail : public UAnimNotifyState
{
	GENERATED_BODY()

public:
	virtual void NotifyBegin(USkeletalMeshComponent* MeshComp, UAnimSequenceBase* Animation,
		float TotalDuration, const FAnimNotifyEventReference& EventReference) override;

	virtual void NotifyEnd(USkeletalMeshComponent* MeshComp, UAnimSequenceBase* Animation,
		const FAnimNotifyEventReference& EventReference) override;
};
```

### 6.3 `ANS_SlashTrail.cpp`

```cpp
#include "Notifies/ANS_SlashTrail.h"

#include "Character/BaseCharacter.h"
#include "Components/Combat/FirstCombatComponent.h"
#include "Items/Weapons/FirstWeaponBase.h"

void UANS_SlashTrail::NotifyBegin(USkeletalMeshComponent* MeshComp, UAnimSequenceBase* Animation,
	float TotalDuration, const FAnimNotifyEventReference& EventReference)
{
	Super::NotifyBegin(MeshComp, Animation, TotalDuration, EventReference);

	ABaseCharacter* Character = MeshComp
		? Cast<ABaseCharacter>(MeshComp->GetOwner())
		: nullptr;
	if (!Character)
	{
		return;
	}

	UFirstCombatComponent* Combat =
		Character->FindComponentByClass<UFirstCombatComponent>();
	if (!Combat)
	{
		return;
	}

	if (AFirstWeaponBase* Weapon = Combat->GetCharacterCurrentEquippedWeapon())
	{
		Weapon->SetSlashTrailEnabled(true);
	}
}

void UANS_SlashTrail::NotifyEnd(USkeletalMeshComponent* MeshComp, UAnimSequenceBase* Animation,
	const FAnimNotifyEventReference& EventReference)
{
	Super::NotifyEnd(MeshComp, Animation, EventReference);

	ABaseCharacter* Character = MeshComp
		? Cast<ABaseCharacter>(MeshComp->GetOwner())
		: nullptr;
	if (!Character)
	{
		return;
	}

	UFirstCombatComponent* Combat =
		Character->FindComponentByClass<UFirstCombatComponent>();
	if (!Combat)
	{
		return;
	}

	if (AFirstWeaponBase* Weapon = Combat->GetCharacterCurrentEquippedWeapon())
	{
		Weapon->SetSlashTrailEnabled(false);
	}
}
```

### 编译闸门 2

完整编译。此时通知类应该出现在蒙太奇编辑器的“添加通知”搜索列表里（显示名 `Slash Trail`）。

## 7. 第四步：BP_DKWeapon_Sword 配置

1. 打开 `BP_DKWeapon_Sword → 类默认值`；
2. 新增 `Weapon | VFX` 分类下的 **`Slash Trail Template`**：
   - 先选 `NS_SlashTrail_Basic`（验证流程）；
   - 确定风格后换成喜欢的，例如 `NS_SlashTrail_Dark`；
3. 组件面板选中 `SlashTrailComponent`，在细节面板调整 **位置/旋转**，让拖影起点贴合刀身：
   - 先不动（相对 0 0 0 从刀身根部出发），进 PIE 挥一刀看效果再微调；
   - 通常只需要调 `Relative Rotation`，让 Ribbon 平面与挥剑轨迹一致；
4. 可选「常驻光刃」：再添加一个 Niagara Component，Asset 选 `NS_Sword_Dark`，挂到 `WeaponMesh`，保持默认激活——持剑期间刀身常亮。

## 8. 第五步：攻击蒙太奇加通知

1. 打开 `BP_DKWeapon_Sword → 类默认值 → DKWeaponData → Light Attack Montages`，确认实际引用的 4 个蒙太奇（应当就是 `/Game/DKCharacter/AnimBP/Sword/Montages/Attack/AM_DK_Sword_Attack_1~4`）——不要凭文件名去改未被引用的副本；
2. 逐个打开攻击蒙太奇；
3. 在时间轴上找到挥剑有效区间（可以对照已有的 `Weapon Collision` 通知窗口），在该区间右键 `Add Notify...` → 搜索 **`Slash Trail`** → 创建；
4. 让 `Slash Trail` 窗口与碰撞窗口**对齐或稍宽**：
   - 对齐 = 拖影只在有效伤害帧出现；
   - 稍宽 = 拖影更飘逸（表现优先于判定，两者互不影响）；
5. 四个攻击蒙太奇都加好后保存。

## 9. 调参速查

| 想调整 | 去哪里 |
|---|---|
| 拖影颜色/亮度/风格 | `Material/SlashTrail/Instances/MI_SlashTrail_<风格>` 材质实例 |
| 拖影宽窄、弧度、流速 | `Niagara/<风格>/NS_SlashTrail_<风格>` 系统内的 Ribbon/渲染模块 |
| 拖影出现/消失淡入淡出 | 同上系统里的 `Camera Fade` / 材质参数 |
| 拖影与伤害窗口的时序 | 只改蒙太奇里的 `Slash Trail` 通知起止点 |
| 换整套风格 | 只改 `BP_DKWeapon_Sword → Slash Trail Template` |

## 10. 验收清单

1. 装备剑：挥出四段轻攻击，每段都有拖影；
2. 收招后拖影立即消失，不会残留；
3. 拔刀/收刀（未攻击）状态下没有拖影；
4. 攻击被闪避、格挡或受击打断时，拖影随蒙太奇中断关闭；
5. 拖影位置贴合刀身，方向与挥剑弧线一致；
6. 未装备剑时不攻击（现有逻辑阻止），不存在无剑拖影；
7. 战斗帧率无明显波动（Niagara 预览视口关闭后对比）。

## 11. 常见问题与排查

**11.1 游戏里完全没有拖影**
按顺序检查：

1. 通知加的是不是武器真正引用的那 4 个蒙太奇（14.4 节说过的陷阱：项目里有多个副本）；
2. `BP_DKWeapon_Sword` 的 `Slash Trail Template` 是否已配置；
3. 武器是否真的处于“装备”状态（`GetCharacterCurrentEquippedWeapon()` 返回空则通知找不到武器）；
4. 拖影是 Ribbon，武器必须**快速运动**才会显示——静止挥刀看不出效果。

**11.2 拖影在预览（演示地图）正常，自己的武器上位置不对**
调整 `SlashTrailComponent` 的 `Relative Rotation/Location`。先确认组件确实挂在了 `WeaponMesh` 上，而不是 Actor 根。

**11.3 拖影一帧就消失 / 拖尾特别短**
检查 `NS_SlashTrail_<风格>` 系统内 Ribbon 粒子生存时间（Lifetime），以及通知窗口是否太窄（先与碰撞窗口完全对齐排除窗口因素）。

**11.4 游戏里被截断（镜头一转就消失）**
Niagara Ribbon 的自动边界估算不稳定时会被视锥剔除：打开系统 → `Overview` → `Local Space/Camera Bounds` 相关设置，手动扩大边界，或把 `Sim Target` 相关剔除参数调松。

**11.5 攻击被打断后拖影残留**
正常情况下蒙太奇被打断时 NotifyState 的 `NotifyEnd` 仍会回调并关闭拖影。若发现残留：在 `SetSlashTrailEnabled(false)` 里断点/日志确认是否被调用；仍不稳定时，可选的兜底是让 `UFirstCombatComponent::ToggleWeaponCollision(false)` 同帧调用 `Weapon->SetSlashTrailEnabled(false)`（该方案会动到战斗组件，属后续优化，首版先验证通知方案）。

## 12. 本册刻意不做

- **命中火花 `NS_Hit_<风格>`**：后续可接在你现有的 `DK.Event.MeleeHit` 事件链上，随命中帧在目标位置生成；
- **射弹刀光 `BP_Slash_Projectile_*`**：留给将来的远程招式；
- **BOSS 武器拖影**：`ANS_SlashTrail` 是通用通知，之后给 `BP_BossWeapon` 加同样的拖影组件即可复用；
- **拖影速度缩放、残影定格**等高级表现：需要修改 Niagara 系统内部逻辑，等基础版稳定后再做。

## 13. 推荐实施顺序与编译闸门

```text
阶段 1：First.Build.cs 加 Niagara 依赖
阶段 2：FirstWeaponBase 拖影组件 + SetSlashTrailEnabled   → 完整编译（闸门 1）
阶段 3：ANS_SlashTrail 通知                              → 完整编译（闸门 2）
阶段 4：BP_DKWeapon_Sword 填模板 + 调组件位置
阶段 5：四个攻击蒙太奇加 Slash Trail 通知
阶段 6：按第 10 节验收
```

> 新增了 UCLASS 与 UPROPERTY（Niagara 组件属于反射类型），阶段 2/3 完成后请关闭编辑器完整编译，不要只依赖 Live Coding。

---

## 14. 最终结构检查

```text
攻击蒙太奇（挥剑区间）
├─ Weapon Collision（FirstANS_WeaponCollision）  ← 伤害判定窗口（已有）
└─ Slash Trail（ANS_SlashTrail）                 ← 拖影表现窗口（本册新增，可独立调宽窄）

武器 Actor（AFirstWeaponBase / BP_DKWeapon_Sword）
├─ WeaponMesh（静态网格，根）
├─ WeaponCollisionBox（Overlap 命中盒）
└─ SlashTrailComponent（Niagara，默认停用，Attach 到 WeaponMesh）
```

两层窗口相互独立：先有伤害判定，再有表现；以后把拖影拉宽或换成光刃环都不会影响战斗数值。
