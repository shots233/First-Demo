# 主角索敌（锁定、切换与解除）实现指导

> 适用项目：`First`（UE 5.6 / C++ / GAS / Enhanced Input / 中文版编辑器）
>
> 项目核对日期：2026-08-25。本文按当前 `D:\UE2026\First` 的真实源码与资产结构编写。
>
> 前置条件：主角移动、相机、Enhanced Input、BOSS 与 `Shared.Status.Dead` 已能正常工作。
>
> 范围说明：本册按当前项目的单机、本地玩家模式实现；多人复制与分屏不在本册范围内。
>
> 本文只提供实现指导，不会自动修改 C++、蓝图、输入资产或动画资产。
>
> 本册与 BOSS 受击逻辑无关：当前 `GA_Boss_HitReact` 的激活条件保持不变。

---

## 0. 先看结论（本册完成后得到什么）

完成本册后，主角将拥有一套基础的类魂锁敌系统：

```text
按下鼠标中键
→ 在主角附近查找可锁定敌人
→ 排除死亡、超距、屏幕外和被墙遮挡的目标
→ 优先选择最接近屏幕中心的敌人
→ 显示锁定标记
→ 镜头平滑朝向目标
→ 主角平滑转向锁定方向，W/S 靠近或远离，A/D 围绕目标移动

锁定期间滚动鼠标滚轮
→ 向屏幕左侧或右侧切换目标

再次按下鼠标中键
→ 手动解除锁定

目标死亡、销毁、距离过远或被遮挡超过 1 秒
→ 自动解除锁定
→ 隐藏标记
→ 恢复普通移动转向和自由镜头
```

本册采用独立组件：

```text
UDKTargetLockComponent
├─ 持有 CurrentTarget
├─ 搜集与筛选候选
├─ 初始目标评分
├─ 左右切换
├─ 镜头跟随
├─ 锁定标记
└─ 所有解除路径的统一清理

ADKCharacter
├─ 创建组件
├─ 把输入转发给组件
├─ 锁定时阻止自由 Look
└─ 为八向闪避提供精确的目标方向

AEnemyCharacter
└─ 提供每个敌人可单独调整的 TargetLockPoint
```

锁敌不是攻击判定。玩家的剑仍然必须真实碰到敌人，才会由现有武器 Overlap 和 GAS 伤害流程结算。

---

## 1. 当前项目实际状态

### 1.1 已经可以直接复用的内容

| 现有内容 | 当前状态 | 本册如何复用 |
|---|---|---|
| `ADKCharacter::Input_Move` | 按 `Controller Yaw` 计算移动方向 | 镜头朝向目标后，W/S/A/D 会自然变成目标相对移动 |
| `CameraBoom` | 使用控制器旋转，带相机碰墙 | 只更新 `ControlRotation`，无需重建相机 |
| `CharacterMovement` | 普通状态朝移动方向旋转 | 锁定时暂时切换为朝控制器期望方向旋转 |
| `DA_InputConfig` | 按 GameplayTag 查找原生输入 | 锁敌和切换都加入 `NativeInputActions` |
| `UDKInputComponent` | 已支持标签式原生输入绑定 | 不需要修改 |
| `AEnemyCharacter` | 当前为空的敌人语义基类 | 作为“可锁定敌人”的首版类型边界 |
| `ABossCharacter` | 继承 `AEnemyCharacter` | 自动成为可锁定目标 |
| `Shared.Status.Dead` | 血量归零时加入 ASC | 用来排除尸体并自动断锁 |
| `UMG` | 模块依赖和 HUD 已存在 | 直接创建独立锁定标记 |
| 八向闪避 | 已有方向量化 | 锁定时改用“主角到目标”的 Yaw |

### 1.2 当前没有的内容

项目目前没有：

- 主角锁敌组件；
- 锁敌输入 Tag；
- `IA_TargetLock` / `IA_SwitchTarget`；
- 专用 Target 碰撞通道；
- 阵营或 `GenericTeamId`；
- 锁定标记控件；
- 锁定移动专用 2D Blend Space。

本册首版不创建自定义碰撞通道，也不引入阵营系统。候选查询使用：

```text
对象类型：ECC_Pawn
类型过滤：AEnemyCharacter
遮挡检测：ECC_Visibility
死亡过滤：Shared.Status.Dead
```

### 1.3 当前动画限制（必须先知道）

当前：

- `BS_DK_Locomotion` 是 1D Blend Space；
- `BS_DK_Sword_Locomotion` 也是 1D Blend Space；
- `UFirstDKAnimInstance` 只有 `GroundSpeed`，没有移动方向 `Direction`。

所以锁定后虽然角色能按目标相对方向侧移、后退，并以锁定方向作为转向基准，但动画仍可能播放前进动画，视觉上表现为横滑或倒着滑。

这不是索敌代码错误，而是当前移动动画资产不包含锁定侧走/后退表现。本册先验收“选择、镜头、朝向、切换、解除”功能；2D 锁定移动动画放到后续扩展。

> 当前 `CameraBoom` 使用了 `SocketOffset.Y = 50` 的肩位镜头。本册根据“相机位置到目标”计算 `ControlRotation`，所以近距离锁定时，角色正前方与“主角原点到目标”的方向可能存在轻微夹角。这不影响移动与锁定；闪避方向会另外使用精确的“主角到目标” Yaw。以后若要求角色始终像尺子一样正对目标，可在锁定时把相机横向肩偏移平滑归零，或把角色 Yaw 与镜头 Yaw 分开控制。

---

## 2. 设计边界与职责

### 2.1 为什么不用 BOSS 的 AIPerception 和黑板

BOSS 的索敌是 AI 决策：

```text
AIPerception → Blackboard TargetActor → Behavior Tree
```

主角锁敌是本地玩家操控模式：

```text
玩家输入 → TargetLockComponent → 相机/移动/UI
```

二者生命周期和使用者不同。不要把玩家当前锁定目标写进 `BB_Boss`，也不要让主角读取 BOSS AIController 的 `TargetActor`。

### 2.2 为什么首版不用 Gameplay Ability

锁敌本身：

- 没有消耗；
- 没有冷却；
- 需要持续维护镜头；
- 需要与输入、UI、移动模式长期协作。

`UGameplayAbility` 本身没有普通 Actor 那样的 Tick。为了一个持续镜头模式再增加长期 AbilityTask，会让初学阶段的生命周期更复杂。

因此本册选择：

```text
核心状态：UDKTargetLockComponent
跨系统可读状态：DK.Status.TargetLocked
```

GAS Tag 只负责告诉其它系统“当前处于锁定模式”，不负责保存目标指针。

### 2.3 为什么使用独立组件

`ABaseCharacter` 已彻底关闭 Actor Tick。若把锁敌维护全部写进 `ADKCharacter::Tick`，就必须重新开启主角 Actor Tick。

独立组件可以：

- 只在锁定成功后开启组件 Tick；
- 解锁后立即关闭；
- 不影响角色其它功能；
- 将候选、镜头和 UI 从 `ADKCharacter` 中拆开；
- 以后可以单独替换评分或切换算法。

### 2.4 为什么首版直接使用 AEnemyCharacter

当前所有真正敌人都应继承 `AEnemyCharacter`，而 `ABossCharacter` 已经这样做。

首版直接按这个基类过滤，比立刻增加 Targetable Interface 更少文件，也不会依赖具体的 `ABossCharacter`。以后如果宝箱、训练假人或可破坏物也需要锁定，再把资格判断升级为接口。

---

## 3. 默认规则与推荐数值

| 参数 | 推荐值 | 含义 |
|---|---:|---|
| AcquireRadius | 2000 | 按下锁敌键时的搜索半径 |
| BreakDistance | 2600 | 已锁定后超过这个距离才断锁 |
| MaxAcquireAngleDegrees | 65° | 镜头前方左右各 65°可进入初始候选 |
| MaxHeightDifference | 600 | 过滤楼层差过大的目标 |
| OcclusionGraceTime | 1.0 秒 | 短暂经过柱子不立刻断锁 |
| CameraRotationInterpSpeed | 8.0 | 镜头跟随速度 |
| MinCameraPitch | -35° | 镜头最低俯仰 |
| MaxCameraPitch | 25° | 镜头最高仰角 |
| SwitchCooldown | 0.20 秒 | 限制连续滚轮输入的切换频率 |
| SwitchScreenDeadZone | 20 像素 | 切换时忽略几乎重叠的目标 |
| TargetIndicatorSize | 40 × 40 | 锁定标记大小 |

`BreakDistance` 必须大于 `AcquireRadius`。这是距离迟滞：

```text
2000 内才能首次锁定
锁定后允许目标移动到 2600 才解除
```

如果两者相同，目标在边界附近移动时会频繁锁定/断锁。

---

## 4. 涉及文件与资源

### 4.1 新增 C++

| 文件 | 职责 |
|---|---|
| `Source/First/Public/Components/Targeting/DKTargetLockComponent.h` | 组件接口、参数与状态 |
| `Source/First/Private/Components/Targeting/DKTargetLockComponent.cpp` | 查询、评分、切换、镜头、UI 和清理 |

### 4.2 修改 C++

| 文件 | 修改内容 |
|---|---|
| `MyGameplayTags.h/.cpp` | 新增两个输入 Tag 和一个锁定状态 Tag |
| `EnemyCharacter.h/.cpp` | 新增可在每个敌人蓝图中调整的 `TargetLockPoint` |
| `DKCharacter.h/.cpp` | 创建组件、绑定输入、锁定时拦截 Look、闪避方向联动 |

### 4.3 新增 UE 资产

| 建议路径 | 资产 |
|---|---|
| `/Game/DKCharacter/Input/Action/IA_TargetLock` | Boolean Input Action |
| `/Game/DKCharacter/Input/Action/IA_SwitchTarget` | Axis1D Input Action |
| `/Game/UI/WBP_TargetLockIndicator` | 锁定目标标记 |

### 4.4 修改 UE 资产

| 资产 | 修改内容 |
|---|---|
| `/Game/DKCharacter/Input/IMC_Default` | 添加鼠标中键和鼠标滚轮映射 |
| `/Game/DKCharacter/DA_InputConfig` | 把两个新输入加入 `NativeInputActions` |
| `/Game/DKCharacter/BP_DKCharacter` | 给组件配置标记控件与数值 |
| `/Game/Enemy/BP_Boss` | 把 `TargetLockPoint` 调到胸口附近 |

### 4.5 不需要修改

- `First.Build.cs`：已经包含 `EnhancedInput`、`GameplayTags` 和 `UMG`；
- BOSS 行为树与黑板；
- BOSS AIPerception；
- 攻击 Ability；
- 武器碰撞；
- `DefaultEngine.ini`；
- 当前 BOSS 受击能力。

---

## 5. 第一步：给敌人增加锁定点

直接使用 `GetActorLocation()` 容易让镜头和标记落在胶囊中心或模型腰部。不同体型敌人的胸口高度也不同，因此把锁定点放到敌人基类。

### 5.1 修改 EnemyCharacter.h

文件：

`Source/First/Public/Character/EnemyCharacter.h`

在 `#include "EnemyCharacter.generated.h"` 下方增加前置声明：

```cpp
class USceneComponent;
```

把类体改为：

```cpp
UCLASS()
class FIRST_API AEnemyCharacter : public ABaseCharacter
{
	GENERATED_BODY()

public:
	AEnemyCharacter();

	// 锁敌镜头、遮挡检测和屏幕标记共用这个世界坐标。
	UFUNCTION(BlueprintPure, Category="Target Lock")
	FVector GetTargetLockLocation() const;

private:
	// 可在 BP_Boss 的组件视口中移动到胸口、头部或弱点位置。
	UPROPERTY(
		VisibleAnywhere,
		BlueprintReadOnly,
		Category="Target Lock",
		meta=(AllowPrivateAccess="true"))
	TObjectPtr<USceneComponent> TargetLockPoint;
};
```

### 5.2 修改 EnemyCharacter.cpp

文件：

`Source/First/Private/Character/EnemyCharacter.cpp`

完整内容：

```cpp
#include "Character/EnemyCharacter.h"

#include "Components/SceneComponent.h"

AEnemyCharacter::AEnemyCharacter()
{
	TargetLockPoint =
		CreateDefaultSubobject<USceneComponent>(TEXT("TargetLockPoint"));
	TargetLockPoint->SetupAttachment(GetRootComponent());

	// ACharacter 的 ActorLocation 已在胶囊中心附近；
	// 再向上 40，首版通常会落在上半身。
	TargetLockPoint->SetRelativeLocation(FVector(0.f, 0.f, 40.f));
}

FVector AEnemyCharacter::GetTargetLockLocation() const
{
	return TargetLockPoint
		? TargetLockPoint->GetComponentLocation()
		: GetActorLocation() + FVector(0.f, 0.f, 40.f);
}
```

这里只提供坐标，不处理锁敌输入、镜头或 UI。敌人基类仍然不知道“是谁锁定了自己”。

---

## 6. 第二步：新增 GameplayTag

### 6.1 修改 MyGameplayTags.h

文件：

`Source/First/Public/MyGameplayTags.h`

在原生输入 Tag 区域，放在 `InputTag_ToggleSword` 附近：

```cpp
// 玩家锁定/解除目标：角色层原生输入，不直接激活 Ability。
FIRST_API UE_DECLARE_GAMEPLAY_TAG_EXTERN(InputTag_TargetLock);

// 锁定期间向左/右切换目标：Axis1D 原生输入。
FIRST_API UE_DECLARE_GAMEPLAY_TAG_EXTERN(InputTag_SwitchTarget);
```

在 DK 状态 Tag 区域增加：

```cpp
// 当前处于锁定目标模式；目标指针仍由 TargetLockComponent 持有。
FIRST_API UE_DECLARE_GAMEPLAY_TAG_EXTERN(DK_Status_TargetLocked);
```

### 6.2 修改 MyGameplayTags.cpp

文件：

`Source/First/Private/MyGameplayTags.cpp`

输入 Tag 定义区域增加：

```cpp
UE_DEFINE_GAMEPLAY_TAG(InputTag_TargetLock, "InputTag.TargetLock");
UE_DEFINE_GAMEPLAY_TAG(InputTag_SwitchTarget, "InputTag.SwitchTarget");
```

DK 状态 Tag 定义区域增加：

```cpp
UE_DEFINE_GAMEPLAY_TAG(
	DK_Status_TargetLocked,
	"DK.Status.TargetLocked");
```

锁敌输入属于 `NativeInputActions`，不要放进 `AbilityInputActions`。

---

## 7. 第三步：创建 UDKTargetLockComponent

### 7.1 用 UE 向导创建类

1. UE 顶部菜单：**工具（Tools）→ 新建 C++ 类（New C++ Class）**；
2. 选择 **Actor Component**；
3. 类名填写 `DKTargetLockComponent`；
4. 选择模块 `First`；
5. 创建后，把文件整理到：

```text
Public/Components/Targeting/DKTargetLockComponent.h
Private/Components/Targeting/DKTargetLockComponent.cpp
```

类名不要手动填写 `U` 前缀。UE 会生成：

```text
UDKTargetLockComponent
```

### 7.2 头文件

文件：

`Source/First/Public/Components/Targeting/DKTargetLockComponent.h`

完整内容：

```cpp
#pragma once

#include "CoreMinimal.h"
#include "Components/ActorComponent.h"
#include "DKTargetLockComponent.generated.h"

class ABaseCharacter;
class ADKCharacter;
class AEnemyCharacter;
class UUserWidget;

DECLARE_DYNAMIC_MULTICAST_DELEGATE_OneParam(
	FOnTargetLockChanged,
	AEnemyCharacter*,
	NewTarget);

UCLASS(ClassGroup=(Custom), meta=(BlueprintSpawnableComponent))
class FIRST_API UDKTargetLockComponent : public UActorComponent
{
	GENERATED_BODY()

public:
	UDKTargetLockComponent();

	virtual void TickComponent(
		float DeltaTime,
		ELevelTick TickType,
		FActorComponentTickFunction* ThisTickFunction) override;

	// 未锁定：寻找最佳目标；已锁定：手动解除。
	UFUNCTION(BlueprintCallable, Category="Target Lock")
	void ToggleTargetLock();

	// Direction > 0 向屏幕右侧切换；Direction < 0 向左侧切换。
	UFUNCTION(BlueprintCallable, Category="Target Lock")
	void SwitchTarget(float Direction);

	UFUNCTION(BlueprintCallable, Category="Target Lock")
	void ClearTargetLock();

	UFUNCTION(BlueprintPure, Category="Target Lock")
	bool IsTargetLocked() const
	{
		return bLockModeActive && CurrentTarget.IsValid();
	}

	UFUNCTION(BlueprintPure, Category="Target Lock")
	AEnemyCharacter* GetCurrentTarget() const
	{
		return CurrentTarget.Get();
	}

	UFUNCTION(BlueprintPure, Category="Target Lock")
	FVector GetCurrentTargetLocation() const;

	// UI、攻击辅助或其它系统以后都可以监听，不需要反向依赖组件实现。
	UPROPERTY(BlueprintAssignable, Category="Target Lock")
	FOnTargetLockChanged OnTargetLockChanged;

protected:
	virtual void BeginPlay() override;
	virtual void EndPlay(
		const EEndPlayReason::Type EndPlayReason) override;

private:
	TArray<AEnemyCharacter*> GatherCandidates(
		bool bRequireAcquireCone) const;

	bool IsCandidateValid(
		AEnemyCharacter* Candidate,
		bool bRequireAcquireCone) const;

	bool IsCharacterDead(const ABaseCharacter* Character) const;
	bool HasLineOfSightTo(AEnemyCharacter* Candidate) const;

	bool ProjectTargetToScreen(
		AEnemyCharacter* Candidate,
		FVector2D& OutScreenPosition) const;

	float CalculateAcquireScore(
		AEnemyCharacter* Candidate) const;

	AEnemyCharacter* FindBestTarget() const;

	void SetCurrentTarget(AEnemyCharacter* NewTarget);
	void UpdateLockedTarget(float DeltaTime);
	void UpdateControlRotation(float DeltaTime);

	void ApplyLockedMovementMode();
	void RestoreMovementMode();

	void ShowTargetIndicator();
	void HideTargetIndicator();
	void UpdateTargetIndicator();

	void ClearTargetLockInternal(const TCHAR* Reason);

	// —— 可在 BP_DKCharacter 的组件详情里调整 ——

	UPROPERTY(
		EditAnywhere,
		BlueprintReadOnly,
		Category="Target Lock|Detection",
		meta=(AllowPrivateAccess="true", ClampMin="100.0"))
	float AcquireRadius = 2000.f;

	UPROPERTY(
		EditAnywhere,
		BlueprintReadOnly,
		Category="Target Lock|Detection",
		meta=(AllowPrivateAccess="true", ClampMin="100.0"))
	float BreakDistance = 2600.f;

	UPROPERTY(
		EditAnywhere,
		BlueprintReadOnly,
		Category="Target Lock|Detection",
		meta=(AllowPrivateAccess="true", ClampMin="0.0", ClampMax="180.0"))
	float MaxAcquireAngleDegrees = 65.f;

	UPROPERTY(
		EditAnywhere,
		BlueprintReadOnly,
		Category="Target Lock|Detection",
		meta=(AllowPrivateAccess="true", ClampMin="0.0"))
	float MaxHeightDifference = 600.f;

	UPROPERTY(
		EditAnywhere,
		BlueprintReadOnly,
		Category="Target Lock|Detection",
		meta=(AllowPrivateAccess="true", ClampMin="0.0"))
	float OcclusionGraceTime = 1.f;

	UPROPERTY(
		EditAnywhere,
		BlueprintReadOnly,
		Category="Target Lock|Camera",
		meta=(AllowPrivateAccess="true", ClampMin="0.0"))
	float CameraRotationInterpSpeed = 8.f;

	UPROPERTY(
		EditAnywhere,
		BlueprintReadOnly,
		Category="Target Lock|Camera",
		meta=(AllowPrivateAccess="true"))
	float MinCameraPitch = -35.f;

	UPROPERTY(
		EditAnywhere,
		BlueprintReadOnly,
		Category="Target Lock|Camera",
		meta=(AllowPrivateAccess="true"))
	float MaxCameraPitch = 25.f;

	UPROPERTY(
		EditAnywhere,
		BlueprintReadOnly,
		Category="Target Lock|Switch",
		meta=(AllowPrivateAccess="true", ClampMin="0.0"))
	float SwitchCooldown = 0.2f;

	UPROPERTY(
		EditAnywhere,
		BlueprintReadOnly,
		Category="Target Lock|Switch",
		meta=(AllowPrivateAccess="true", ClampMin="0.0"))
	float SwitchScreenDeadZone = 20.f;

	UPROPERTY(
		EditAnywhere,
		BlueprintReadOnly,
		Category="Target Lock|UI",
		meta=(AllowPrivateAccess="true"))
	TSubclassOf<UUserWidget> TargetIndicatorWidgetClass;

	UPROPERTY(
		EditAnywhere,
		BlueprintReadOnly,
		Category="Target Lock|UI",
		meta=(AllowPrivateAccess="true"))
	FVector2D TargetIndicatorSize = FVector2D(40.f, 40.f);

	// —— 运行时状态 ——

	UPROPERTY()
	TObjectPtr<ADKCharacter> OwnerCharacter;

	UPROPERTY()
	TWeakObjectPtr<AEnemyCharacter> CurrentTarget;

	UPROPERTY()
	TObjectPtr<UUserWidget> TargetIndicatorWidget;

	float OccludedElapsedTime = 0.f;
	float NextAllowedSwitchTime = 0.f;

	bool bLockModeActive = false;
	bool bAddedTargetLockTag = false;

	bool bMovementSettingsCached = false;
	bool bCachedOrientRotationToMovement = true;
	bool bCachedUseControllerDesiredRotation = false;
};
```

`CurrentTarget` 使用 `TWeakObjectPtr`。目标被销毁后它会自动失效，不会留下普通裸指针悬空。

### 7.3 源文件

文件：

`Source/First/Private/Components/Targeting/DKTargetLockComponent.cpp`

完整内容：

```cpp
#include "Components/Targeting/DKTargetLockComponent.h"

#include "AbilitySystem/FirstAbilitySystemComponent.h"
#include "Blueprint/UserWidget.h"
#include "Blueprint/WidgetLayoutLibrary.h"
#include "Character/BaseCharacter.h"
#include "Character/DKCharacter.h"
#include "Character/EnemyCharacter.h"
#include "CollisionQueryParams.h"
#include "Engine/EngineTypes.h"
#include "Engine/World.h"
#include "GameFramework/CharacterMovementComponent.h"
#include "GameFramework/PlayerController.h"
#include "Kismet/KismetSystemLibrary.h"
#include "MyGameplayTags.h"

UDKTargetLockComponent::UDKTargetLockComponent()
{
	// 组件拥有自己的 Tick，不需要重新开启 ADKCharacter 的 Actor Tick。
	PrimaryComponentTick.bCanEverTick = true;
	PrimaryComponentTick.bStartWithTickEnabled = false;
}

void UDKTargetLockComponent::BeginPlay()
{
	Super::BeginPlay();

	OwnerCharacter = Cast<ADKCharacter>(GetOwner());
	SetComponentTickEnabled(false);

	if (!OwnerCharacter)
	{
		UE_LOG(
			LogTemp,
			Error,
			TEXT("[TargetLock] 组件 Owner 不是 ADKCharacter"));
	}
}

void UDKTargetLockComponent::EndPlay(
	const EEndPlayReason::Type EndPlayReason)
{
	ClearTargetLockInternal(TEXT("EndPlay"));

	if (TargetIndicatorWidget)
	{
		TargetIndicatorWidget->RemoveFromParent();
		TargetIndicatorWidget = nullptr;
	}

	Super::EndPlay(EndPlayReason);
}

void UDKTargetLockComponent::TickComponent(
	float DeltaTime,
	ELevelTick TickType,
	FActorComponentTickFunction* ThisTickFunction)
{
	Super::TickComponent(
		DeltaTime,
		TickType,
		ThisTickFunction);

	UpdateLockedTarget(DeltaTime);
}

void UDKTargetLockComponent::ToggleTargetLock()
{
	if (bLockModeActive)
	{
		ClearTargetLockInternal(TEXT("Manual"));
		return;
	}

	if (!OwnerCharacter ||
		IsCharacterDead(OwnerCharacter))
	{
		return;
	}

	AEnemyCharacter* BestTarget = FindBestTarget();
	if (!BestTarget)
	{
		UE_LOG(
			LogTemp,
			Log,
			TEXT("[TargetLock] Acquire failed: no valid target"));
		return;
	}

	SetCurrentTarget(BestTarget);
}

TArray<AEnemyCharacter*>
UDKTargetLockComponent::GatherCandidates(
	bool bRequireAcquireCone) const
{
	TArray<AEnemyCharacter*> Result;

	if (!OwnerCharacter || !GetWorld())
	{
		return Result;
	}

	// 先按 Pawn 对象类型做球形查询，再用 AEnemyCharacter 做语义过滤。
	TArray<TEnumAsByte<EObjectTypeQuery>> ObjectTypes;
	ObjectTypes.Add(
		UEngineTypes::ConvertToObjectType(ECC_Pawn));

	TArray<AActor*> ActorsToIgnore;
	ActorsToIgnore.Add(OwnerCharacter);

	TArray<AActor*> OverlappedActors;
	UKismetSystemLibrary::SphereOverlapActors(
		this,
		OwnerCharacter->GetActorLocation(),
		AcquireRadius,
		ObjectTypes,
		AEnemyCharacter::StaticClass(),
		ActorsToIgnore,
		OverlappedActors);

	for (AActor* Actor : OverlappedActors)
	{
		AEnemyCharacter* Candidate =
			Cast<AEnemyCharacter>(Actor);

		if (IsCandidateValid(
			Candidate,
			bRequireAcquireCone))
		{
			Result.AddUnique(Candidate);
		}
	}

	return Result;
}

bool UDKTargetLockComponent::IsCandidateValid(
	AEnemyCharacter* Candidate,
	bool bRequireAcquireCone) const
{
	if (!OwnerCharacter ||
		!IsValid(Candidate) ||
		IsCharacterDead(Candidate))
	{
		return false;
	}

	const FVector OwnerLocation =
		OwnerCharacter->GetActorLocation();
	const FVector TargetLocation =
		Candidate->GetTargetLockLocation();
	const FVector OwnerToTarget =
		TargetLocation - OwnerLocation;

	if (OwnerToTarget.SizeSquared() >
		FMath::Square(AcquireRadius))
	{
		return false;
	}

	if (FMath::Abs(
		Candidate->GetActorLocation().Z -
		OwnerLocation.Z) > MaxHeightDifference)
	{
		return false;
	}

	APlayerController* PlayerController =
		Cast<APlayerController>(
			OwnerCharacter->GetController());
	if (!PlayerController)
	{
		return false;
	}

	if (bRequireAcquireCone)
	{
		FVector ViewLocation;
		FRotator ViewRotation;
		PlayerController->GetPlayerViewPoint(
			ViewLocation,
			ViewRotation);

		FVector FlatViewForward = ViewRotation.Vector();
		FlatViewForward.Z = 0.f;
		FlatViewForward.Normalize();

		FVector FlatViewToTarget =
			TargetLocation - ViewLocation;
		FlatViewToTarget.Z = 0.f;
		FlatViewToTarget.Normalize();

		const float MinimumDot =
			FMath::Cos(
				FMath::DegreesToRadians(
					MaxAcquireAngleDegrees));

		if (FVector::DotProduct(
			FlatViewForward,
			FlatViewToTarget) < MinimumDot)
		{
			return false;
		}
	}

	return HasLineOfSightTo(Candidate);
}

bool UDKTargetLockComponent::IsCharacterDead(
	const ABaseCharacter* Character) const
{
	if (!Character)
	{
		return true;
	}

	const UFirstAbilitySystemComponent* ASC =
		Character->GetFirstAbilitySystemComponent();

	return ASC &&
		ASC->HasMatchingGameplayTag(
			MyGameplayTags::Shared_Status_Dead);
}

bool UDKTargetLockComponent::HasLineOfSightTo(
	AEnemyCharacter* Candidate) const
{
	if (!OwnerCharacter ||
		!Candidate ||
		!GetWorld())
	{
		return false;
	}

	APlayerController* PlayerController =
		Cast<APlayerController>(
			OwnerCharacter->GetController());
	if (!PlayerController)
	{
		return false;
	}

	FVector ViewLocation;
	FRotator ViewRotation;
	PlayerController->GetPlayerViewPoint(
		ViewLocation,
		ViewRotation);

	FHitResult HitResult;
	FCollisionQueryParams TraceParams(
		SCENE_QUERY_STAT(TargetLockVisibility),
		false,
		OwnerCharacter);
	TraceParams.AddIgnoredActor(OwnerCharacter);

	const bool bHitSomething =
		GetWorld()->LineTraceSingleByChannel(
			HitResult,
			ViewLocation,
			Candidate->GetTargetLockLocation(),
			ECC_Visibility,
			TraceParams);

	if (!bHitSomething)
	{
		return true;
	}

	AActor* HitActor = HitResult.GetActor();
	return HitActor == Candidate ||
		(HitActor && HitActor->IsOwnedBy(Candidate));
}

bool UDKTargetLockComponent::ProjectTargetToScreen(
	AEnemyCharacter* Candidate,
	FVector2D& OutScreenPosition) const
{
	if (!OwnerCharacter || !Candidate)
	{
		return false;
	}

	APlayerController* PlayerController =
		Cast<APlayerController>(
			OwnerCharacter->GetController());
	if (!PlayerController)
	{
		return false;
	}

	const bool bProjected =
		PlayerController->ProjectWorldLocationToScreen(
			Candidate->GetTargetLockLocation(),
			OutScreenPosition,
			false);

	int32 ViewportX = 0;
	int32 ViewportY = 0;
	PlayerController->GetViewportSize(
		ViewportX,
		ViewportY);

	return bProjected &&
		ViewportX > 0 &&
		ViewportY > 0 &&
		OutScreenPosition.X >= 0.f &&
		OutScreenPosition.X <= ViewportX &&
		OutScreenPosition.Y >= 0.f &&
		OutScreenPosition.Y <= ViewportY;
}

float UDKTargetLockComponent::CalculateAcquireScore(
	AEnemyCharacter* Candidate) const
{
	FVector2D ScreenPosition;
	if (!ProjectTargetToScreen(
		Candidate,
		ScreenPosition))
	{
		return MAX_flt;
	}

	APlayerController* PlayerController =
		Cast<APlayerController>(
			OwnerCharacter->GetController());

	int32 ViewportX = 0;
	int32 ViewportY = 0;
	PlayerController->GetViewportSize(
		ViewportX,
		ViewportY);

	const FVector2D ViewportCenter(
		ViewportX * 0.5f,
		ViewportY * 0.5f);

	const float HalfDiagonal =
		FMath::Max(
			ViewportCenter.Size(),
			1.f);

	const float ScreenCenterScore =
		(ScreenPosition - ViewportCenter).Size() /
		HalfDiagonal;

	const float DistanceScore =
		FVector::Dist(
			OwnerCharacter->GetActorLocation(),
			Candidate->GetActorLocation()) /
		FMath::Max(AcquireRadius, 1.f);

	// 屏幕中心优先，其次才是距离。
	return ScreenCenterScore * 0.75f +
		DistanceScore * 0.25f;
}

AEnemyCharacter*
UDKTargetLockComponent::FindBestTarget() const
{
	const TArray<AEnemyCharacter*> Candidates =
		GatherCandidates(true);

	AEnemyCharacter* BestTarget = nullptr;
	float BestScore = MAX_flt;

	for (AEnemyCharacter* Candidate : Candidates)
	{
		const float Score =
			CalculateAcquireScore(Candidate);

		if (Score < BestScore)
		{
			BestScore = Score;
			BestTarget = Candidate;
		}
	}

	UE_LOG(
		LogTemp,
		Log,
		TEXT(
			"[TargetLock] Acquire Candidates=%d Chosen=%s Score=%.3f"),
		Candidates.Num(),
		*GetNameSafe(BestTarget),
		BestScore);

	return BestTarget;
}

void UDKTargetLockComponent::SetCurrentTarget(
	AEnemyCharacter* NewTarget)
{
	if (!OwnerCharacter || !IsValid(NewTarget))
	{
		return;
	}

	const bool bWasAlreadyLocked = bLockModeActive;

	CurrentTarget = NewTarget;
	OccludedElapsedTime = 0.f;

	if (!bWasAlreadyLocked)
	{
		bLockModeActive = true;
		ApplyLockedMovementMode();
		ShowTargetIndicator();
		SetComponentTickEnabled(true);

		if (UFirstAbilitySystemComponent* ASC =
			OwnerCharacter->
				GetFirstAbilitySystemComponent())
		{
			ASC->AddLooseGameplayTag(
				MyGameplayTags::
					DK_Status_TargetLocked);
			bAddedTargetLockTag = true;
		}
	}

	UpdateTargetIndicator();
	OnTargetLockChanged.Broadcast(NewTarget);

	UE_LOG(
		LogTemp,
		Log,
		TEXT("[TargetLock] Locked: %s"),
		*GetNameSafe(NewTarget));
}

void UDKTargetLockComponent::SwitchTarget(
	float Direction)
{
	if (!IsTargetLocked() ||
		FMath::Abs(Direction) < 0.5f ||
		!GetWorld())
	{
		return;
	}

	const float CurrentTime =
		GetWorld()->GetTimeSeconds();
	if (CurrentTime < NextAllowedSwitchTime)
	{
		return;
	}
	NextAllowedSwitchTime =
		CurrentTime + SwitchCooldown;

	AEnemyCharacter* OldTarget =
		CurrentTarget.Get();

	FVector2D OldTargetScreenPosition;
	if (!ProjectTargetToScreen(
		OldTarget,
		OldTargetScreenPosition))
	{
		return;
	}

	APlayerController* PlayerController =
		Cast<APlayerController>(
			OwnerCharacter->GetController());

	int32 ViewportX = 0;
	int32 ViewportY = 0;
	PlayerController->GetViewportSize(
		ViewportX,
		ViewportY);

	const TArray<AEnemyCharacter*> Candidates =
		GatherCandidates(false);

	AEnemyCharacter* BestTarget = nullptr;
	float BestScore = MAX_flt;

	for (AEnemyCharacter* Candidate : Candidates)
	{
		if (Candidate == OldTarget)
		{
			continue;
		}

		FVector2D CandidateScreenPosition;
		if (!ProjectTargetToScreen(
			Candidate,
			CandidateScreenPosition))
		{
			continue;
		}

		const FVector2D ScreenDelta =
			CandidateScreenPosition -
			OldTargetScreenPosition;

		if (Direction > 0.f &&
			ScreenDelta.X <= SwitchScreenDeadZone)
		{
			continue;
		}

		if (Direction < 0.f &&
			ScreenDelta.X >= -SwitchScreenDeadZone)
		{
			continue;
		}

		const float NormalizedX =
			FMath::Abs(ScreenDelta.X) /
			FMath::Max(
				static_cast<float>(ViewportX),
				1.f);

		const float NormalizedY =
			FMath::Abs(ScreenDelta.Y) /
			FMath::Max(
				static_cast<float>(ViewportY),
				1.f);

		const float DistanceScore =
			FVector::Dist(
				OwnerCharacter->GetActorLocation(),
				Candidate->GetActorLocation()) /
			FMath::Max(AcquireRadius, 1.f);

		const float Score =
			NormalizedX +
			NormalizedY * 1.5f +
			DistanceScore * 0.15f;

		if (Score < BestScore)
		{
			BestScore = Score;
			BestTarget = Candidate;
		}
	}

	if (!BestTarget)
	{
		// 同侧没有候选：保持当前目标，不自动解除。
		return;
	}

	UE_LOG(
		LogTemp,
		Log,
		TEXT(
			"[TargetLock] Switch Dir=%.1f From=%s To=%s"),
		Direction,
		*GetNameSafe(OldTarget),
		*GetNameSafe(BestTarget));

	SetCurrentTarget(BestTarget);
}

void UDKTargetLockComponent::UpdateLockedTarget(
	float DeltaTime)
{
	if (!bLockModeActive)
	{
		return;
	}

	if (!OwnerCharacter)
	{
		ClearTargetLockInternal(
			TEXT("InvalidOwner"));
		return;
	}

	if (IsCharacterDead(OwnerCharacter))
	{
		ClearTargetLockInternal(
			TEXT("OwnerDead"));
		return;
	}

	if (!OwnerCharacter->GetController())
	{
		ClearTargetLockInternal(
			TEXT("NoController"));
		return;
	}

	AEnemyCharacter* Target = CurrentTarget.Get();
	if (!IsValid(Target))
	{
		ClearTargetLockInternal(
			TEXT("Destroyed"));
		return;
	}

	if (IsCharacterDead(Target))
	{
		ClearTargetLockInternal(
			TEXT("Dead"));
		return;
	}

	const FVector OwnerLocation =
		OwnerCharacter->GetActorLocation();

	if (FVector::DistSquared(
		OwnerLocation,
		Target->GetActorLocation()) >
		FMath::Square(BreakDistance))
	{
		ClearTargetLockInternal(
			TEXT("Distance"));
		return;
	}

	if (FMath::Abs(
		Target->GetActorLocation().Z -
		OwnerLocation.Z) > MaxHeightDifference)
	{
		ClearTargetLockInternal(
			TEXT("Height"));
		return;
	}

	if (HasLineOfSightTo(Target))
	{
		OccludedElapsedTime = 0.f;
	}
	else
	{
		OccludedElapsedTime += DeltaTime;

		if (OccludedElapsedTime >=
			OcclusionGraceTime)
		{
			ClearTargetLockInternal(
				TEXT("Occluded"));
			return;
		}
	}

	UpdateControlRotation(DeltaTime);
	UpdateTargetIndicator();
}

void UDKTargetLockComponent::UpdateControlRotation(
	float DeltaTime)
{
	AEnemyCharacter* Target = CurrentTarget.Get();
	APlayerController* PlayerController =
		OwnerCharacter
			? Cast<APlayerController>(
				OwnerCharacter->GetController())
			: nullptr;

	if (!Target || !PlayerController)
	{
		return;
	}

	FVector ViewLocation;
	FRotator UnusedViewRotation;
	PlayerController->GetPlayerViewPoint(
		ViewLocation,
		UnusedViewRotation);

	FRotator DesiredRotation =
		(Target->GetTargetLockLocation() -
			ViewLocation).Rotation();

	DesiredRotation.Pitch = FMath::Clamp(
		FRotator::NormalizeAxis(
			DesiredRotation.Pitch),
		MinCameraPitch,
		MaxCameraPitch);
	DesiredRotation.Roll = 0.f;

	FRotator NewRotation = FMath::RInterpTo(
		PlayerController->GetControlRotation(),
		DesiredRotation,
		DeltaTime,
		CameraRotationInterpSpeed);
	NewRotation.Roll = 0.f;

	PlayerController->SetControlRotation(
		NewRotation);
}

void UDKTargetLockComponent::ApplyLockedMovementMode()
{
	if (!OwnerCharacter)
	{
		return;
	}

	UCharacterMovementComponent* Movement =
		OwnerCharacter->GetCharacterMovement();
	if (!Movement)
	{
		return;
	}

	if (!bMovementSettingsCached)
	{
		bCachedOrientRotationToMovement =
			Movement->bOrientRotationToMovement;
		bCachedUseControllerDesiredRotation =
			Movement->bUseControllerDesiredRotation;
		bMovementSettingsCached = true;
	}

	// 角色不再朝移动方向转，而是平滑朝 Controller 的目标 Yaw。
	Movement->bOrientRotationToMovement = false;
	Movement->bUseControllerDesiredRotation = true;
}

void UDKTargetLockComponent::RestoreMovementMode()
{
	if (!OwnerCharacter ||
		!bMovementSettingsCached)
	{
		return;
	}

	if (UCharacterMovementComponent* Movement =
		OwnerCharacter->GetCharacterMovement())
	{
		Movement->bOrientRotationToMovement =
			bCachedOrientRotationToMovement;
		Movement->bUseControllerDesiredRotation =
			bCachedUseControllerDesiredRotation;
	}

	bMovementSettingsCached = false;
}

void UDKTargetLockComponent::ShowTargetIndicator()
{
	if (!OwnerCharacter ||
		!TargetIndicatorWidgetClass)
	{
		// 没配置 UI 不应让核心锁敌失败。
		return;
	}

	APlayerController* PlayerController =
		Cast<APlayerController>(
			OwnerCharacter->GetController());
	if (!PlayerController)
	{
		return;
	}

	if (!TargetIndicatorWidget)
	{
		TargetIndicatorWidget =
			CreateWidget<UUserWidget>(
				PlayerController,
				TargetIndicatorWidgetClass);

		if (!TargetIndicatorWidget)
		{
			return;
		}

		TargetIndicatorWidget->AddToViewport(20);
		TargetIndicatorWidget->
			SetAlignmentInViewport(
				FVector2D(0.5f, 0.5f));
		TargetIndicatorWidget->
			SetDesiredSizeInViewport(
				TargetIndicatorSize);
	}

		TargetIndicatorWidget->SetVisibility(
		ESlateVisibility::HitTestInvisible);
}

void UDKTargetLockComponent::HideTargetIndicator()
{
	if (TargetIndicatorWidget)
	{
		TargetIndicatorWidget->SetVisibility(
			ESlateVisibility::Collapsed);
	}
}

void UDKTargetLockComponent::UpdateTargetIndicator()
{
	if (!TargetIndicatorWidget ||
		!CurrentTarget.IsValid() ||
		!OwnerCharacter)
	{
		return;
	}

	APlayerController* PlayerController =
		Cast<APlayerController>(
			OwnerCharacter->GetController());
	if (!PlayerController)
	{
		return;
	}

	FVector2D WidgetPosition;
	const bool bProjected =
		UWidgetLayoutLibrary::
			ProjectWorldLocationToWidgetPosition(
				PlayerController,
				GetCurrentTargetLocation(),
				WidgetPosition,
				false);

	if (!bProjected)
	{
		TargetIndicatorWidget->SetVisibility(
			ESlateVisibility::Collapsed);
		return;
	}

	TargetIndicatorWidget->SetVisibility(
		ESlateVisibility::HitTestInvisible);

	// ProjectWorldLocationToWidgetPosition 已处理 DPI；
	// 因此 SetPositionInViewport 的 RemoveDPIScale 使用 false。
	TargetIndicatorWidget->SetPositionInViewport(
		WidgetPosition,
		false);
}

void UDKTargetLockComponent::ClearTargetLock()
{
	ClearTargetLockInternal(TEXT("Manual"));
}

void UDKTargetLockComponent::ClearTargetLockInternal(
	const TCHAR* Reason)
{
	if (!bLockModeActive &&
		!CurrentTarget.IsValid())
	{
		return;
	}

	const FString OldTargetName =
		GetNameSafe(CurrentTarget.Get());

	CurrentTarget.Reset();
	OccludedElapsedTime = 0.f;
	NextAllowedSwitchTime = 0.f;

	SetComponentTickEnabled(false);
	RestoreMovementMode();
	HideTargetIndicator();

	if (bAddedTargetLockTag && OwnerCharacter)
	{
		if (UFirstAbilitySystemComponent* ASC =
			OwnerCharacter->
				GetFirstAbilitySystemComponent())
		{
			ASC->RemoveLooseGameplayTag(
				MyGameplayTags::
					DK_Status_TargetLocked);
		}
	}

	bAddedTargetLockTag = false;
	bLockModeActive = false;

	OnTargetLockChanged.Broadcast(nullptr);

	UE_LOG(
		LogTemp,
		Log,
		TEXT(
			"[TargetLock] Cleared Reason=%s Old=%s"),
		Reason,
		*OldTargetName);
}

FVector UDKTargetLockComponent::
GetCurrentTargetLocation() const
{
	const AEnemyCharacter* Target =
		CurrentTarget.Get();

	return Target
		? Target->GetTargetLockLocation()
		: FVector::ZeroVector;
}
```

### 7.4 这份实现的重要行为

1. **初始锁定需要在镜头前方**，保持锁定时不再重复要求目标留在最初的 65°锥体内；
2. **屏幕中心权重 75%**，距离权重 25%，所以准星附近的敌人优先；
3. **切换以当前目标在屏幕上的位置为锚点**，不是简单选择世界坐标最近者；
4. 同侧没有目标时保持当前目标，不自动断锁；
5. 遮挡少于 1 秒继续保持，避免经过细柱时标记闪烁；
6. 所有断锁都进入 `ClearTargetLockInternal`，不会遗漏移动、UI 或 Tag 清理；
7. 没配置锁定标记时，核心锁敌仍然能工作。

---

## 8. 第四步：接入 ADKCharacter

### 8.1 修改 DKCharacter.h

文件：

`Source/First/Public/Character/DKCharacter.h`

在前置声明区域增加：

```cpp
class UDKTargetLockComponent;
```

在输入函数区域增加：

```cpp
void Input_TargetLock(
	const FInputActionValue& Value);

void Input_SwitchTarget(
	const FInputActionValue& Value);
```

在 `GetDodgeInputDirection` 附近增加：

```cpp
// 锁定时严格使用“主角到目标”的 Yaw；
// 不依赖镜头插值是否已经跟到位。
virtual float GetDodgeReferenceYaw() const override;
```

在组件区域增加：

```cpp
UPROPERTY(
	VisibleAnywhere,
	BlueprintReadOnly,
	Category="Target Lock",
	meta=(AllowPrivateAccess="true"))
TObjectPtr<UDKTargetLockComponent>
	TargetLockComponent;
```

在 `public` 区域增加访问函数：

```cpp
FORCEINLINE UDKTargetLockComponent*
GetTargetLockComponent() const
{
	return TargetLockComponent;
}
```

### 8.2 修改 DKCharacter.cpp 的 include

文件：

`Source/First/Private/Character/DKCharacter.cpp`

增加：

```cpp
#include "Components/Targeting/DKTargetLockComponent.h"
```

### 8.3 构造函数创建组件

在创建 `DKCombatComponent` / `DKUIComponent` 的位置附近增加：

```cpp
TargetLockComponent =
	CreateDefaultSubobject<UDKTargetLockComponent>(
		TEXT("TargetLockComponent"));
```

不要打开 `ADKCharacter` 的 Actor Tick。组件拥有自己独立且按需启停的 Tick。

### 8.4 SetupPlayerInputComponent 绑定输入

在现有原生输入绑定区域增加：

```cpp
FirstInput->BindNativeInputAction(
	InputConfigDataAsset,
	MyGameplayTags::InputTag_TargetLock,
	ETriggerEvent::Started,
	this,
	&ThisClass::Input_TargetLock);

FirstInput->BindNativeInputAction(
	InputConfigDataAsset,
	MyGameplayTags::InputTag_SwitchTarget,
	ETriggerEvent::Triggered,
	this,
	&ThisClass::Input_SwitchTarget);
```

锁敌用 `Started`，确保一次按键只切换一次状态。

鼠标滚轮轴用 `Triggered`，组件内部的 0.2 秒冷却负责限制连续滚动时的切换频率。

### 8.5 锁定期间拦截自由 Look

把 `Input_Look` 改为：

```cpp
void ADKCharacter::Input_Look(
	const FInputActionValue& Value)
{
	// 锁定镜头由 TargetLockComponent 控制。
	// 若这里继续 AddControllerYaw/Pitch，会和自动镜头互相争抢。
	if (TargetLockComponent &&
		TargetLockComponent->IsTargetLocked())
	{
		return;
	}

	const FVector2D LookVector =
		Value.Get<FVector2D>();

	AddControllerYawInput(LookVector.X);
	AddControllerPitchInput(LookVector.Y);
}
```

### 8.6 新增两个输入函数

在 `.cpp` 文件末尾增加：

```cpp
void ADKCharacter::Input_TargetLock(
	const FInputActionValue& Value)
{
	if (TargetLockComponent)
	{
		TargetLockComponent->ToggleTargetLock();
	}
}

void ADKCharacter::Input_SwitchTarget(
	const FInputActionValue& Value)
{
	if (TargetLockComponent)
	{
		TargetLockComponent->SwitchTarget(
			Value.Get<float>());
	}
}
```

### 8.7 精确联动八向闪避

增加：

```cpp
float ADKCharacter::GetDodgeReferenceYaw() const
{
	if (TargetLockComponent &&
		TargetLockComponent->IsTargetLocked())
	{
		FVector ToTarget =
			TargetLockComponent->
				GetCurrentTargetLocation() -
			GetActorLocation();
		ToTarget.Z = 0.f;

		if (!ToTarget.IsNearlyZero())
		{
			return ToTarget.Rotation().Yaw;
		}
	}

	return Super::GetDodgeReferenceYaw();
}
```

这样即使刚锁定或刚切换目标、镜头仍在 `RInterpTo` 过渡，闪避方向也会立即以真实目标方向为基准。

> 当前项目核对提示：`FirstGA_DKDodge.cpp` 的 `EndAbility()` 里目前重复调用了两次 `Super::EndAbility(...)`。这不是索敌功能造成的，也不属于本册修改范围；如果锁定闪避测试出现 Ability 重复结束、警告或异常回调，应先把它记为既有问题，之后单独处理，不要误判成锁敌方向错误。

---

## 9. 第五步：编译闸门

### 9.1 推荐顺序

```text
闸门 0
→ 不添加任何索敌代码
→ 先对当前工程执行一次普通 Build
→ 确认现有工程本身可以编译

闸门 1
→ EnemyCharacter.h/.cpp
→ MyGameplayTags.h/.cpp
→ Build

闸门 2
→ DKTargetLockComponent.h/.cpp
→ Build

闸门 3
→ DKCharacter.h/.cpp
→ Build

闸门 4
→ 打开 UE
→ 创建/修改输入和 UI 资产
```

### 9.2 新类和 UPROPERTY 不建议只依赖热重载

新增：

- C++ 类；
- `UPROPERTY`；
- `UFUNCTION`；
- 原生 GameplayTag；

时，建议：

1. 保存并关闭 UE；
2. 用 IDE 执行普通 Build；
3. 编译成功后重开 UE。

否则可能出现“代码编译了，但蓝图里找不到类/属性/Tag”的假问题。

### 9.3 常见编译错误

| 报错 | 原因 | 修复 |
|---|---|---|
| 找不到 `ABaseCharacter` | 组件头文件的前置声明漏抄 | 确认完整头文件顶部有 `class ABaseCharacter;` |
| 找不到 `AEnemyCharacter::StaticClass` | `.cpp` 没 include EnemyCharacter | 检查 include |
| 找不到 `UEngineTypes` | 缺少 `Engine/EngineTypes.h` | 添加 include |
| 找不到 `SCENE_QUERY_STAT` / `FCollisionQueryParams` | 缺少碰撞查询头 | 添加 `CollisionQueryParams.h` |
| 找不到 `UWidgetLayoutLibrary` | 缺少 WidgetLayoutLibrary 头 | 添加对应 include |
| 链接 UMG 符号失败 | 模块依赖缺失 | 当前项目已包含 `UMG`；检查是否误改 `First.Build.cs` |
| `GetDodgeReferenceYaw` 不是虚函数 | BaseCharacter 版本不一致 | 当前项目中它已是 `virtual`，确认没有抄到其它工程 |

---

## 10. 第六步：编辑器输入资产

### 10.1 创建 IA_TargetLock

路径：

`/Game/DKCharacter/Input/Action`

1. 右键 → **输入（Input）→ 输入操作（Input Action）**；
2. 命名 `IA_TargetLock`；
3. **值类型（Value Type）**选择 **数字（布尔）（Digital / Bool）**；
4. 不需要 Trigger 和 Modifier。

### 10.2 创建 IA_SwitchTarget

同一路径创建：

`IA_SwitchTarget`

配置：

```text
Value Type = Axis1D
```

### 10.3 修改 IMC_Default

打开：

`/Game/DKCharacter/Input/IMC_Default`

增加：

| Input Action | 键位 | 说明 |
|---|---|---|
| `IA_TargetLock` | Middle Mouse Button（鼠标中键） | 锁定/解除 |
| `IA_SwitchTarget` | Mouse Wheel Axis（鼠标滚轮轴） | 正值向右、负值向左 |

推荐暂时不要使用鼠标右键，因为右键通常会留给格挡、弹反或重攻击。

手柄的锁定键可以后续增加：

| Input Action | 手柄键 |
|---|---|
| `IA_TargetLock` | Gamepad Right Thumbstick |

本册暂时不要把持续的 `Gamepad Right X` 直接接入 `IA_SwitchTarget`。摇杆保持倾斜时会每帧产生输入，`SwitchCooldown` 只能限速，不能保证“一推只切一次”。手柄切换需要增加“越过阈值只触发一次，回中后才能再次触发”的门闩，放到后续扩展。

### 10.4 修改 DA_InputConfig

打开：

`/Game/DKCharacter/DA_InputConfig`

在 **Native Input Actions** 添加：

| InputTag | InputAction |
|---|---|
| `InputTag.TargetLock` | `IA_TargetLock` |
| `InputTag.SwitchTarget` | `IA_SwitchTarget` |

不要把它们放进 **Ability Input Actions**。

---

## 11. 第七步：创建锁定标记 UI

### 11.1 创建控件

路径：

`/Game/UI`

创建 Widget Blueprint：

`WBP_TargetLockIndicator`

父类使用普通 `UserWidget` 即可。

### 11.2 推荐布局

为了让 `SetPositionInViewport` 定位准确：

```text
WBP_TargetLockIndicator
└─ Size Box
    Width Override = 40
    Height Override = 40
    └─ Image
        Brush = 圆环 / 菱形 / 四角准星
        Visibility = Not Hit-Testable (Self & All Children)
```

不要用一个铺满全屏的 Canvas 作为最终可见尺寸，否则移动的是整张全屏画布，标记可能偏移。

代码也会把整个 `UserWidget` 设置为 `HitTestInvisible`，确保锁定标记不会挡住菜单或其它 HUD 点击。

### 11.3 配置 BP_DKCharacter

打开：

`/Game/DKCharacter/BP_DKCharacter`

选中继承的：

`TargetLockComponent`

配置：

```text
Target Indicator Widget Class = WBP_TargetLockIndicator
Acquire Radius = 2000
Break Distance = 2600
Max Acquire Angle Degrees = 65
Max Height Difference = 600
Occlusion Grace Time = 1.0
Camera Rotation Interp Speed = 8.0
Switch Cooldown = 0.2
```

### 11.4 调整 BP_Boss 的 TargetLockPoint

打开：

`/Game/Enemy/BP_Boss`

1. 在组件树选择继承的 `TargetLockPoint`；
2. 在视口把它移动到胸口或上半身；
3. 确保该点位于胶囊碰撞内部或非常接近模型身体。

代码允许 Visibility Trace 的第一处命中是目标本身，所以先碰到 BOSS 胶囊属于正常可见。仍然不要把锁定点放到身体外很远的位置，否则锁定标记和镜头会明显偏离敌人；如果点落到身体后方的墙体里，还可能被墙先挡住而误判为不可见。

---

## 12. 第八步：碰撞检查

不创建新通道，但需要确认 `BP_Boss`：

```text
CapsuleComponent
├─ Collision Enabled：Query Only 或 Query And Physics
└─ Object Type：Pawn
```

Mesh 和 BOSS 胶囊对 `Visibility` 可以保持当前配置。本文的 LOS 规则是：

```text
视线没有命中任何阻挡物 → 可见
第一处阻挡物是目标 → 可见
第一处阻挡物是墙或其它障碍 → 不可见
```

因此真正必须 **Block Visibility** 的是墙、柱子等遮挡物，不是 BOSS 本身。

如果 BOSS 胶囊不再是 `Pawn`，球形查询会找不到它；如果墙体不阻挡 `Visibility`，才会出现隔墙锁定。

---

## 13. 运行时完整流程

### 13.1 初始锁定

```text
鼠标中键 Started
→ ADKCharacter::Input_TargetLock
→ TargetLockComponent::ToggleTargetLock
→ SphereOverlapActors（ECC_Pawn）
→ Cast AEnemyCharacter
→ 排除死亡 / 超距 / 高差 / 视锥外 / 遮挡 / 屏幕外
→ 计算屏幕中心分数 + 距离分数
→ SetCurrentTarget
→ 添加 DK.Status.TargetLocked
→ 切换 CharacterMovement 旋转模式
→ 显示锁定标记
→ 开启组件 Tick
```

### 13.2 锁定维护

```text
每帧组件 Tick
├─ 玩家是否死亡
├─ 目标是否有效/死亡
├─ 是否超过 BreakDistance
├─ 是否超过允许高差
├─ 是否遮挡超过 1 秒
├─ 平滑更新 ControlRotation
└─ 更新锁定标记屏幕位置
```

不会每帧重新遍历所有敌人。完整候选查询只在：

- 初次锁定；
- 主动切换目标；

时发生。

### 13.3 左右切换

```text
鼠标滚轮产生 Axis1D
→ Direction 正/负决定右/左
→ 重新查询可见候选
→ 当前目标投影到屏幕
→ 只保留对应一侧目标
→ 比较横向距离、纵向偏差和世界距离
→ 选择最合理的新目标
→ 同侧没有候选则保持原目标
```

### 13.4 统一解除

```text
Manual / Dead / Destroyed / Distance / Height / Occluded / OwnerDead
→ ClearTargetLockInternal
→ 关闭组件 Tick
→ 恢复普通移动转向
→ 隐藏 UI
→ 移除 DK.Status.TargetLocked
→ 清空 Weak Target
→ 广播 OnTargetLockChanged(nullptr)
```

---

## 14. 测试清单

建议先在单 BOSS 地图完成基础测试，再测试多目标切换。

| 测试 | 操作 | 预期结果 |
|---|---|---|
| 正常锁定 | BOSS 在镜头中央 2000 内，按中键 | 标记出现，镜头平滑对准 BOSS |
| 手动解除 | 已锁定时再按中键 | 标记消失，恢复自由镜头和普通转向 |
| 无目标 | 镜头前没有敌人时按中键 | 不报错、不进入锁定 |
| 屏幕中心优先 | 中央和边缘各放一个敌人 | 优先锁中央目标 |
| 背后目标 | 目标在镜头背后，按中键 | 不应直接锁定 |
| 隔墙目标 | BOSS 与玩家之间有阻挡 Visibility 的墙 | 不会成为最终锁定目标 |
| 短暂遮挡 | 锁定后快速经过柱子，遮挡少于 1 秒 | 保持锁定 |
| 长时间遮挡 | 躲到墙后超过 1 秒 | 自动解除 |
| 距离迟滞 | 2000 内锁定，然后退到 2400 | 保持锁定 |
| 超距断锁 | 继续退到 2600 外 | 自动解除 |
| 目标死亡 | 锁定 BOSS 后击杀 | 立即解除，不再锁尸体 |
| 目标销毁 | 调试时对当前目标执行 Destroy Actor | 自动解除，不访问失效指针 |
| 玩家死亡 | 锁定状态下让主角死亡 | 自动解除并恢复移动模式 |
| 左右切换 | 多目标场景滚动滚轮 | 只切到对应屏幕一侧 |
| 无同侧目标 | 只有当前目标时滚轮 | 保持当前锁定 |
| 连续滚轮 | 快速连续滚动 | 切换频率受 0.2 秒冷却限制，不会每帧跳转 |
| 八向闪避 | 锁定后向左/右/后闪避 | 方向以敌人为基准 |
| 重复 PIE | 多次运行、停止 | 不残留标记、Tag 或移动模式 |
| 状态 Tag 清理 | 锁定后用 GAS 调试查看 Tag，再以不同原因解除 | `DK.Status.TargetLocked` 每次都被移除 |
| 未配置 UI | 暂时清空 WidgetClass 后锁定 | 镜头与移动仍工作，只是不显示标记 |

### 14.1 多 BOSS 切换测试警告

当前 `BossAIController`：

- 没有 `GenericTeamId`；
- 感知回调把任意 `ABaseCharacter` 都可能当目标；
- 多个 BOSS 可能互相感知甚至互相攻击。

为了只测试玩家左右切换，可以：

1. 临时复制多个 `BP_Boss`；
2. 把所有参与切换测试的 BOSS（包括原本那只）的 **自动控制 AI（Auto Possess AI）** 临时关闭；
3. 测完删除副本或恢复；
4. 不要把 BOSS 互相索敌误判为玩家锁敌 Bug。

更干净的做法是创建一个仅用于锁定测试、继承 `AEnemyCharacter` 且不运行 AI 的简单测试假人。

真正的多敌战斗应在后续完成阵营系统后测试。

---

## 15. 常见问题与排查

| 现象 | 常见原因 | 修复 |
|---|---|---|
| 按中键完全没反应 | Tag 未编译、IA 未映射、DA 条目放错数组 | 检查 `InputTag.TargetLock → IA_TargetLock` 是否在 NativeInputActions |
| 启动时提示 InputConfig 找不到 Tag | C++ Tag 已写但编辑器仍开着旧反射数据 | 关闭 UE，普通 Build，重开 |
| BOSS 在面前却找不到 | 胶囊不是 Pawn、超距、视角超过 65°或 Visibility 被障碍阻挡 | 逐项检查碰撞和参数 |
| 隔墙仍能锁定 | 墙不阻挡 Visibility | 修改墙体碰撞响应 |
| 一锁定立刻断开 | `BreakDistance < AcquireRadius`、目标已有 Dead Tag 或控制器为空 | 检查参数与日志 Reason |
| 标记在脚下/腰部 | `TargetLockPoint` 太低 | 在 BP_Boss 中移到胸口 |
| 标记偏离且受 DPI 影响 | 使用了普通屏幕投影或重复移除 DPI Scale | 使用本文的 Widget 投影函数和 `SetPositionInViewport(..., false)` |
| 镜头抖动、和鼠标抢控制 | 锁定时 `Input_Look` 仍在执行 | 添加锁定状态提前返回 |
| 角色一边朝目标一边朝移动方向抖 | 没关闭 `bOrientRotationToMovement` | 检查 ApplyLockedMovementMode |
| 解锁后仍一直朝镜头 | 恢复函数没走或在多个地方自行清目标 | 所有路径统一调用 `ClearTargetLockInternal` |
| 目标死后仍被锁 | 只判断指针，没有检查 `Shared.Status.Dead` | 保留死亡 Tag 判断 |
| 滚轮切换过快 | `SwitchCooldown` 太小，或误把持续摇杆轴接入首版输入 | 恢复 0.2 秒左右；首版只使用鼠标滚轮 |
| 锁定侧移出现横滑 | 当前 1D Blend Space 没有侧走/后退动画 | 这是动画限制，后续制作 2D 锁定移动 |
| 攻击没有“自动命中”目标 | 锁敌只调整相机与朝向，不修改伤害目标 | 仍需武器实际 Overlap，这是正确设计 |
| 顶部 BOSS 血条和锁定目标不一致 | 现有 BossHUD 固定寻找场景第一个 BOSS | 单 BOSS 暂可接受；多目标时改为监听 OnTargetLockChanged |
| 多个 BOSS 互相打 | 当前没有阵营系统 | 增加 GenericTeamId 后再做多敌实战 |

### 15.1 推荐日志阅读顺序

输出日志过滤：

```text
[TargetLock]
```

三个最重要的日志：

```text
[TargetLock] Acquire Candidates=... Chosen=... Score=...
[TargetLock] Switch Dir=... From=... To=...
[TargetLock] Cleared Reason=...
```

这里的 `Candidates` 是“通过世界距离、角度、死亡和遮挡等基础过滤”的数量，屏幕投影过滤在之后的评分阶段进行。因此下面这种日志不一定是错误：

```text
Candidates > 0，但 Chosen=None
```

它通常表示基础候选存在，但它们都无法投影到当前屏幕可见区域。此时先检查敌人是否在镜头内，再检查 Viewport 与投影设置。

排查顺序：

```text
输入是否进入
→ Candidates 是否大于 0
→ Chosen 是否有效
→ 是否马上出现 Cleared
→ Cleared Reason 是什么
→ 再检查对应的距离、死亡或遮挡条件
```

---

## 16. 参数调节建议

### 16.1 锁不到稍远目标

先把：

```text
AcquireRadius：2000 → 2400
```

不要同时把 `BreakDistance` 设得更小。推荐保持：

```text
BreakDistance = AcquireRadius + 500～800
```

### 16.2 总是锁到近处屏幕边缘敌人

当前公式已经让屏幕中心占 75%。如果仍不理想：

```cpp
ScreenCenterScore * 0.85f +
DistanceScore * 0.15f
```

### 16.3 镜头太硬

降低：

```text
CameraRotationInterpSpeed：8 → 5
```

镜头跟不上则提高到 10～12。

### 16.4 经过障碍总是掉锁

提高：

```text
OcclusionGraceTime：1.0 → 1.3
```

不建议无限保持遮挡目标，否则玩家隔墙也会一直锁定。

### 16.5 切换目标过快或过慢

```text
过快：SwitchCooldown 0.2 → 0.3
过慢：SwitchCooldown 0.2 → 0.12
```

---

## 17. 暂不在本册实现的内容

### 17.1 锁定移动 2D Blend Space

完整类魂移动通常需要：

```text
ForwardSpeed / GroundSpeed
Direction（-180～180）
bIsTargetLocked
```

并准备：

- 前走；
- 后退；
- 左侧移；
- 右侧移；
- 对角移动；
- 锁定待机。

这是解决锁定侧移横滑的正确方向。

### 17.2 阵营系统

以后使用：

- `IGenericTeamAgentInterface`；
- `FGenericTeamId`；
- 或统一的敌我 GameplayTag；

让玩家、BOSS、友军和召唤物都使用同一套敌我判断。到那时，组件的候选过滤应调用统一阵营接口，而不是只依赖 `AEnemyCharacter`。

### 17.3 自动切换到下一个目标

首版目标死亡后直接解除，比自动跳转更可预测。

以后可以在 `Dead` 路径中：

```text
先查找剩余候选
→ 有候选则切换
→ 无候选才 Clear
```

### 17.4 BOSS HUD 跟随当前锁定目标

现有 `UFirstBossHUDWidget` 使用 `TActorIterator` 找场景第一个 BOSS，适用于当前单 BOSS。

多 BOSS 时建议：

```text
TargetLockComponent::OnTargetLockChanged
→ PlayerHUD / BossHUD 解除旧目标委托
→ 绑定新目标 EnemyUIComponent
→ 显示新目标血条和名称
```

不要在本册顺手把 HUD、索敌和战斗重构绑在一起。

### 17.5 攻击起手吸附与位移

锁敌只保证朝向。攻击磁吸、根运动校正或短距离位移属于攻击 Ability：

```text
锁敌组件提供 CurrentTarget
攻击 Ability 决定是否读取
攻击 Ability 决定允许的吸附距离和角度
```

锁敌组件不应直接移动玩家或强制让伤害命中。

### 17.6 Targetable Interface

当训练假人、弱点、机关也能锁定时，再创建：

```text
ITargetableInterface
├─ CanBeTargetedBy
├─ GetTargetLockLocation
└─ OnTargeted / OnUntargeted（可选）
```

当前只有 `AEnemyCharacter`，现在增加接口收益不大。

---

## 18. 最终职责总结

```text
ADKCharacter
├─ 接收 TargetLock / SwitchTarget 输入
├─ 把请求转给组件
├─ 锁定时停止自由 Look
└─ 向闪避系统提供精确目标 Yaw

UDKTargetLockComponent
├─ 搜集与筛选敌人
├─ 选择屏幕中心最佳目标
├─ 保存 Weak CurrentTarget
├─ 左右切换
├─ 检查死亡、距离和遮挡
├─ 更新相机与角色旋转模式
├─ 更新锁定标记
├─ 添加/移除 DK.Status.TargetLocked
└─ 统一清理所有解除路径

AEnemyCharacter
└─ 只提供 TargetLockPoint

攻击 Ability
├─ 不负责寻找锁定目标
├─ 不因为锁定而直接命中
└─ 继续使用真实武器碰撞结算伤害

动画系统
└─ 后续根据 DK.Status.TargetLocked
   切换锁定移动 2D Blend Space
```

本册完成的验收重点是：

```text
选得对
跟得稳
切得准
死/远/遮挡会解除
解除后状态完全恢复
```

锁定侧移动画是否自然属于下一阶段，不应阻塞本册核心功能验收。
