# 主角锁定八向移动动画实现指导

> 适用项目：`First`（UE 5.6 / C++ / GAS / Enhanced Input / 中文版编辑器）
>
> 素材来源：`SwordAnimsetPro` 的 `Root-Motion/Walk_* / Run_*` 定向走跑库（F / F_L / F_R / L / R / B_L / B_R / B 共 8 方向）
>
> 前置条件：主角索敌（锁定、切换与解除）已经可用，`DKTargetLockComponent` 在锁定时已把角色转向目标。
>
> 本文只提供实现指导，不会自动修改 C++、动画蓝图或资产。

---

## 0. 最终效果

```text
锁定敌人后：
→ 向前推摇杆：目标方向正常前进
→ 向左/右推摇杆：左脚/右脚跨步侧移，角色保持面向目标
→ 向后推摇杆：正确倒退步态
→ 四个 45° 方向：真实 45° 定向步态（不再依赖四角混合近似）
→ 任意中间角度：BlendSpace 在相邻两个真实样本间平滑插值

解锁后：恢复现有普通 Walk/Run 混合，无任何残留。
锁定中攻击/闪避/格挡表现不受影响（Slot 蒙太奇覆盖）。
```

## 1. 本册设计

### 1.1 八向 = 2D BlendSpace 中 8 个真实样本

```text
        (-1,0) Walk_B
           ▲（X 轴 = 前向输入）
           │
(-0.5,0.5) │ (0.5,0.5)
  Walk_B_L │ Walk_B_R
 ──────────┼──────────►（Y 轴 = 右向输入）
(-0.5,-0.5)│ (0.5,-0.5)
  Walk_F_L │ Walk_F_R
           │
        (1,0) Walk_F
```

- 四角放正方向 `F / B / L / R`；
- 四边中点放 45° 方向 `F_L / F_R / B_L / B_R`；
- 任意中间角度（比如 20°）由邻接样本自动插值；
- 前进方向 `Walk_F` 直接用现有资产，零新增成本。

### 1.2 素材与现有体系完全同源

当前移动混合空间 `BS_DK_Sword_Locomotion` 的引用链：

```text
/Game/SwordAnimsetPro/Animations/Root-Motion/Walk_F_Anim / Run_F_Anim
      ▼ 复制到项目并转 In-Place
/Game/DKCharacter/AnimBP/Sword/Source/A_DK_Sword_Walk_F_InPlace
/Game/DKCharacter/AnimBP/Sword/Source/A_DK_Sword_Run_F_InPlace
      ▼
BS_DK_Sword_Locomotion → ABP_DK_Sword
```

本册完全复用这条链：**同骨架（`SK_Mannequin_Skeleton`）无需重定向，同目录、同命名规范**。

### 1.3 动画方向的参考系

```text
角色 Forward = 面向目标（锁定时由 DKTargetLockComponent 控制，已成立）
移动输入     = 相机相对（Enhanced Input 现状）
动画方向     = 角色相对
```

动画蓝图需要的两个输入：

```text
MoveForward = 移动输入投影到 角色Forward   （-1..1）
MoveRight   = 移动输入投影到 角色Right     （-1..1）
```

**不要**用相机方向做投影基准——相机在切换目标时有跳变，投影到角色轴上天然平滑（角色旋转受 RotationRate 限制）。

### 1.4 本册不变的内容

- 锁定时速度仍用现有 250（步行）/500（奔跑）；
- 八向闪避方向早已按目标方向量化（`GetDodgeReferenceYaw`），与八向移动方向一致；
- 攻击、格挡、处决等蒙太奇通过 `DefaultSlot` 覆盖移动动画。

## 2. 当前工程已确认的事实

| 项目 | 当前情况 |
|---|---|
| 锁定时朝向 | `DKTargetLockComponent` 已设 `bUseControllerDesiredRotation = true`，角色面向目标 ✅ |
| 锁定状态查询 | `ADKCharacter::GetTargetLockComponent()->IsTargetLocked()`（公开，现成） |
| 当前移动动画 | `/Game/DKCharacter/AnimBP/Sword/Source/A_DK_Sword_Walk_F_InPlace`、`A_DK_Sword_Run_F_InPlace`、`A_DK_Sword_Idle` |
| 移动混合空间 | `/Game/DKCharacter/AnimBP/Sword/BS_DK_Sword_Locomotion` |
| 动画蓝图 | `ABP_DK_Sword`：当前只有 `GroundSpeed / bHasAcceleration / bShouldMove` 三个输入 |
| 定向动画源库 | `/Game/SwordAnimsetPro/Animations/Root-Motion/`（Walk 8 方向 + Run 8 方向，命名 `Walk_F_Anim` / `Walk_F_L_Anim` …） |
| 骨架 | `SK_Mannequin_Skeleton`（主角当前骨架，定向动画同骨架直接可用） |

## 3. 改动总览

| 文件/资产 | 操作 | 内容 |
|---|---|---|
| `FirstDKAnimInstance.h/.cpp` | 修改 | 新增 `bIsTargetLocked / MoveForward / MoveRight` 三个输入 |
| 14 个新 In-Place 动画 | 复制转化 | Walk 7 个（L / R / B / F_L / F_R / B_L / B_R）+ Run 7 个（同）——`F` 方向已存在，零新增 |
| `BS_DK_LockedWalk` / `BS_DK_LockedRun` | 新建 | 2D BlendSpace，8 个方向样本（四角 + 四边中点） |
| `ABP_DK_Sword` | 修改 | 加 `bIsTargetLocked` 分支：锁定走八向 BlendSpace，否则走现有混合 |

## 4. 第一步：动画蓝图状态输入（C++）

### 4.1 修改 `FirstDKAnimInstance.h`

文件：

```text
Source/First/Public/AnimInstances/FirstDKAnimInstance.h
```

前置声明区域增加：

```cpp
class ADKCharacter;
```

在 `bShouldMove` 之后增加：

```cpp
	// 当前是否处于目标锁定。AnimBP 用它切换到“锁定定向移动”分支。
	UPROPERTY(VisibleDefaultsOnly, BlueprintReadOnly, Category="AnimData|Locomotion")
	bool bIsTargetLocked = false;

	// 移动输入在角色本地坐标系的分量（-1..1）：
	// MoveForward：沿角色朝前为正；MoveRight：沿角色右侧为正。
	// AnimBP 的 2D BlendSpace 用它取方向动画。
	UPROPERTY(VisibleDefaultsOnly, BlueprintReadOnly, Category="AnimData|Locomotion")
	float MoveForward = 0.f;

	UPROPERTY(VisibleDefaultsOnly, BlueprintReadOnly, Category="AnimData|Locomotion")
	float MoveRight = 0.f;
```

### 4.2 修改 `FirstDKAnimInstance.cpp`

文件：

```text
Source/First/Private/AnimInstances/FirstDKAnimInstance.cpp
```

顶部 include 区域增加：

```cpp
#include "Character/DKCharacter.h"
```

`NativeThreadSafeUpdateAnimation` 中，在现有 `GroundSpeed / bShouldMove` 计算之后增加：

```cpp
	MoveForward = 0.f;
	MoveRight = 0.f;
	bIsTargetLocked = false;

	const ADKCharacter* DKCharacter = Cast<ADKCharacter>(OwningCharacter);
	if (DKCharacter)
	{
		bIsTargetLocked =
			DKCharacter->GetTargetLockComponent() &&
			DKCharacter->GetTargetLockComponent()->IsTargetLocked();

		if (bIsTargetLocked && OwningCharacter)
		{
			// 输入向量按“角色本地轴”投影：
			// 锁定时角色面向目标，动画角色相对，两者正交不乖离。
			const FVector InputVector =
				OwningCharacter->GetLastMovementInputVector();
			if (!InputVector.IsNearlyZero())
			{
				const FVector InputFlat = FVector(InputVector.X, InputVector.Y, 0.f).GetSafeNormal();

				const FVector Forward = OwningCharacter->GetActorForwardVector();
				const FVector Right = OwningCharacter->GetActorRightVector();

				MoveForward = FMath::Clamp(
					FVector::DotProduct(InputFlat, Forward),
					-1.f, 1.f);
				MoveRight = FMath::Clamp(
					FVector::DotProduct(InputFlat, Right),
					-1.f, 1.f);
			}
		}
	}
```

说明：

- 锁定判断直接问组件，不依赖 ASC 标签（组件是权威状态）；
- `GetLastMovementInputVector` 每帧由输入产生，动画线程只读是安全的（与现有 GroundSpeed 同模式）；
- 未锁定或停止移动时三个值强制归零，避免 AnimBP 拿旧值混合；
- 输入在角色轴上是连续值（不强行量化），这样 BlendSpace 可以表示 8 个方向**之间**的任意角度。

### 编译闸门 1

关闭编辑器完整编译。此时动画蓝图里应该能搜到 `MoveForward / MoveRight / bIsTargetLocked` 三个变量；行为与之前完全一致（还没有使用它们的逻辑）。

## 5. 第二步：复制并转化 14 个定向动画

### 5.1 源动画与命名对照表

在 `/Game/SwordAnimsetPro/Animations/Root-Motion/` 找到源动画，复制到 `/Game/DKCharacter/AnimBP/Sword/Source/`（建议子目录 `LockedMove/`），按现有规范命名：

| 方向 | Walk 源 | Walk 目标 | Run 源 | Run 目标 |
|---|---|---|---|---|
| F | `Walk_F_Anim` | `A_DK_Sword_Walk_F_InPlace`（**已有，跳过**） | `Run_F_Anim` | `A_DK_Sword_Run_F_InPlace`（**已有，跳过**） |
| F_L | `Walk_F_L_Anim` | `A_DK_Sword_Walk_F_L_InPlace` | `Run_F_L_Anim` | `A_DK_Sword_Run_F_L_InPlace` |
| F_R | `Walk_F_R_Anim` | `A_DK_Sword_Walk_F_R_InPlace` | `Run_F_R_Anim` | `A_DK_Sword_Run_F_R_InPlace` |
| L | `Walk_L_Anim` | `A_DK_Sword_Walk_L_InPlace` | `Run_L_Anim` | `A_DK_Sword_Run_L_InPlace` |
| R | `Walk_R_Anim` | `A_DK_Sword_Walk_R_InPlace` | `Run_R_Anim` | `A_DK_Sword_Run_R_InPlace` |
| B_L | `Walk_B_L_Anim` | `A_DK_Sword_Walk_B_L_InPlace` | `Run_B_L_Anim` | `A_DK_Sword_Run_B_L_InPlace` |
| B_R | `Walk_B_R_Anim` | `A_DK_Sword_Walk_B_R_InPlace` | `Run_B_R_Anim` | `A_DK_Sword_Run_B_R_InPlace` |
| B | `Walk_B_Anim` | `A_DK_Sword_Walk_B_InPlace` | `Run_B_Anim` | `A_DK_Sword_Run_B_InPlace` |

### 5.2 转 In-Place（关键，与你已有 Walk_F 的做法一致）

现有 `A_DK_Sword_Walk_F_InPlace` 就是“Root-Motion 源 → 去除根位移”的产物，新复制的 14 个动画按同参数处理：

1. 打开动画序列 → **Details → 根运动（Root Motion）**；
2. 若 `Enable Root Motion` 为开：用于 BlendSpace 前必须去除根位移（复制序列 → 关闭根运动烘焙，或导入时选 In-Place）；
3. 参考 `A_DK_Sword_Walk_F_InPlace` / `A_DK_Sword_Run_F_InPlace` 的做法保持一致。

主角位移由 `CharacterMovement` 驱动；动画残留根位移会出现“角色被拖走”双重位移。

### 5.3 骨架检查

与现有移动动画同骨架（`SK_Mannequin_Skeleton`），**无需重定向**。确认新动画能直接拖进 `BS_DK_Sword_Locomotion` 同款混合空间即可。

## 6. 第三步：创建两个 2D BlendSpace（8 样本）

### 6.1 `BS_DK_LockedWalk`（步行）

1. `/Game/DKCharacter/AnimBP/Sword/` → 右键 **Animation → Blend Space 2D**，命名 `BS_DK_LockedWalk`，骨架 `SK_Mannequin_Skeleton`；
2. 轴设置：
   - **X Axis**（MoveForward）：`-1` ～ `1`
   - **Y Axis**（MoveRight）：`-1` ～ `1`
   - 每轴 **Grid Divisions（网格分段）= 4**：生成 5×5 = 25 个格点，坐标 `-1 / -0.5 / 0 / 0.5 / 1`——八个样本位置（含 `(±0.5, ±0.5)` 的 45° 点）全部落在格点上；
3. 放置八个样本：**把动画资产从内容浏览器拖到中间网格对应格点**，或右键格点 → **Add Sample @ (x, y)** → 选中样本后在左侧详情设置 Animation（X = MoveForward，Y = MoveRight）：

| 样本 | 坐标 | 动画 |
|---|---|---|
| F | `(1, 0)` | `A_DK_Sword_Walk_F_InPlace`（已有） |
| F_R | `(0.5, 0.5)` | `A_DK_Sword_Walk_F_R_InPlace` |
| R | `(0, 1)` | `A_DK_Sword_Walk_R_InPlace` |
| B_R | `(-0.5, 0.5)` | `A_DK_Sword_Walk_B_R_InPlace` |
| B | `(-1, 0)` | `A_DK_Sword_Walk_B_InPlace` |
| B_L | `(-0.5, -0.5)` | `A_DK_Sword_Walk_B_L_InPlace` |
| L | `(0, -1)` | `A_DK_Sword_Walk_L_InPlace` |
| F_L | `(0.5, -0.5)` | `A_DK_Sword_Walk_F_L_InPlace` |

4. 8 个样本之间留空的位置（如 `(1,1)`）保持空置——它们负责在相邻样本间插值，不需要样本；

### 6.2 `BS_DK_LockedRun`（奔跑）

同 6.1，八个位置换八个 Run 动画。ABP 里按 `GroundSpeed` 阈值在 Walk/Run 两个空间之间切换（阈值与现有普通走跑一致）。

## 7. 第四步：ABP_DK_Sword 加“锁定定向移动”分支

1. 打开 `ABP_DK_Sword`；
2. 状态机新增状态 **`LockedMove`**：内部按 `GroundSpeed` 阈值切 `BS_DK_LockedWalk` / `BS_DK_LockedRun`（阈值与现在普通走跑切换一致）；
3. 过渡：
   - 现有 Locomotion → `LockedMove`：`bIsTargetLocked && bShouldMove`；
   - `LockedMove` → 现有 Locomotion：`!bIsTargetLocked || !bShouldMove`；
4. `LockedMove` 与 Idle 之间保留 0.2 秒左右 Blend，保证刚锁定时站立无跳变（锁定时站立 Idle 可继续用现有 `A_DK_Sword_Idle`）；
5. `LockedMove` 的输入接 `MoveForward / MoveRight`，**不是**速度轴；
6. 确认攻击/闪避/格挡蒙太奇仍能从 `DefaultSlot` 覆盖（Slot 优先于状态机输出，无需改）。

## 8. 调参速查

| 想调整 | 去哪里 |
|---|---|
| 侧移脚部跨步幅度 | 定向动画本身或 BlendSpace 播放速率 |
| 任意角度混合软硬 | `BS_DK_LockedWalk/Run` 插值参数（默认线性即可） |
| 锁定中步速观感（滑步感） | 匹配「动画步频 ↔ GroundSpeed」：调 BlendSpace 播放速率或锁定时 `MaxWalkSpeed` |
| 锁定/解锁切换混合时长 | 两个状态间过渡时长（0.1～0.3 起调） |
| 某方向脚部方向怪 | 检查该方向样本坐标是否与表格一致（最常见：X/Y 轴接反） |

## 9. 验收清单

1. 锁定敌人后向左推摇杆：左移步态正确，角色保持面向目标；
2. 向右/向后同理，四正方向脚部无滑步；
3. **四个 45° 方向：真实 45° 步态**（`F_L / F_R / B_L / B_R` 样本直接命中，不是混合近似）；
4. 20°、70° 等任意角度：相邻样本平滑插值，无跳变；
5. 锁定中 Shift 奔跑：八向奔跑动画同样正确；
6. 走 ↔ 跑切换顺畅（沿用现有速度阈值）；
7. 解锁瞬间：0.2 秒内平稳回到普通 Walk/Run，无方向动画残留；
8. 锁定中攻击：攻击蒙太奇正常覆盖，收招后回锁定移动；
9. 锁定中闪避：闪避方向（目标相对）与移动方向一致；
10. 八向闪避后长按进入奔跑：锁定状态仍走锁定奔跑动画。

## 10. 常见问题与排查

**10.1 锁定后没切换到定向动画**

1. AnimBP 变量是否编译出来（`bIsTargetLocked / MoveForward / MoveRight`）；
2. 过渡条件写的是 `bIsTargetLocked && bShouldMove`（原地站立停留在 Idle，不进入 LockedMove——预期行为）；
3. `GetTargetLockComponent()` 是否已锁定（看组件或 GAS Debugger 的 `DK.Status.TargetLocked`）。

**10.2 侧移方向错 90°（左变前/右变后）**

投影基准错了：确认 `MoveForward/MoveRight` 用的是**角色** Forward/Right，AnimBP 的轴顺序是 X=Forward、Y=Right，不要接反。

**10.3 某个 45° 方向完全无效（走到最近正方向）**

该样本没放到格点上：确认每轴 **Grid Divisions = 4**（5×5 个格点），且坐标是 `(0.5, ±0.5)` / `(-0.5, ±0.5)` 这类**格点**坐标——BlendSpace 2D 的样本会吸附到格点，放偏了等于没放。若格点不在 ±0.5 处，说明 Grid Divisions 被改过（不是 4）。

**10.4 角色有明显滑步**

动画步频与移动速度不匹配：对照动画实际步速，调 BlendSpace 播放速率或锁定时 `MaxWalkSpeed`。

**10.5 角色被“拖走”（移动距离远超输入）**

新复制的动画残留根位移：回到 5.2 检查、转 In-Place。这是 Root-Motion 目录素材进 BlendSpace 最常见的坑。

**10.6 锁定中“跑着后退”太快**

预期行为（八向跑库支持后向奔跑）。若觉得违和，先做“锁定中 Shift 不生效”再考虑后移降速——见 11。

## 11. 本册刻意不做

- **锁定奔跑限制 / 后移降速**：涉及移动速度规则，单独调优；
- **锁定专用闪避动画**：八向闪避已有自己的动画集与方向计算；
- **锁定站姿专用 Idle**：首版复用现有 Idle，需要时再补；
- **运动中攻击的方向动画采样**：攻击蒙太奇由 Slot 覆盖，不参与移动方向混合。

## 12. 推荐实施顺序与编译闸门

```text
阶段 1：FirstDKAnimInstance 三个输入          → 完整编译（闸门 1）
阶段 2：复制 14 个定向动画并转 In-Place        （编辑器，参考现有 Walk_F 的做法）
阶段 3：BS_DK_LockedWalk / BS_DK_LockedRun（8 样本，网格 5×5）
阶段 4：ABP_DK_Sword 加锁定分支              （编辑器）
阶段 5：按第 9 节验收
```

> 阶段 1 完成后必须完整编译；资产命名沿用 `A_DK_Sword_*_InPlace` 规范，与现有 `Walk_F` 完全同构。

---

## 13. 最终结构检查

```text
SwordAnimsetPro/Root-Motion（8 向 Walk/Run 源库）
  ▼ 复制 + 转 In-Place
/DKCharacter/AnimBP/Sword/Source/A_DK_Sword_{Walk,Run}_{F,F_L,F_R,L,R,B_L,B_R,B}_InPlace
  ▼
BS_DK_LockedWalk / BS_DK_LockedRun
  2D 空间：X=MoveForward, Y=MoveRight，每轴 Grid Divisions=4（5×5 格点）
  样本：F(1,0) F_L(0.5,-0.5) L(0,-1) F_R(0.5,0.5) R(0,1) B_L(-0.5,-0.5) B(-1,0) B_R(-0.5,0.5)
  ▼
ABP_DK_Sword 状态机
├─ Locomotion（现状：BS_DK_Sword_Locomotion，未锁定）
└─ LockedMove（新增：bIsTargetLocked && bShouldMove 进入）

ADKCharacter → FirstDKAnimInstance 每帧写入：
bIsTargetLocked / MoveForward / MoveRight（角色本地轴投影，连续值不量化）
```
