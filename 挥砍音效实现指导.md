# 挥砍音效（Woosh）实现指导

> 适用项目：First（UE 5.6 / C++ / GAS / Behavior Tree / Motion Warping）
>
> 文档状态：C++ `UAnimNotify_PlaySFX` 已实现；以下为完整编辑器操作指引（中文界面）
>
> 音效定位：纯听觉反馈（挥砍破空声），不参与任何玩法判定；命中/格挡/弹反等结果音另走事件回调

---

## 1. 已完成（C++ 部分）

- [x] 新增 `AnimNotify_PlaySFX`（公开头文件 + 私有实现）
- [x] 支持多音效变体随机：每段攻击在 `Sounds` 数组里随机挑一个（对应 01~07 变体，消除重复感）
- [x] 支持音量 `VolumeMultiplier`、Pitch 随机范围（默认 0.95～1.05）
- [x] 可选衰减资产 `AttenuationOverride`、可选骨骼挂点 `Socket Name`
- [x] 防御写法与 `ANS_WeaponTelegraph` 一致：动画预览器 / 无 Mesh / 空列表时安全返回

---

## 2. 第一步：编译项目（新增了 C++ 类，必须先编译）

> 如果 UE 编辑器现在开着，**先关闭编辑器**再编译（不然 DLL 被占用会失败）。

1. 打开 `D:\UE2026\First\First.sln`（Visual Studio）；
2. 顶部工具栏**两个下拉框**确认：左边选 `Development Editor`，右边选 `Win64`；
3. 菜单栏 → `生成(B)` → `生成解决方案`（快捷键 `Ctrl+Shift+B`）；
4. 等待底部"输出"窗口出现 `Build succeeded`（几分钟到十几分钟）；
5. 关掉 Visual Studio，双击 `D:\UE2026\First\First.uproject` 打开 UE 编辑器。

> 备选：不关编辑器，直接按 `Ctrl+Alt+F11` 触发 Live Coding。新增类第一次建议用上面的完整编译，Live Coding 偶尔对新增类不生效。

## 3. 第二步：打开攻击蒙太奇

1. 编辑器左下角打开**内容浏览器**（Content Browser）；
2. 左侧目录依次展开：`Content` → `DKCharacter` → `AnimBP` → `Sword` → `Montages` → `Attack`；
3. 双击打开 `AM_DK_Sword_Attack_1`（蓝色蒙太奇图标，名字带 `AM_` = Anim Montage）。

窗口底部是**动画时间轴**，中间有若干横条叫**通知轨道**（Notify Track），上面已有的小块（如 `Slash Trail`、`Weapon Collision`、`Combo Window`）就是已有的通知。

## 4. 第三步：添加 `Play SFX` 通知

1. 时间轴空白处**右键**（任意一条通知轨道上）→ 弹出菜单选 **`添加通知...`**（Add Notify...）；
2. 在弹出的搜索框里输入 `SFX`（英文，它显示的名字是 `Play SFX`，与中文界面无关）；
3. 点选 **`Play SFX`** 这一项，时间轴上就会出现一个蓝色小方块通知；
4. 用鼠标拖动它，放到挥剑开始那一段——**具体位置看第 5 节**；
5. 点选这个通知方块，编辑器右侧**细节**（Details）面板里出现它的属性：
   - `Sounds` → 点右边的小三角展开 → 点 **`+`** 添加 7 个空槽；
   - 在内容浏览器里打开 `Content/Weapons_woosh/Cues/`，把 `Woosh_Sword_Normal_01_Cue` 到 `07_Cue` 这 7 个**声音提示**（Sound Cue）资产，依次拖入 7 个槽中；
   - `Volume Multiplier` 填 `0.8`；
   - `Pitch Range` 保持默认 `0.95 ~ 1.05`；
   - `Socket Name` 留空（`None`）；`Attenuation` 留空。

> 如果不小心拖错了槽：展开 `Sounds`，点某个槽右边 `X` 删除；要重新放，直接拖新资产覆盖。

## 5. 第四步：放到正确的帧（重要）

原则：**破空声先于命中声**。你不需要懂动画，照着已有的窗口放：

1. 在时间轴上找到该攻击段已有的 **`Slash Trail`**（刀光窗口）或 **`Weapon Collision`**（伤害窗口，英文类名 `FirstANS_WeaponCollision`）通知块；
2. 看它最左边（开始帧）那条线对应的刻度；
3. 把你的 `Play SFX` 方块拖到**它开始前 2~4 帧**的位置（用时间轴上方的时间刻度对齐，例如碰撞窗口在 `0.30` 秒开始，`Play SFX` 放在 `0.26 ~ 0.28` 秒）；
4. 放好后**保存蒙太奇**（`Ctrl+S`）。

**各 Montage 放置清单：**

| Montage | 放置数量 | 音效（放 `Sounds` 里） |
|---|---|---|
| `AM_DK_Sword_Attack_1~4` | 各 1 个 | `Woosh_Sword_Normal_01~07_Cue`（每个通知 7 个） |
| `AM_Boss_Attack_Normal` | 1 个 | `Weapon_Woosh_01~07_Cue` |
| `AM_Boss_Attack_Combo` | 3 个（三段各 1） | `Weapon_Woosh_01~07_Cue` |
| `AM_Boss_RetreatChargedSlash` | 1 个（只放前冲斩段） | `Woosh_Sword_Heavy_01~07_Cue` |

BOSS 蒙太奇路径：`Content/Enemy/AnimBP/Montages/Attacks/`（三连与重斩在里面，`AM_Boss_Attack_Normal` 在同级 `Montages/` 下）。

> 后撤蓄力斩：**后撤段和蓄力段不放**挥砍音（蓄力阶段已有 `Weapon Telegraph` 白光提示音，避免两种音打架）；只在前冲斩的挥剑帧放 1 个。

## 6. 第五步：验收测试

1. 关掉 Montage 窗口，点编辑器顶部工具栏的 **`播放`** 按钮（PIE）进入 `Map`；
2. 按 `1` 键拔出剑，**鼠标左键**连按轻攻击：
   - 每段挥剑都应听到破空声；
   - 连续 7 次以上，听到的音高应有轻微差别（随机 Pitch 生效），且不会连续两次一模一样；
3. 快速测试 BOSS：走到 BOSS 附近触发战斗，观察它普攻/三连时挥剑有声、蓄力阶段只有白光提示音；
4. 验收标准：**挥砍声在听到命中声之前响起**，且音量明显弱于后续的命中音。

## 7. 常见问题（排查用）

| 症状 | 原因与解决 |
|---|---|
| 右键"添加通知"搜不到 `SFX` | 没编译成功或编辑器没重启。回到第 2 步，确认 VS 输出 `Build succeeded`，然后完全关闭编辑器重开 |
| 通知方块是灰的或显示异常 | 编译后第一次打开旧资产通常会自动刷新；不行就重新双击打开资产，或用内容浏览器右键该资产 → `刷新` |
| 有声音但听不见 | 编辑器音量太小；先把 `Volume Multiplier` 临时改 `2.0` 试听，能听到后再调回 0.8 |
| 同一段攻击响了好几声 | `Sounds` 里拖 7 个是对的；但如果你是**放了 7 个 Notify 方块**，删掉多余的（每段攻击只保留 1 个方块） |
| 预览窗口里动画也响 | 正常现象：预览器会真实播放 Montage 通知（只看动画时可以忽略，不影响游戏内表现） |
| BOSS 攻击没有声音 | 确认是 `AM_Boss_Attack_Normal`/`AM_Boss_Attack_Combo`，且通知放在了挥剑帧；三连击三段各需 1 个通知 |

## 8. 后续规划（不急着做）

- 命中音（`Impact_*`）：走武器碰撞/防御判定回调，**不能**用 `Play SFX` 通知（否则格挡失败也响）——下个阶段再接；
- 脚步音、拔剑音：全部复用 `Play SFX`，同一个通知类，不同资产而已；
- 如果后期音效变多：再统一建 `Audio/` 目录、Sound Class 与衰减模板，然后给 `Play SFX` 填 `Attenuation` 覆盖默认。
