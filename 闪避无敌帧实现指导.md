# 闪避无敌帧实现指导

> 适用项目：`First`（UE 5.6 **中文版** / C++ / GAS）
> 前置条件：8 向闪避已经能正常播放（`UFirstGA_DKDodge` + 8 个闪避蒙太奇）；伤害链路正常（`UFirstGE_Damage` → `GEExecCalc_DamageTaken` → 扣血）
> 本册目标：闪避动画的指定窗口内，玩家**免疫一切伤害**（所有伤害来源都生效）

---

## 0. 先看结论（本册完成后得到什么）

```text
玩家按闪避 → 播放 8 向闪避蒙太奇
  → 蒙太奇上的"无敌窗口"Notify 打开时：加 DK.Status.Invincible 标签
  → 窗口内任何伤害：ExecCalc 直接判 0（不掉血、不触发受击）
  → 窗口结束：移除标签
  → 闪避异常被打断时：Dodge 能力 EndAbility 兜底移除，不会残留无敌
```

---

## 1. 设计说明（先读）

### 1.1 为什么用独立标签，而不是 DK.Status.Dodging

`DK.Status.Dodging` 覆盖整个闪避（用于攻击/闪避互斥），而**无敌帧通常只占闪避动画的一部分**（起手和收尾没有）。两者时长不同，所以用独立的 `DK.Status.Invincible` 标签，由动画窗口精确控制。

### 1.2 为什么检查放在 ExecCalc

你的所有伤害（主角攻击、BOSS 攻击、以后的陷阱/投技）都走 `UFirstGE_Damage` → `GEExecCalc_DamageTaken`。在 ExecCalc 开头判断目标标签，**一处修改，全部伤害来源自动生效**。掉血为 0 → 受击反应（监听 Health 下降）也不会触发，等于"无敌帧期间不扣血也不吃僵直"。

### 1.3 窗口控制用动画 Notify State

和武器碰撞（`FirstANS_WeaponCollision`）、动作取消（`ANS_ActionCancel`）同一模式：在闪避蒙太奇上摆放"无敌窗口"，精确控制哪段时间无敌，以后调手感只改动画，不动 C++。

---

## 2. 本册改动总览（按文件找位置）

| 文件/资产 | 路径 | 操作 | 新增/修改内容 |
|---|---|---|---|
| `MyGameplayTags.h/.cpp` | `Source/First/Public/`<br>`Source/First/Private/` | 修改 | 新增 `DK.Status.Invincible` 标签 |
| `ANS_InvincibleWindow.h/.cpp` | `Public/Notifies/`<br>`Private/Notifies/` | 新建 | 无敌窗口 Notify State（开/关标签） |
| `GEExecCalc_DamageTaken.cpp` | `Private/AbilitySystem/GEExecCalc/` | 修改 | 开头加"目标无敌 → 伤害为 0"检查 |
| `FirstGA_DKDodge.h/.cpp` | `Public/AbilitySystem/Abilities/DK/`<br>`Private/AbilitySystem/Abilities/DK/` | 修改 | `EndAbility` 兜底移除无敌标签 |
| 8 个闪避蒙太奇 | `/Game/DKCharacter/AnimBP/Sword/Montages/Dodge_8Way/` | 配置 | 每个都加 `Invincible Window` 通知状态 |

---

## 3. 第一步：新增 Gameplay Tag

> 【改动位置】文件 `MyGameplayTags.h`
> 【新增内容】`DK.Status.Invincible` 声明

在 `DK_Status_Dodging` 声明附近新增：

```cpp
	// 无敌帧：只在闪避动画的"无敌窗口"内存在，由 Notify 开关。
	FIRST_API UE_DECLARE_GAMEPLAY_TAG_EXTERN(DK_Status_Invincible);
```

> 【改动位置】文件 `MyGameplayTags.cpp`
> 【新增内容】对应定义

```cpp
	UE_DEFINE_GAMEPLAY_TAG(DK_Status_Invincible, "DK.Status.Invincible");
```

编译闸门 1。

---

## 4. 第二步：新建 UFirstANS_InvincibleWindow（无敌窗口 Notify）

用编辑器向导创建：

1. **工具（Tools）→ 新建 C++ 类（New C++ Class）** → 勾选 **显示所有类（Show All Classes）**；
2. 搜索并选择父类 **`AnimNotifyState`**（即 `UAnimNotifyState`）；
3. 类名填写 `ANS_InvincibleWindow`（不要带 U 前缀，向导会自动加 U，最终类名 `UFirstANS_InvincibleWindow`）；
4. 文件放到 `Notifies/`，整体替换为下面的内容（新建文件）。

> 【改动位置】新建文件 `ANS_InvincibleWindow.h` / `ANS_InvincibleWindow.cpp`
> 【新增内容】Begin 加 `DK.Status.Invincible`，End 移除；死亡时不加

`ANS_InvincibleWindow.h`：

```cpp
#pragma once

#include "CoreMinimal.h"
#include "Animation/AnimNotifies/AnimNotifyState.h"
#include "ANS_InvincibleWindow.generated.h"

/**
 * 无敌窗口（Invincible Window）。
 * 放在闪避蒙太奇上：Begin 给角色加 DK.Status.Invincible，End 移除。
 */
UCLASS(meta = (DisplayName = "Invincible Window"))
class FIRST_API UFirstANS_InvincibleWindow : public UAnimNotifyState
{
	GENERATED_BODY()

public:
	virtual void NotifyBegin(
		USkeletalMeshComponent* MeshComp,
		UAnimSequenceBase* Animation,
		float TotalDuration,
		const FAnimNotifyEventReference& EventReference) override;

	virtual void NotifyEnd(
		USkeletalMeshComponent* MeshComp,
		UAnimSequenceBase* Animation,
		const FAnimNotifyEventReference& EventReference) override;
};
```

`ANS_InvincibleWindow.cpp`：

```cpp
#include "Notifies/ANS_InvincibleWindow.h"

#include "AbilitySystem/FirstAbilitySystemComponent.h"
#include "Character/BaseCharacter.h"
#include "MyGameplayTags.h"

void UFirstANS_InvincibleWindow::NotifyBegin(
	USkeletalMeshComponent* MeshComp,
	UAnimSequenceBase* Animation,
	float TotalDuration,
	const FAnimNotifyEventReference& EventReference)
{
	Super::NotifyBegin(MeshComp, Animation, TotalDuration, EventReference);

	ABaseCharacter* Character = MeshComp
		? Cast<ABaseCharacter>(MeshComp->GetOwner())
		: nullptr;
	UFirstAbilitySystemComponent* ASC = Character
		? Character->GetFirstAbilitySystemComponent()
		: nullptr;

	if (!ASC)
	{
		return;
	}

	// 死亡后不再进入无敌。
	if (ASC->HasMatchingGameplayTag(MyGameplayTags::Shared_Status_Dead))
	{
		return;
	}

	ASC->AddLooseGameplayTag(MyGameplayTags::DK_Status_Invincible);
}

void UFirstANS_InvincibleWindow::NotifyEnd(
	USkeletalMeshComponent* MeshComp,
	UAnimSequenceBase* Animation,
	const FAnimNotifyEventReference& EventReference)
{
	Super::NotifyEnd(MeshComp, Animation, EventReference);

	ABaseCharacter* Character = MeshComp
		? Cast<ABaseCharacter>(MeshComp->GetOwner())
		: nullptr;
	UFirstAbilitySystemComponent* ASC = Character
		? Character->GetFirstAbilitySystemComponent()
		: nullptr;

	if (!ASC)
	{
		return;
	}

	ASC->RemoveLooseGameplayTag(MyGameplayTags::DK_Status_Invincible);
}
```

编译闸门 2。

---

## 5. 第三步：ExecCalc 增加"目标无敌 → 伤害为 0"

> 【改动位置】文件 `GEExecCalc_DamageTaken.cpp`，`Execute_Implementation` 开头
> 【新增内容】目标带 `DK.Status.Invincible` 时直接 return，不输出任何伤害

在 `Execute_Implementation` 里、`EvaluateParameters` 赋值完成之后（计算伤害之前）新增：

```cpp
	// 无敌帧：目标带 DK.Status.Invincible 时，本次伤害完全无效。
	// 掉血为 0 → 受击反应（监听 Health 下降）也不会触发。
	const FGameplayTagContainer* TargetTags = EvaluateParameters.TargetTags;
	if (TargetTags && TargetTags->HasTagExact(MyGameplayTags::DK_Status_Invincible))
	{
		return;
	}
```

`MyGameplayTags.h` 已经在这个文件里 include 了，不用再加。

编译闸门 3。

---

## 6. 第四步：Dodge 能力 EndAbility 兜底移除

> 【改动位置】文件 `FirstGA_DKDodge.h`
> 【新增内容】`EndAbility` 覆写声明

在 `protected:` 区域新增：

```cpp
	// 兜底清理：无论闪避怎么结束，都确保无敌标签被移除。
	virtual void EndAbility(
		const FGameplayAbilitySpecHandle Handle,
		const FGameplayAbilityActorInfo* ActorInfo,
		const FGameplayAbilityActivationInfo ActivationInfo,
		bool bReplicateEndAbility,
		bool bWasCancelled) override;
```

> 【改动位置】文件 `FirstGA_DKDodge.cpp`
> 【新增内容】`EndAbility` 实现

新增 include（如果还没有）：

```cpp
#include "AbilitySystem/FirstAbilitySystemComponent.h"
```

文件末尾新增：

```cpp
void UFirstGA_DKDodge::EndAbility(
	const FGameplayAbilitySpecHandle Handle,
	const FGameplayAbilityActorInfo* ActorInfo,
	const FGameplayAbilityActivationInfo ActivationInfo,
	bool bReplicateEndAbility,
	bool bWasCancelled)
{
	// 无敌窗口的 NotifyEnd 正常会移除标签；
	// 这里兜底处理"蒙太奇被异常打断、NotifyEnd 没触发"的情况。
	if (UFirstAbilitySystemComponent* ASC = GetFirstAbilitySystemComponentFromActorInfo())
	{
		ASC->RemoveLooseGameplayTag(MyGameplayTags::DK_Status_Invincible);
	}

	Super::EndAbility(Handle, ActorInfo, ActivationInfo, bReplicateEndAbility, bWasCancelled);
}
```

编译闸门 4。

---

## 7. 第五步：闪避蒙太奇加无敌窗口

对 **8 个闪避蒙太奇**分别操作：

```text
AM_DK_Sword_Dodge_F / FR / R / BR / B / BL / L / FL
```

1. 双击打开闪避蒙太奇；
2. 在通知轨道上右键 → **添加通知状态（Add Notify State）** → 选择 **Invincible Window**；
3. 把窗口拖到**起手之后、收尾之前**的位置（推荐：从动画前 1/3 开始，到 2/3 结束；起手和收尾不无敌）；
4. 8 个蒙太奇都放一遍，保存。

> 手感调优：窗口太短躲不掉、太长像"全程无敌"。先按上面推荐值测，再逐帧微调。

---

## 8. 编译顺序汇总

```text
闸门 1：MyGameplayTags（DK.Status.Invincible）
闸门 2：UFirstANS_InvincibleWindow
闸门 3：GEExecCalc_DamageTaken（无敌检查）
闸门 4：UFirstGA_DKDodge（EndAbility 兜底清理）
最后：8 个闪避蒙太奇加 Invincible Window
```

每完成一个闸门做一次普通 Build。

---

## 9. 测试清单

| 测试 | 操作 | 预期结果 |
|---|---|---|
| 窗口内无敌 | 闪避中被打 | 不掉血、不触发受击动画 |
| 起手被打 | 闪避刚起手时 | 掉血（起手不在窗口内） |
| 收尾被打 | 闪避快结束时 | 掉血（收尾不在窗口内） |
| BOSS 攻击 | 无敌帧内被 BOSS 三连/普攻命中 | 伤害为 0，不扣血 |
| 体力不足 | 体力耗尽时按闪避 | 闪避失败，被打正常掉血 |
| 无残留 | 闪避被打断后再看标签 | `DK.Status.Invincible` 已移除 |
| 死亡 | 死后检查 | 不再进入无敌 |
| 调试 | 控制台 `ShowDebug AbilitySystem` | 无敌窗口内能看到 `DK.Status.Invincible` 标签 |

---

## 10. 常见问题与排查

| 现象 | 原因 | 修复 |
|---|---|---|
| 完全没有无敌 | 蒙太奇没加窗口，或标签名不一致 | 检查 7 与 `DK.Status.Invincible` 拼写 |
| 全程无敌 | 窗口盖满整个动画 | 把窗口缩短，留出起手/收尾 |
| 无敌残留（一直免疫） | NotifyEnd 没触发，或 EndAbility 清理没加 | 检查 6 的兜底移除 |
| 窗口内还是扣血 | ExecCalc 检查没生效 | 确认检查代码在伤害计算**之前**、标签一致 |
| 无敌帧还吃受击 | 伤害没走 ExecCalc（如直接 SetHealth） | 所有伤害必须通过 `UFirstGE_Damage` → ExecCalc |

---

## 11. 后续扩展

1. **格挡 / 弹反复用**：格挡状态也加进"免疫标签集合"，ExecCalc 判断"目标带任一免疫标签"即可，公式不用改；
2. **按动画曲线控制窗口**：用蒙太奇曲线驱动标签开关，比手动拖窗口更精细；
3. **投技无视无敌**：投技伤害打上"忽略无敌"标记（如给伤害 Spec 加一个 Tag，ExecCalc 先判断它再判断无敌）。
