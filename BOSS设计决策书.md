# BOSS 设计决策书

> 适用项目：`First`（UE 5.6 / C++ / GAS / 内置 AI）  
> 本文是 BOSS 战的**设计大纲与决策清单**，只罗列方向和待定项，不做详细实现。  
> 详细设计（代码、资产、数值、测试）在决策确定后再分别展开。

---

## 0. 项目目标

本项目核心目标是 **BOSS 战**。玩家拥有：移动、跳跃、连招、闪避、格挡/弹反（规划中）、精力系统（规划中）。

BOSS 需要拥有：区域巡逻、索敌、追击、轻攻击、重攻击、技能、投技、普通僵直、大僵直、韧性、处决、死亡。

---

## 1. 总体架构（已确认方向）

```text
① 决策层  AIController + AIPerception + BehaviorTree + Blackboard
② 行为层  GameplayAbility（攻击/技能/投技/僵直/处决/死亡）
③ 表现层  AnimBP + AnimMontage + Notify（伤害窗口/事件/位移）
④ 数据层  AttributeSet + GameplayEffect + GameplayTag
```

已确认原则：

- 使用 UE 自带的 AI 感知与行为树，不引入第三方插件；
- 所有战斗动作通过 GAS 能力实现，与玩家侧同一套机制；
- 状态互斥通过 GameplayTag 表达（攻击中、僵直中、死亡中等）。

---

## 2. 已确认的决策记录

以下决策已在对话中确认，后续详细设计以此为准：

| 编号 | 决策 | 结论 |
|---|---|---|
| A1 | BOSS 拥有哪些能力 | 区域巡逻、索敌、轻攻击、重攻击、技能、投技、僵直、死亡 |
| A2 | AI 方案 | UE 内置 AIPerception + BehaviorTree + Blackboard |
| A3 | 韧性大僵直阈值 | 韧性削减到最大值的 **50%** 触发大僵直 |
| A4 | 大僵直后韧性 | **不重置**，可继续削减 |
| A5 | 韧性归零 | 触发 **处决**，播放处决动画 |
| A6 | 韧性自动恢复 | 最后一次受伤后 **5 秒**无伤害，一次性回满 |
| A7 | 武器韧性削减 | 主角武器附带韧性削减数值，轻/重攻击数值**不同** |

---

## 3. BOSS 行为模块大纲

### 3.1 区域巡逻

目标：BOSS 在未索敌时按固定路径或区域移动。

方案要点：

```text
巡逻点列表（场景摆放或数据资产配置）
到达当前点 → 等待片刻 → 前往下一个点
```

待决策：

| 编号 | 问题 | 选项 | 推荐 |
|---|---|---|---|
| D1 | 巡逻点如何配置 | A. 场景中摆放 Actor 数组；B. 数据资产存坐标 | A（直观，便于摆 BOSS 战区域） |
| D2 | 巡逻移动速度 | 步行/奔跑可配 | 巡逻步行 250，追击奔跑 500（2026-08 确认） |

### 3.2 索敌与追击

目标：通过视觉感知发现玩家，进入战斗并追击。

方案要点：

```text
UAISenseConfig_Sight：检测半径、半角、刺激记忆时间
发现玩家 → 写入 Blackboard（TargetActor）
战前视觉判定失败 → 放弃警戒，回巡逻
正式入战后丢失视野 → 保持锁定；仅在离开 BOSS 领地、死亡或目标失效时脱战
```

待决策：

| 编号 | 问题 | 选项 | 推荐 |
|---|---|---|---|
| D3 | 索敌半径 | 数值可配 | 800～1200 |
| D4 | 索敌视野角度 | 数值可配 | 左右各 60°（共 120°） |
| D5 | 视觉刺激最大记忆年龄 | 数值可配 | 5 秒；它不是视觉失败回调延迟，也不再用于正式战斗脱战 |

### 3.3 攻击系统（轻攻击 / 重攻击 / 技能）

目标：BOSS 在攻击距离内按规则选择招式并造成伤害。

方案要点：

```text
每个招式 = 一个 GameplayAbility
  ├─ 播放 Montage
  ├─ Notify 打开武器碰撞（复用 FirstANS_WeaponCollision）
  ├─ 命中 → MeleeHit 事件 → 伤害 GE
  └─ 动画结束 → 能力结束 → 通知行为树“攻击完成”
```

待决策：

| 编号 | 问题 | 选项 | 推荐 |
|---|---|---|---|
| D6 | 攻击选择策略 | A. 随机；B. 按距离；C. 循环队列；D. 按阶段解锁 | 先随机 + 距离条件，阶段解锁后续再加 |
| D7 | 位移型攻击 | 用根运动还是代码位移 | 根运动（动画即位移，表现自然） |
| D8 | 攻击能力实现 | 纯 C++；C++ 基类 + 蓝图配置 | C++ 基类 + 蓝图配置数值（学习成本与灵活平衡） |
| D9 | 每种攻击的韧性削减 | 轻/重/技能各自数值 | 已确认轻重不同，数值待定 |

### 3.4 投技

目标：近距离抓取玩家，播放抓取演出并结算伤害。

方案要点：

```text
距离检测（在投技距离内）
BOSS 播放抓取前摇动画
动画 Notify：
  - 锁定玩家位置
  - 把玩家位移/附着到抓取 Socket
  - 通过 GameplayEvent 触发玩家“被投技”能力
玩家侧：暂停输入控制、播放被抓动画
BOSS 播放摔投动画 → 结算伤害 → 释放玩家
```

待决策：

| 编号 | 问题 | 选项 | 推荐 |
|---|---|---|---|
| D10 | 投技抓取距离 | 数值可配 | 120～160 |
| D11 | 投技是否计入韧性削减 | A. 计入；B. 不计入 | A（统一削减规则） |
| D12 | 被投技期间玩家输入 | 完全禁用移动/闪避/攻击 | 完全禁用，释放后恢复 |

### 3.5 普通僵直

目标：玩家命中 BOSS 时，BOSS 短暂中断当前动作。

方案要点：

```text
命中 → 应用僵直 GE（添加 Boss.Status.Staggered 标签）
行为树检测到标签 → 取消当前攻击 → 播放普通受击动画
动画结束 → 移除标签 → 回战斗
```

待决策：

| 编号 | 问题 | 选项 | 推荐 |
|---|---|---|---|
| D13 | 是否任何攻击都能打断 | A. 是；B. 仅部分攻击 | A（先全部可打断，后续按招式调整） |
| D14 | 普通僵直是否也削减韧性 | A. 是；B. 否 | A（每次命中都削韧） |

### 3.6 韧性系统（已确认）

```text
Poise / MaxPoise 属性
玩家命中 → 按武器数值削减韧性
韧性 ≤ MaxPoise × 50% → 大僵直
大僵直期间韧性不再自动恢复，可继续削减
韧性归零 → 处决
最后一次受伤后 5 秒无伤害 → 韧性一次性回满
```

待决策：

| 编号 | 问题 | 选项 | 推荐 |
|---|---|---|---|
| D15 | Poise/MaxPoise 放哪个属性集 | A. 加进 FirstAttributeSet；B. 独立 BossAttributeSet | A（学习阶段简单；以后想拆再拆） |
| D16 | 大僵直动画结束后韧性 | 不重置（已确认 A4），继续沿用 5 秒回满规则 | 按 A4 |

### 3.7 处决

目标：韧性归零时，玩家可对 BOSS 执行处决演出。

方案要点：

```text
韧性归零 → 添加 Boss.Status.Executable 标签
玩家接近 → 交互或自动触发处决能力
玩家播放处决演出动画（可与 BOSS 位置对齐）
BOSS 播放被处决动画 → 结算“玩家当前 AttackPower × 10”的大量原始伤害
├─ 生命归零：进入现有死亡链
└─ 仍有生命：处决演出结束后恢复战斗
```

待决策：

| 编号 | 问题 | 选项 | 推荐 |
|---|---|---|---|
| D17 | 处决触发方式 | A. 靠近自动触发；B. 按键触发 | B（按键触发，玩家可控） |
| D18 | 处决结果 | 造成玩家当前 `AttackPower × 10` 的原始伤害；不经过普通 DefensePower/Combo 公式 | 已确认；伤害致死才进入死亡链，否则恢复战斗 |
| D19 | 处决演出期间双方控制 | BOSS 被锁定、玩家播处决动画 | 锁定 BOSS 行为树与移动 |

### 3.8 死亡

目标：生命归零后播放死亡动画并清理战斗状态。

方案要点：

```text
复用现有机制：Health ≤ 0 → 添加 Shared.Status.Dead 标签
行为树死亡分支：停止感知/导航/攻击，播放死亡蒙太奇
可选的清理：关闭碰撞、掉落、战败演出
```

待决策：

| 编号 | 问题 | 选项 | 推荐 |
|---|---|---|---|
| D20 | 死亡后是否保留碰撞 | 保留（可踩踏）/ 关闭 | 关闭或只保留地面碰撞 |
| D21 | 死亡后是否掉落 | 无/掉落物 | 先无，后续扩展 |

---

## 4. BOSS 战流程（扩展项）

以下属于进阶内容，**不进入第一版**，先记录方向：

```text
阶段切换：血量低于 50% / 30% 进入狂暴
狂暴表现：新招式解锁、攻击频率提升、演出变化
战前演出：入场动画、索敌广播
击败演出：死亡动画 + 结算界面
```

待决策：

| 编号 | 问题 | 选项 | 推荐 |
|---|---|---|---|
| D22 | 是否做阶段切换 | 第一版不做 / 后续加 | 后续加 |
| D23 | 是否做战前演出 | 第一版不做 / 后续加 | 后续加 |

---

## 5. 数值与属性规划

BOSS 属性初步规划（最终数值在详细设计阶段定）：

| 属性 | 说明 | 备注 |
|---|---|---|
| Health / MaxHealth | 生命 | 现有 |
| Poise / MaxPoise | 韧性 | 新增 |
| AttackPower | 攻击力 | 现有 |
| DefensePower | 防御力 | 现有 |
| DamageTaken | Meta 伤害 | 现有 |

初始 GE：建议新增 `GE_Boss_Initialize`（独立于 `GE_Enemy_Initialize`），方便单独调 BOSS 数值。

待决策：

| 编号 | 问题 | 选项 | 推荐 |
|---|---|---|---|
| D24 | BOSS 初始 GE | 新建 GE_Boss_Initialize / 复用 GE_Enemy_Initialize | 新建 |

---

## 6. 角色与资产规划

```text
C++：
  ABossCharacter（继承 AEnemyCharacter）
  ABossAIController（感知 + 行为树 + 黑板）
  BOSS 攻击/投技/僵直/处决/死亡 GA
  BTTask / BTService（追击、攻击选择、状态检测）

资产：
  BB_Boss（黑板）、BT_Boss（行为树）、ABP_Boss（动画蓝图）、BP_Boss
  BOSS 蒙太奇（攻击/投技/僵直/处决/死亡）

动画素材：复用 Dark_Knight、SwordAnimsetPro、Katana_Animations 中的
攻击、受击、死亡、抓取类动画（必要时做重定向）
```

> 参考实现：Warrior 完整版位于 `D:\UE2026\ue5-warrior-main`（完整 GAS 动作框架）。
> 与本决策直接相关的可对照文件：`AI/BTTask_RotateToFaceTarget`（平滑转头任务）、
> `AI/BTService_OrientToTargetActor`（持续面向服务）、`Controllers/WAIController`（GenericTeamId 队伍判定 +
> Detour Crowd Avoidance 人群避让）。这些只作为学习参考，不推翻 A1～A7 的既定决策。

待决策：

| 编号 | 问题 | 选项 | 推荐 |
|---|---|---|---|
| D25 | BOSS 角色类 | 新建 ABossCharacter（推荐）/ 直接扩展 AEnemyCharacter | 新建 |
| D26 | 行为树任务实现 | C++ BTTask / 蓝图任务 | 核心用 C++，简单判断用蓝图 |

---

## 7. 实施阶段划分（决策确定后按阶段展开）

| 阶段 | 内容 | 验收标准 |
|---|---|---|
| Phase 1 | 巡逻、索敌、追击、轻攻击 | BOSS 能发现玩家、追到身边、放出一段攻击并造成伤害 |
| Phase 2 | 重攻击、技能、攻击选择、对峙周旋（三距离，见 8.1） | 多种攻击按规则释放，对峙循环闭环 |
| Phase 3 | 普通僵直、韧性、大僵直、处决（可参考受击窗口，见 8.3） | 玩家能打断、削韧、触发大僵直和处决 |
| Phase 4 | 投技 | 抓取、玩家动画、结算、释放全流程 |
| Phase 5 | BOSS 战流程 | 血条 UI、阶段切换、战前/击败演出（可选） |

---

## 8. 外部参考与设计补充（类魂 BOSS AI 视频）

> 参考来源：B 站视频《【UE5】从零开始制作一个类魂BOSS（AI行为篇）》（BV1JC41197B6），字幕全文保存在本地 Codex 输出目录。
> 定位：仅作为设计参考与补充，**不推翻已确认决策 A1～A7**；每条吸收内容都标注了对应实施阶段。

### 8.1 对峙系统：三距离阈值（纳入 Phase 2）

视频观点：ARPG 的 BOSS 不会连续进攻，而是"对峙 ↔ 进攻"往复循环；一对一的对峙用三个距离阈值驱动：

```text
最大距离（MaxDistance）      玩家太远 → 靠近行为
周旋距离（StrafeDistance）   中等距离 → 侧移行为（绕圈周旋）
最小距离（MinDistance）      玩家太近 → 后退行为
```

映射到本项目现有距离体系：

| 视频概念 | 本项目对应 | 建议初始值 |
|---|---|---:|
| 最小距离 | 现有 AttackRadius | 250 |
| 周旋距离 | 新增 StrafeRadius | 500（待定） |
| 最大距离 | 现有 AlertRadius | 800 |

行为树控制规则（照视频）：靠近的进入/退出由"最大距离 + 周旋距离"控制；后退的进入/退出由"最小距离 + 周旋距离"控制；两者之间执行侧移。整体循环为：**攻击 → 对峙（按距离后退/侧移/靠近）→ 再攻击**，正好落实对话中确认的"攻击后对峙一会再进行下一轮攻击"。

### 8.2 招式数据类与连招（纳入 Phase 2 攻击选择）

视频做法：怪物招式用"数据类"存储属性（伤害、攻击范围、技能朝向、下一段连招组）；动画通知窗口生效期间，按战场信息 + 招式属性决定是否有下一段连招、下一段是什么；把选出的招式类交给行为树执行。

与本项目 D6 / D8 对应：攻击能力仍按"每个招式一个 GameplayAbility"（决策书 3.3），但攻击选择建议引入轻量数据驱动：

```text
UBossAttackData（数据资产，蓝图可配）
  ├─ BaseDamage / 韧性削减（轻/重不同，D9）
  ├─ AttackRange（攻击距离过滤）
  ├─ FacingType（朝向：锁定 / 不锁定）
  └─ NextComboGroup（下一段连招候选组，供 Notify 窗口变招）
```

第一版（轻/重攻击手册）先做"固定循环 + 距离过滤"，招式数据类作为 Phase 2 的扩展实现。

### 8.3 受击窗口与韧性（纳入 Phase 3）

视频简化方案：只用两个动画通知开启/关闭"受击区域"；只在攻击后摇等指定窗口内受到攻击才会进入受击；受击时播放受击动画并暂停行为树；解除绑定有两种方式——固定时间（最大连续硬直）或完整播完受击动画（要求玩家连续攻击才能达成最大硬直）。

与本项目 A3～A7 韧性系统互补：

```text
韧性数值系统（保留）：Poise 削减 → 50% 大僵直 → 归零处决
受击窗口（参考）   ：只在指定动画窗口内可被打断，防止 BOSS 被无限连击
```

### 8.4 读指令（后续扩展，不进入第一版）

视频的三个问题：什么状态监听（周旋状态）、监听哪些操作（玩家攻击/回复）、执行哪些反应（玩家回复 → 特定技能攻击；玩家攻击 → 提前结束周旋发起进攻）。本阶段只记录方向，不实现。

### 8.5 巡逻备选：样条线路线

视频用样条线存点做固定路线巡逻；本项目当前用"家点 + 随机可达点"（Phase 1 已实现）。两者可并存：需要固定演出型路线的 BOSS 场景，以后可用样条线或巡逻点数组（D1 扩展）实现。

### 8.6 新增待决策

| 编号 | 决策点 | 推荐默认 |
|---|---|---|
| D27 | 是否采用三距离对峙（最小/周旋/最大） | 采用（纳入 Phase 2） |
| D28 | 周旋距离初始值 | 500（可调） |
| D29 | 是否采用"仅受击窗口可打断" | 采用（防无限连，纳入 Phase 3） |
| D30 | 是否做读指令 | 第一版不做，后续加 |

---

## 9. 待决策总表

| 编号 | 决策点 | 推荐默认 |
|---|---|---|
| D1 | 巡逻点配置方式 | 场景摆放 Actor 数组 |
| D2 | 巡逻速度 | 巡逻步行 250 / 追击奔跑 500（已确认） |
| D3 | 索敌半径 | 800～1200 |
| D4 | 索敌视野角度 | 左右各 60° |
| D5 | 视觉刺激最大记忆年龄 | 5 秒；正式入战后由领地边界决定脱战 |
| D6 | 攻击选择策略 | 随机 + 距离条件 |
| D7 | 位移型攻击 | 根运动 |
| D8 | 攻击能力实现 | C++ 基类 + 蓝图配置 |
| D9 | 轻重技能韧性削减数值 | 待定（轻重不同） |
| D10 | 投技抓取距离 | 120～160 |
| D11 | 投技是否计韧性 | 计入 |
| D12 | 被投技期间输入 | 完全禁用 |
| D13 | 是否所有攻击可打断 | 先全部可打断 |
| D14 | 普通僵直是否削韧 | 削韧 |
| D15 | Poise 属性集 | 加入 FirstAttributeSet |
| D16 | 大僵直后韧性 | 不重置（已确认） |
| D17 | 处决触发方式 | 按键触发 |
| D18 | 处决结果 | 玩家当前 `AttackPower × 10` 的原始伤害；不保证击杀 |
| D19 | 处决演出控制 | 锁定 BOSS |
| D20 | 死亡后碰撞 | 关闭 |
| D21 | 死亡掉落 | 先无 |
| D22 | 阶段切换 | 后续加 |
| D23 | 战前演出 | 后续加 |
| D24 | BOSS 初始 GE | 新建 GE_Boss_Initialize |
| D25 | BOSS 角色类 | 新建 ABossCharacter |
| D26 | 行为树任务实现 | 核心 C++ |
| D27 | 三距离对峙（最小/周旋/最大） | 采用（Phase 2） |
| D28 | 周旋距离初始值 | 500（可调） |
| D29 | 仅受击窗口可打断 | 采用（Phase 3） |
| D30 | 读指令 | 第一版不做 |

---

## 10. 待办事项（不属于 BOSS 决策书）

以下事项已排队，但不在本文档范围内：

```text
1. 玩家八向闪避（以按键方向为基准，S+Shift 向后、A+Shift 向左、单独 Shift 向前）
2. 玩家格挡与弹反（按住右键格挡 / 点按弹反，精力归零大僵直）
3. 精力自动恢复实施（手册已就绪）
4. 连招输入缓冲方案 B 实施（手册已就绪）
```

---

## 11. 下一步

1. 用户确认 D1～D30 中需要修改的决策；
2. 从 **Phase 1（巡逻/索敌/追击/轻攻击）** 开始写详细实现手册；
3. 八向闪避单独出决策与实现手册。
