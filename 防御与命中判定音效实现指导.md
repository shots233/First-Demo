# 命中与防御反馈（音效 + 特效）实现指导

> 适用项目：First（UE 5.6 / C++ / GAS）
>
> 文档状态：C++ 已完成；BP 配置为编辑器手工操作
>
> 组织原则：**反馈按"谁的表现"归属**——攻击者的命中反馈配在攻击者（BP_Boss），
> 防御者的防御表现配在防御者（BP_DKCharacter），血花按"受击者的血"配在受击者（BP_Boss）。

---

## 1. 归属与挂点总览

### A. BOSS 命中玩家（攻击者组织 → 配在 BP_Boss）

```
BOSS 攻击 GA 命中回调（普攻/三连/蓄力斩共用基类）
  → ResolveIncomingMeleeAttack 判定（玩家防御组件，只负责判定）
  → DefenseResult == Damaged（真实命中：没被格挡/弹反/无敌帧）
      → [新增] PlayBossHitPlayerFeedback(TargetActor)   ← FirstBossGameplayAbility 基类辅助
          ├─ 音效 HitPlayerSound + 血花 HitPlayerVFX    ← 配置在 BP_Boss（Boss|Feedback）
          └─ 生成在玩家躯干位置，火花朝来击方向
  → 其他结果（Blocked/Parried/Avoided）→ 无命中反馈（格挡/弹反/闪避时绝不飙血）
```

### B. 玩家命中 BOSS（受击者组织 → 血花配在 BP_Boss）

```
玩家武器碰撞 → DKCombatComponent::OnHitTargetActor（去重后）
  → [新增] PlayHitTargetVFX(HitActor)
      └─ 从被击目标读取 BloodVFX（BP_Boss 的 Boss|Feedback，每个 BOSS 配自己的血）
  → 命中音 HitSound 仍配在玩家侧（武器打击声属于攻击者的剑）
```

### C. 玩家防御表现（防御者组织 → 配在 BP_DKCharacter）

```
玩家防御组件 ResolveIncomingMeleeAttack：
  ├─ 格挡成功 → GuardHitSound + GuardHitVFX
  ├─ 弹反成功 → ParrySuccessSound + ParrySuccessVFX
  └─ 破防     → GuardBrokenSound + GuardBrokenVFX
```

## 2. 已完成（C++）

- [x] `ABossCharacter`：新增 `Boss|Feedback` 3 个属性——`HitPlayerSound`（BOSS 命中玩家音）、`HitPlayerVFX`（BOSS 命中玩家血花）、`BloodVFX`（BOSS 自己被砍的血花）
- [x] `UFirstBossGameplayAbility`：新增基类辅助 `PlayBossHitPlayerFeedback`；三个 BOSS 攻击 GA（普攻/三连/蓄力斩）在判定 Damaged 后调用
- [x] `UDKDefenseComponent`：移除原 HitTaken 音效/特效槽（避免与 BOSS 侧重复），保留格挡/弹反/破防 3+3 槽
- [x] `UDKCombatComponent`：血花改为从被击目标（ABossCharacter）读取 `BloodVFX`；命中音 `HitSound` 保留在玩家侧
- [x] `UDKDefenseComponent`：`Defense|Shake` 2 组槽——`ParryShakeClass/Scale`（弹反，默认 1.2）、`GuardHitShakeClass/Scale`（格挡，默认 0.6）；在弹反/格挡判定点直接播玩家相机震动
- [x] 血花资产已迁入：`Content\Effects\ParticleSystems\Gameplay\Player\P_body_bullet_impact`（依赖材质/贴图同步就位）

## 3. 第一步：编译

1. 关掉 UE 编辑器（或直接 `Ctrl+Alt+F11` Live Coding）；
2. 失败则 VS 里 `生成解决方案`（`Development Editor` / `Win64`）；
3. 重开编辑器。

## 4. 第二步：在 BP 里配置

### 4.1 `BP_Boss` 的 `ABossCharacter`（新：`Boss | Feedback` 类别）

| 属性 | 资产 | 说明 |
|---|---|---|
| `Hit Player Sound` | 暂用 `Weapons_woosh\Impact_08_Cue`（后续可迁 slash3 的 `HitFlesh1~4`） | BOSS 砍中玩家的音效 |
| `Hit Player VFX` | `Effects\ParticleSystems\Gameplay\Player\P_body_bullet_impact` | ⭐ 玩家身上溅血 |
| `Blood VFX` | 同上 `P_body_bullet_impact` | ⭐ BOSS 被砍时溅血 |

### 4.2 `BP_DKCharacter` → `DKDefenseComponent`（防御表现）

- `Defense | SFX`：`Guard Hit Sound`（`Impact_Sword_01_Cue`）、`Parry Success Sound`（`Impact_Sword_02_Cue`）、`Guard Broken Sound`（`Impact_08_strong_Cue`）
- `Defense | VFX`：`Guard Hit VFX` / `Parry Success VFX` / `Guard Broken VFX`（暂空或后续配级联火花）

### 4.3 `BP_DKCharacter` → `DKCombatComponent`（命中的音）

- `Combat | SFX`：`Hit Sound` → `Weapons_woosh\Impact_03_Cue` 或 slash3 `HitFlesh1~4`（武器打击音）
- （`Hit VFX` 槽已移除——血花现在从 BOSS 上读）

### 4.4 镜头震动（弹反/格挡）

**第 1 步：创建两个"摄像机抖动"蓝图资产**

1. 内容浏览器（建议新建目录 `Content\Effects\CameraShake\`）→ 右键 → **蓝图类** → 搜索/选择 **`CameraShakeBase`** → 命名 `BP_Shake_Parry`；
2. 双击打开 → 左上角 **类默认值（Class Defaults）** → 找到 **`Root Shake Pattern`**（根抖动模式）→ 下拉选 **`Wave Oscillator`**；
3. 展开配置参考值：

| 参数 | 弹反 `BP_Shake_Parry` | 格挡 `BP_Shake_Guard` |
|---|---|---|
| Duration（持续时间） | `0.18` | `0.10` |
| Blend In / Out | `0.02` / `0.05` | `0.01` / `0.05` |
| Rotation 振荡（Pitch/Yaw Amplitude） | `0.8` | `0.4` |
| Rotation 频率（Frequency） | `40` | `40` |
| Location（X 振幅） | `4`（可选，更强的冲击感） | `2` |

4. `Ctrl+S` 保存；复制 `BP_Shake_Parry` 一份改名为 `BP_Shake_Guard`，按上表把参数调弱。

**第 2 步：拖入组件槽**

`BP_DKCharacter` → `DKDefenseComponent` → **`Defense | Shake`**：

| 属性 | 值 |
|---|---|
| `Parry Shake Class` | `BP_Shake_Parry` |
| `Parry Shake Scale` | `1.2`（默认） |
| `Guard Hit Shake Class` | `BP_Shake_Guard` |
| `Guard Hit Shake Scale` | `0.6`（默认） |

## 5. 第三步：验收

PIE 进游戏，靠近 BOSS 触发战斗，逐项测：

1. **玩家命中 BOSS** → BOSS 身上溅血花（读取自 `BP_Boss` 的 `Blood VFX`）+ 玩家侧打击音；
2. **BOSS 命中玩家（不格挡）** → 玩家身上溅血花 + `Hit Player Sound`，两者都来自 BP_Boss；
3. **格挡/弹反/无敌帧** → **不飙血、不响命中音**（判定正确性的关键验证）——格挡只响金属声+轻微震屏、弹反只响"叮"+明显震屏；
4. **破防** → 重击声 + （若配了）炸裂；
5. 换 BOSS 血花测试：把 `BP_Boss` 的 `Blood VFX` 换别的特效，玩家命中它时血花跟着变；
6. **震动**：连续格挡 3 次感受轻震；弹反 1 次感受明显一震——震得太轻/太重调 `Shake Scale`（BP 里直接改）或资产里的 Amplitude。

## 6. 常见问题

| 症状 | 原因与解决 |
|---|---|
| 挨打没血花 | ① `BP_Boss` 的 `Hit Player VFX` 没配；② 没编译（新属性不存在）；③ 那一下被格挡/弹反/闪避了（属预期） |
| 打 BOSS 没血花 | ① `BP_Boss` 的 `Blood VFX` 没配；② 训练假人/非 Boss 目标无血花（属预期） |
| 血花出现但没声音 | 对应 Sound 槽没配或留空（属预期）；先随便填一个验证 |
| 血花出现在脚底/头顶 | 躯干偏移固定 90cm；如果 BOSS 特别高大需要可调，告诉我改成属性 |
| 格挡失败也响命中音 | 不可能：命中音只在 GA 判定 `Damaged` 后播（复位检查：确认编译的是最新代码） |

## 7. 后续规划（未做）

- 迁入 slash3 打击音（`HitFlesh1~4`、`Shink` 弹反叮声、`LargeSmash` 破防重击）；
- BOSS 受击动画/死前表现音；
- 拔剑音/死亡音（`Play SFX` 通知即可）；
- hit stop、镜头震动（打击感升级）。
